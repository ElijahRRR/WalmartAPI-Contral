# 店铺占用与产品分配子计划

> **怎么读本文档**:第一部分「现行设计」是**唯一权威**,写代码只看它;
> 第二部分「决策留痕」是历史(拍板记录 + 已作废方案 + 摸底事实),
> 用来回答"当初为什么这么定""这条为什么不做",**不作为实现依据**。
> 两部分冲突时一律以第一部分为准。
>
> **当前状态(2026-08-15)**:A0 收口;A0.5 审计**生产首跑完成**(实测见
> §十一.1)并产出五份明细清单;**A1 占用台账代码就绪**(claims 表 +
> services/claims + store_release + alloc_backfill + list_new 占用闸),
> 待所有者填类目三列、处理清单后生产回填;**A1.5 订单 ASIN 归一生产完成**
> (99.2% 行覆盖,§十一.4);A2 引擎待动工(§十),设计已按所有者 08-15 晚七问定稿(§7.4~7.4g)。
>
> 立案 2026-08-07;历次校准 08-12(四外部仓库摸底)、08-12 晚、08-13、
> 08-15(main 迁入审核系统与类目映射后的七路调研 + 对抗式审查)。

---

# 第一部分 现行设计(唯一权威)

## 一、需求(所有者原述,未变)

1. 一个品牌只能同时存在于一家店(采集数据带真实品牌;提交沃尔玛仍
   Unbranded,两不冲突);
2. 一个产品(ASIN)只能同时存在于一家店;
3. 一个店最多做**两个大类目**;
4. 一个店只做**一种配送方式**(FBA 或 FBM)(2026-08-13 追加);
5. 约束在**前期产品进来后的分配阶段**执行;
6. 店铺终止运营 → 释放其占用的品牌与产品;暂停 → 绑定保持;
   救不回来的暂停店 → 手动点名释放;
7. **只要没决定停止运营,品牌和产品就不能出现在其他店——即使当前没有上架
   到这个店也不行**(占用与在线状态无关);
8. 分配本身是复杂决策:好坏产品要筛,店铺间怎么分要参考订单/销量/销售额/
   在线数/容量等数据。

## 二、核心原则

**占用是决策台账,不从在线快照推导。** 在线产品(`catalog.walmart_items`)
是观测,天天抖动;归属(占用)是决策,只有"分配"和"释放"两个显式动作能改。
两者解耦,占用才稳定——与病历(`product_events`)的"决策/观测"分层同源。

**释放只走显式动作,没有任何代码自动释放占用。** 这是稳定性的根本保证。

**永久决策要更硬的证据。** 上架错了是一条 feed 失败,占用/开类目错了是
**永久烧掉一个槽位**(无自动释放)。凡属永久决策的动作,证据门槛单独提高
(体现在 §三.3 的 PT 实证要求)。

## 三、现行口径总表

每条都是拍板结论,括号内是拍板日期;实现细节见对应小节。

| # | 口径 | 结论 |
|---|---|---|
| 1 | **大类目定义** | = `catalog.risk_product_types.category`(飞书「沃尔玛类目」表经 risk_sync 日更)。**2026-08-15 P3 实测:与 `audit.walmart_pt_meta` 两本 7,008 行完全一致(差集 0、大类取值不一致 0),漂移担忧解除**;大类取值域 **26 个**(不是设计稿写的 27)(08-07 定,08-15 实测校准) |
| 1a | **店铺准入大类的权威** | = 限额表「**类目1 / 类目2 / 类目3**」三列(所有者建列 2026-08-15)。**填了就只准入填的那几个;三列都空 = 该店不限制类目**。判定唯一出处 `services/store_targets.allowed()`。⇒ **不建 `catalog.store_categories` 表、引擎永不自行开类目**(与配送限制同一治理方式:改类目 = 改表格一格)。首轮由 `alloc_audit` C1 给每店"在线数量最多的两类"建议,所有者填表 |
| 2 | **店铺状态** | 从库读 `ops.store_kpi_daily.store_status`(每店最新一行)。**必须再 AND `store ∈ services/stores.load_stores()`**——凭证表已删的店 KPI 行永远停在最后一次 ACTIVE,状态闸单独拦不住(08-12 定,08-15 补)。无记录/状态为空 = fail-open 视同 ACTIVE(全仓统一)。**状态既不决定占用归属、也不决定参不参与分配**:SUSPENDED 的占用照旧保持(§六.2),不接新货是 #13 那个开关(08-15 晚澄清) |
| 3 | **PT 与类目可信度** | 分配的类目约束用 `products.walmart_pt` → 大类,**不分 pt_source**。~~"开新大类须实证 PT"~~ **已作废**(08-15 晚:类目档案改人工填表,引擎不开类目,门槛无处可施;实测 84% 候选是推断 PT,卡门槛会把空店全卡死)。pt_source 仍随 claims 快照存档,供事后追溯 |
| 4 | **配送方式权威** | = 限额表「配送限制」列(填 fba/fbm,`registry.RETIRE_LIMITS.fields.channel_limit`)。**未填 = 不接自由流分配**,报告点名补填;不建表、不做引擎锁定(08-13) |
| 5 | **品牌归一化** | `services/brand_key`(唯一出处):`" ".join(raw.lower().split())` + 占位符表(phase0 20 项 ∪ mp_mapper `unknown`);brand 是占位符时用 manufacturer 兜底;两者皆占位符 = 真·无品牌**不占用**。**不做别名表**(08-13 定,08-15 落地) |
| 6 | **分配单元** | 品牌组(不是单品)——品牌排他 ⇒ 同品牌必同店 |
| 7 | **分配优先级** | 两梯队:先 ACTIVE ∧ 有在线产品,后 ACTIVE ∧ 空店(08-13) |
| 8 | **产品分中的销量** | 无订单史是常态 ⇒ 该信号**缺失时不计入、权重摊回其余信号**,只加分不减分(08-13) |
| 9 | **引擎方法** | 第一版 = 透明打分 + 确定性匹配,**不用 ML 也不用 LLM**(可解释/可调参/可复现);LLM 只继续负责字段映射。学习型是远景(§七.8)(08-07) |
| 10 | **执行方式** | 全程 dry-run 出方案表 → 所有者审 → `--execute` 才落占用与上架表 |
| 11 | **规划范围**(所有者定稿 2026-08-15) | **店名含「谭总」的店不在分配规划内**(`registry.alloc_excluded_stores()`,env `ALLOC_EXCLUDE_STORES` 可改):不给它们分配、不判类目、**不占品牌与产品**、其在线商品**不进冲突清单也不拦 list_new 全局去重** ⇒ 其他店可与它们重复上架。**但销量照常计入**产品/品牌/类目三个全局维度——排除的是"归属"不是"数据" |
| 13 | **参不参与分配**(所有者定稿 2026-08-15 晚) | = 限额表「**单店最大在线数**」是不是 `0`(`services/store_targets.accepts_allocation`)。**与店铺状态无关**,**且不动占用**——所有者按的开关,与类目、配送限制同一治理方式(改表格一格)。**三态不许压成两态**:`0`=不接货 / `None`=还没填(点名补填) / `>0`=参与。填 0 的店不再被追问其余三列。⚠ SUSPENDED **不是**这个开关:停用店占用照旧保持、在线行照常计入冲突(§六.2) |
| 14 | **在线口径排 RETIRED**(2026-08-15 晚,所有者质疑后查证) | `catalog.walmart_items` 里退市商品的 `missing_since` **也是 NULL**(它没缺席,只是退市了)——`catalog_sync` 全量扫描显式含一轮 `("RETIRED", None)`。所以在线口径必须是 `missing_since IS NULL` **AND** `coalesce(upper(lifecycle_status),'ACTIVE')='ACTIVE'`,否则历史上架过、早已退市的 SKU 会被当成活货位,占用与冲突组凭空多一批。coalesce 到 ACTIVE 是 fail-open。先例:`maintenance_intents._SQL_MATCH_INV`、`product_events` 把 RETIRED 记 'gone' |
| 15 | **类目不符处置**(同上) | 填完类目三列后,`alloc_audit` 出 `alloc_类目不符下架清单.csv`(所有者自行下架,与渠道不符同一形态)。**与销量无关**——类目是硬闸,不准入就得走。⚠ **归不到大类的行不进清单,单列计数**:那是「不知道它属于哪类」不是「属于不该有的类」,与渠道那条「未知不算不符」同一纪律 |
| 16 | **类目不符的行不产生占用**(2026-08-15 晚) | `services/alloc_survey.claimable(rows, registered, cfg)` 第四条筛法。补的是一个会**永久锁错品牌**的洞:`alloc_backfill._pick` 对只有一家店有的键直接归属、不过类目闸(硬闸只在 `resolve_conflicts` 的多店组里跑),于是"某店独有 + 类目不符"的品牌照样被占走,而占用无自动释放 ⇒ 货下架了品牌却永远锁在这家不该做该大类的店上。**两个集合分清**:`slot_rows`(占着货位,处置清单出自它)/ `claim_rows`(该产生占用 = slot_rows 去掉类目不符)。⚠ 归不到大类的行两边都留 —— 与「未知不算不符」一致,口径不一致会造出"没进下架清单却也没人占"的无主货 |
| 16a | **渠道不符的行同样不产生占用**(2026-08-16 补,所有者追问「分配的时候 FBA/FBM 限制了吗」时才发现) | 与 #16 是**同一个洞的另一半**:08-15 晚只补了类目,渠道漏了,后果一模一样且不可逆 —— 「某店独有 + 渠道不符」的品牌正躺在 `alloc_渠道不符下架清单.csv` 上等着被下架,却照样被 `_pick` 占走并永远锁死。现在 `claimable` 是**五条筛法**(在册 ∧ 已发布 ∧ 规划内 ∧ 类目 ∧ 渠道)。**两个判定抽成共享谓词** `sv.offends_category` / `sv.offends_channel`,占用闸与下架清单各调一次,丢的与列的严格互补(回归钉死)。⚠ 连带两处必须一起改:①`alloc_backfill` 原本 `_fetch_meta(..., with_channel=False)`(注释写着"回填不需要渠道"),不改的话 channel 全 None ⇒ 渠道闸**恒为假且不报错**,审计判一套回填落另一套;②`alloc_audit -p channel=0` 时闸也是关的,报告现在会显式警告「不要照这一份去回填」——它比回填**宽** |
| 16b | **占用对账 `claim_audit`**(2026-08-16) | 占用没有自动释放(§六,有意如此),代价是台账**只进不出**:回填时漏了一道闸、或者事后改了飞书的类目/配送限制,旧占用不会跟着松开,而且**没有任何地方会说**。`workflows/claim_audit`(只读)就是那个"会说"的地方。⚠ **只报一种情况**:该店此刻**仍有**这个键的在架行、而这些行**全都**踩了类目或渠道闸 ⇒ 正躺在下架清单上,当初就不该占。**「该店已无在架行」只计数不列清单** —— 商品下架不释放占用是铁律,列出来会诱人误删,而释放后品牌被下一轮分给别店就不可逆了。同理**只要还有一行站得住,整个占用就站得住**(品牌里有个别违规品,不该让整个品牌被拿走)。判定一律针对**占用店自己**的行,不看这个键在别处的情况——否则被错误的店占走的品牌会因为正确的店也在卖而判成健康,恰好把最该修的那条藏起来 |
| 16c | **切批量再分层,不是整池分层**(所有者 2026-08-16 追问「50 万产品分层我根本用不完」后定) | 候选池 50 万、配额才 3 万,整池分层不会"第一层就分完"——层内上限会把货摊到**第 8 层**,而发出去的仍是那批 top 货。真规模实测(14 店/配额 26,537/池 30 万):整池 10.4 秒、未发清单 273,463 行;先切 `批量×1.5` 只要 0.5 秒、未发 13,268 行,而**发出量、顶层比值、分数中位逐字相同**。⇒ `alloc_plan.HEADROOM = 1.5`。⚠ **切到 1.0× 会真的变差**:少发 7.5%,且顶层比值两家越界 —— 有些组过不了类目/渠道闸,池子刚好够数就没有腾挪余地,轮到某店时它能接的货已被前面挑光 |
| 16d | **候选池与打分只有一份** | `services/product_pool`(SQL + `load` + `score_all`)。`alloc_products`(体检)与 `alloc_plan`(真分配)必须看到**逐字相同**的池子与分数 —— 体检说"可分配 27 万"、分配只认 19 万,那份体检就是假的。这是本仓反复栽的同一类跟头(报告 vs 回填的行口径、两套 `_SQL_ONLINE`、重复的 `MAX_LEAD_DAYS`) |
| 16e | **品牌组的三个组装决定**(`services/alloc_groups`) | 组是原子的(品牌排他 ⇒ 整组去一家店),由此:①**组分 = 组内最高产品分**(取中位数会让"一个爆款带一堆平庸品"的品牌掉下去,而那正是最该抢的);②**组大类 = 件数最多的大类,并列按大类名定序**(`Counter.most_common()` 并列时按插入序 = 上游 SQL 行序 = 一个看不见的随机源,而占用撤不回);③**组渠道 = 件数最多的渠道,少数派整体剔除** —— 店铺一店一渠道而品牌必须整组去一家店,混渠道品牌里不合该店渠道的那部分上不了架,留在组里会让 size 虚高、配额被吃掉却上不了货。渠道未知的整组不发(**不猜**)。无品牌的品**每个 ASIN 自成一组** |
| 16f | **首次生产 dry-run 抓到的三个问题**(2026-08-16,幸好是 dry-run) | 批量写 3,000、定向流却塞了 36,894 个货位,`--execute` 会落 39,992 条**不可逆**占用。①**定向流的容量闸没累计**:逐组判 `size <= room`,十几个组各自"塞得下"、加起来撑爆四倍(A142 剩余容量 1,918 vs 定向流 8,384)⇒ 改累计判,并把定向流吃掉的容量从自由流 `room` 里扣掉;②**定向流不受批量约束** ⇒ 改成定向流优先吃批量(它没得选),自由流分剩下的;容量够而额度用完的归**排队**,与"去不了占用店"分开计数(前者加大 batch 就能发,后者要改配置或释放品牌);③**验收指标分子分母不同单位**:`by_layer` 数组数、`quota` 是货位 ⇒ 拿到大组的店比值天然偏低、小组偏高,实测同一批 0.16 与 2.05 并存、8 家"越界"全是这个原因 ⇒ 改按货位累计。⚠ ③ 与 #16c 报告里的漏斗百分比是**同一类错误**(组数除产品数),两处都已钉死回归 |
| 16g | **两条流分账 + 两张表**(2026-08-16 首批 dry-run 后) | ①**定向流最多吃 `批量 × directed_share`(默认 50%)**,不是"优先"。实测"优先"写法下定向流一口吃光 3,000 批量、自由流 0,后面还排着 27,208 件 —— 按每批 3,000 算要十批之后才轮得到自由流,而所有者要的是**一边补齐已占品牌一边拓新**。一方吃不满时另一方取走余额(上限≠配额,不浪费批量)。②**要动手的与诊断用的分两张表**:`alloc_分配方案.csv`(要上架的)/ `alloc_未入选.csv`(排队/淘汰/未发出)。合成一张的实测后果是批量 3,000 却出 48,816 行,其中 45,815 行是诊断 —— 那张表没法用,而所有者第一眼看到的就是这个总行数。摘要里两个数都报,不存在"藏起来" |
| 16h | **配额不从人为总数里切**(所有者定稿 2026-08-16:「限制 3000 设置的也很奇怪…这个限制我甚至就认为不应该有」) | **推翻 §7.4a 的「批量 × need ÷ Σneed」**。系统已经知道容量上限、当前在线、缺口,再套一个人为总数只会跟这些数打架。新口径:`配额_i = min(剩余容量_i, 缺口_i × 日目标_i ÷ 单品日产出_i)`,本轮可分 = 各店之和。所有者原话:「上限 3500,在线 2200,还有 1300 个位置,按缺口算出来是 1500,但也只能上架 1300 个」——**容量是硬的,缺口是想要的,取小**。`-p batch=` 降级为**可选安全阀**(给了就等比缩,保持各店形状)。⚠ 算不出缺口或货位值时**退回剩余容量而非 0**:「单店最大在线数」是所有者显式填的上限,退回 0 会让没填日目标的店永远分不到货且不报错。⚠ 已提示过的风险不再重复(§7.4a):除以货位值意味着卖得越差的店要的货越多,所有者已知情并选择缺口优先 |
| 16i | **定向流并入同一副牌**(同日,与 16h 一起) | 定向流不是另一条流水线,就是**「只有一家店能要」的牌** —— 带 `store` 进牌堆,由 `alloc_engine._gate` 的归属闸处理。分成两个阶段的实测后果是**两边各有一套配额与容量记账**,谁也不知道对方吃了多少:定向流一口吃光批量(自由流 0)、容量闸各判各的双双超容(A142 剩余容量 1,918 却塞了 8,384)。合成一副之后记账只有引擎一处,`directed_share` 这个参数随之取消。定向流仍**按件筛类目**(§7.3),`--execute` 时**不重复落品牌占用**(它本来就被占着) |
| 16j | **逐店配送时长限制**(所有者建列 2026-08-16) | 限额表新增「配送时长限制」列:该店**只接 `delivery_days ≤ 该值`** 的产品。`store_targets.lead_ok` 是唯一判定。⚠ 与全局 `amz_source.MAX_LEAD_DAYS`(=7)是**两回事**:全局那条管的是"上架但把库存写 0"的既有链路(货照上、只是不卖),这一列管的是"这家店压根不要慢货"。⚠ **未填 = 不限**(与类目三列同一治理方式,不进 `missing_config` 点名清单);⚠ **产品采不到货期时受限店拒收**,不是当它够快 —— 所有者填了这一列就是明确不要慢货,拿"没采到"当"够快"是替他做了他没做的决定(与「归不到大类的,受限店拒收」同一纪律)。<br>**组货期 = 组内最长的那件**,任一件未知则整组未知:组必须整组去一家店,取最长才保证"这家店收得了每一件"。定向流另有优待 —— 去向店已固定,`_fit_to_store` 按件剪掉超期的,不必整组等。<br>★ 顺带把 `alloc_engine._gate` 里**手抄的类目判定**收掉了,改调 `store_targets.allowed`:「三列全空 = 不限制」这类规则正着写反着写都像对的,各写一遍迟早分叉 |
| 16k | **顶层比值的分母是「它自己拿到的自由流总量」,不是配额**(2026-08-16 第三版;前两版都在骗人) | 指标要回答的是所有者那句「**好货有没有堆到一家店**」。定义:`「这家店拿到的自由流货里顶层占多大比例」÷「全体的同一比例」` —— 1.0 = 质量构成与平均一致,1.5 = 好货比例是平均的一倍半。**与配额、容量、定向流体量全部无关,只量质量构成**。<br>⚠ 走过的两个弯路,都是分母选错:①**分子数组数、分母是货位** ⇒ 量的成了"组平均多大"(实测同批 0.16 与 2.05 并存);②**分子只算自由流、分母仍是全配额** ⇒ 被定向流填满的店分子趋近 0 而被误标越界(A142 实测 0.00,分到的 1,909 全是定向),且层内上限按**总接货量**算,定向流吃掉 L1 名额后该店的自由流只能从下面几层拿,比值再次偏低(C017 实测 0.50)。这两种都不是"参数没调对"。<br>⚠ 自由流接货 < `MIN_FREE_FOR_RATIO`(20)的店**不出比值**(报 `—`):分母太小时噪声大过信号,硬报一个数只会让人去追一个不存在的问题。总量独吞另看「分到」与「本轮配额」两列 |
| 16l | **占位 ≠ 上架:候选池排除「已在架」而不是「已占位」**(所有者定稿 2026-08-16:「已占位和已上架是两回事…分配即占位」) | 分配是**决定这一轮该往每家店上什么货**;占位只是这个决定的落库记录,上架才是执行。三条:①**候选池排除已在架的 ASIN**(规划内店;谭总系在架不算 —— 它们退出分配体系,不占任何品牌与产品);②**已占位但没上架的 ASIN 照常进池**,绑定到占用店(`alloc_groups.build(bound_asins=)`,与品牌占用同一处理:都是「只有一家店能要的牌」,引擎的归属闸原样处理);③**剩余容量只减在架数**。⚠ 曾经两处都做反了:**排除已占位的 ASIN** 让方案表变成**增量** —— 上一轮定下、还没执行的上架指令从表上消失,而那恰恰是所有者要照着做的东西;**从容量里扣「已占未上架」**是**双重扣减** —— 那批货下一轮照样进池重新占位,容量却已经被它们扣过一次(那是在给一个本不该存在的症状打补丁,已整个撤掉)。⚠ 组归 A、组里却有被 B 占着的 ASIN(两条占用打架)→ 剔掉并计数,别在方案表里写一条落库时必然被 `claim_many` 拒掉的指令 |
| 17 | **产品分三段式 + 口碑缺项记 0**(2026-08-15 晚,生产实测后两次校准;2026-08-16 上调销量权重) | `score = 口碑(评分+评论,0~60) + 销量加分(0~40) − 罚分(配送/退货/黑历史)`。①**没评分没评论 ⇒ 口碑段记 0**(所有者:「相关分数扣除即可」)—— 这不违反「缺失 ≠ 0」:落地价硬闸用的 price/shipping 与评分**同出一条快照**,能走到打分这步的品一定有快照,所以"没有评分"是**真实观测**不是采集缺口;②**销量纯加分**,没订单史 +0,永远不会因"卖得少"低于"没数据"(口径 #8);③**配送/退货/黑历史只扣分,没数据不扣** —— 那些才是真数据缺口(退货实测覆盖率 0.9%)。⚠ 实测教训:旧的五信号加权平均让"只有配送时效有值"的品独占权重拿 **100 分**(98.4% 的品配送 ≤8 天),且卖过 3 件的品比没订单史的**低 17.6 分**。<br>★ **2026-08-16 所有者:「销量和销售额的权重我感觉有点不够,应该上调一些」** ⇒ 75/25 改 **60/40**;销量加分内部由**件数 0.375 + 销售额 0.625** 合成(销售额权重更高:店铺目标是日 GMV,100 件 × $5 与 10 件 × $100 对补缺口的价值完全不同,只数件数会把低单价走量品排到高客单品前面)。只有一项有值时权重摊回另一项 —— 与口碑缺项按 0 算**相反**,是有意的:口碑那边有快照就一定有那一栏,而"有件数没金额"只可能是历史行的金额列缺失,那是数据缺口。⚠ **代价要知道**:销量覆盖率仅 1.0%,这一改主要是把那 1% 往上顶,同时把 99% 没订单史的品的上限从 75 压到 60 —— 顶层的品类构成会明显变化 |
| 17a | **产品侧与店铺侧是两个窗口**(所有者定稿 2026-08-16:「看销售额,默认统计近一年的」) | `days=90` 管**店铺经营水平**(要"这家店**现在**什么水平"),`sales_days=365`(`product_score.SALES_WINDOW_DAYS`)管**产品销量信号**(要"这个品到底卖没卖过")。合成一个都会坏:取 90 天则产品侧覆盖率只有 1.0%、信号形同虚设;取 365 天则店铺的缺口与货位值被一年前的经营状况稀释。`alloc_products` 与 `alloc_plan` 共用这两个常量,`alloc_产品分.csv` 与 `alloc_分配方案.csv` 的同名列因此**必然是同一个数** |
| 17b | **淘汰线 40 → 35**(所有者定稿 2026-08-16,上调销量权重之后) | 淘汰线是**兜底扫烂品的线,不是排序工具** —— 真正的闸是容量(约 2.8 万货位 vs 十几万候选),引擎按分数排序取到配额为止。⚠ **这条线的严格程度会被口碑满分带着走**:口碑满分 75 时,40 只筛掉 7.5%(92.5% 的可判分品在线上);把口碑压到 60 之后(#17),同一个 40 变成筛掉 **56%**,中位数 38.4 直接掉到线下,牌堆缓冲从 40% 腰斩到 20%、切口分只剩 43.0(离淘汰线仅 3 分)——再薄下去牌堆就撑不满 `可分 × 1.5` 的切口,分配质量会直接落到淘汰线。所有者选了"挪线"而不是"回调权重":要的是**有销量的品排前面**(排序问题),不该让兜底线跟着牵制。⇒ **改 `BASE_MAX` / `SALES_BONUS_MAX` 时必须回头看 `CUTOFF`**,回归里有一条盯着(淘汰线不许高过"中等口碑无销量"的分数) |
| 17c | **搭车地板 50:组内低于它的品不跟车**(所有者定稿 2026-08-16:「组内产品分低于 50 分的不跟车」) | 起因是所有者那句「**产品分 38.6 和 91.4 是怎么混到一起去的**」。答案在**分层切的是「牌」不是「产品」**:先按品牌组成 4.2 万张牌,再把**牌**按名次切 10 层 —— 一张牌装着一个品牌从 91.4 到 38.6 的全部货,牌按**组分(组内最高分)**排队进 L1,低分同门整车跟着进来。它不是 bug(品牌排他要求整组去一家店),但那些货位**挤掉的是排队里组分更高的整组**。<br>⇒ `alloc_groups.RIDE_FLOOR = 50.0`:组里有 ≥50 分的品时,低于 50 的同门剔除并计入 `dropped["搭车品"]`。<br>⚠ **与淘汰线(`CUTOFF`,35)是两条不同的线,不许合并**:淘汰线管"这个产品够不够格进候选池",地板管"够不够格**沾同门高分品的光插队**"。由此有个看着别扭但站得住的结果:44 分的品,同品牌有 92 分的则**不分配**,同品牌顶格就是 44 则照常排队。<br>⚠ **整组都低于地板时一件都不剪** —— 没车可搭,组分就是它自己的水平;而定向流是店铺补齐**自己已占的品牌**,剪光会让店铺连自己名下的品牌都上不了货。这类组发出去时组里仍有低分件,`alloc_plan` 的「搭车上架」余量行继续报(归零了才敢说地板生效)。<br>⚠ **必须剪在渠道投票之前**:`[95分 FBA, 40分 FBM, 38分 FBM]` 先投渠道则 FBM 胜出,反手把 95 分那个当少数派剔掉 —— 车上的人把司机赶下了车。回归里钉着这条 |
| 17d | **候选切口 ≠ 入场线**(2026-08-16 所有者按错前提推算分配行为后补) | 报告里的「切口 43.0」只是**进牌堆**的门槛(`可分货位 × HEADROOM 1.5`),不是发牌实际发到哪一分。实测同一跑:切口 42,705 组 / 组分 43.0,而配额在**第 6,517 张牌**上就填满了(10 层只发完约一层半),真入场线高出十几分。只报切口会让人以为 43 分的货都进来了 ⇒ `alloc_plan` 的「发牌结果」一节报 **`实际入场线:组分 X`(= 最后发出去的那张牌的分)**,切口那行显式标注「⚠ 这不是入场线」,低于切口的组标为**排队中**(区别于被闸挡了 —— 两者处置不同) |
| 18 | **「不明原因消失」要 ≥3 次才扣分**(所有者定稿 2026-08-15 晚) | `catalog.product_risk.unexplained_missing`(消失过 ∧ 我们从没提交过删/停 = 疑似平台强制下架)**单独一次不作数**,要 `missing_times >= 3`。理由:`mark_missing` 的判据是"本轮全量扫没扫到",**整店凭证失效或代理挂掉时一次坏扫描能把一批好品冤枉成平台下架** —— offset 截断那类已被 `catalog_sync` 的单查补漏兜住,这类兜不住。1~2 次更像扫描抖动,≥3 次基本是真被反复下架。常量 `product_score.UNEXPLAINED_MISSING_MIN`。⚠ 消费方**必须把 `missing_times` 一起查出来**,漏这一列该项永不扣分且不报错 |
| 12 | **处理顺序**(同上) | **先判每店类目 → 再判品牌与冲突**。顺序是有依据的:类目定了之后,"某店不再准入该大类"就成了冲突判定的**硬闸**(货必须离开它,与销量无关),排在销量阶梯之前 |

## 四、数据依赖现状(2026-08-15 七路调研核实)

写引擎前对着这张表确认输入,**标 ⚠/❌ 的不要按设计稿想当然取**。

| 依赖 | 状态 | 取法 / 缺口 |
|---|---|---|
| 候选池(审核通过) | ⚠ | `products.audit_status = **'approved'**`(不是 pass;三套词表见 §附 B.4)+ `title` 非空(排 pt_backfill 占位行)+ `walmart_pt <> 'unknown'` + 大类查得到;建议再加 `PT ∈ services/pt_spec.known_pts()`。128 万行无 audit_status 索引 ⇒ **一次性拉进内存**,禁止逐 ASIN 查库 |
| ASIN → 大类 | ⚠ | `products.walmart_pt` → `risk_product_types.category`。**不要在分配层重跑审核的 resolve_pt 链**。覆盖率与两本字典差异看 `alloc_audit` P1/P3 |
| 品牌 | ✅ | `products.brand` + `slow->>'manufacturer'` 兜底,经 `services/brand_key` |
| 渠道(产品侧) | ⚠ | `latest_snapshot.raw->>'is_fba'`,**必须用 amz_source 的 LATERAL 口径**(裸 JOIN `latest_snapshot` 会因 scrape_params 分组让一个 ASIN 出多行;`zip_verify='mismatch'` 要排除) |
| 渠道(店铺侧) | ✅ | `services/store_targets.load_targets()`(已建) |
| 店铺目标/容量 | ✅ | 同上(目标销售额/目标订单为**日目标**;单店最大在线数是总容量上限,≠「上架限制」日配额) |
| 产品分·价格 | ⚠ | **价格带命中不是产品分信号**,是店×产品闸(倍率按店配置)。产品分只能用"落地价算不算得出来"(price 与 shipping 双非空) |
| 产品分·库存/时效 | ✅ | `stock_count`(NULL≠0)、`delivery_days`(NULL 不当超时);阈值沿用 `services/amz_source` 常量 |
| 产品分·评分/评论 | ✅ | **2026-08-15 P2 探针实测:近 5 万条 ok 快照 rating / review_count 命中率 100%**——契约字段表虽无登记,但采集侧确实随 raw 落库(设计稿原判断成立,我此前"本仓零证据"的存疑由探针推翻)。取 `snapshots.raw->>'rating'` / `'review_count'`,text 型读侧解析,**解析失败按"没采到",禁止 `or 0`** |
| 产品分·黑历史 | ✅ | `catalog.product_risk` 视图(`product_events.asin` 无索引 ⇒ 一次性拉内存) |
| 硬闸·黑名单 | ✅ | `services/blacklist.load_banned_asins` + `services/risk_gate.load_gate/check`(品牌与制造商双字段) |
| 硬闸·风控 PT | ✅ | `risk_gate.check(gate, pt, None)` |
| 硬闸·容量 | ⚠ | `walmart_items` 在线数(与 KPI 表口径差一个 `published_status='PUBLISHED'`,`alloc_audit` A0 拆两个数);死店冻结行会污染,见 §九.4 |
| 店铺配额·GMV | ✅ | `orders.order_lines` 聚合(命中 `(store, order_date DESC)` 索引)。**两年历史已入库**(order_history_import #35,15 万行 2024-03~2026-07,494 家店,`source='历史数据'`);时区统一 UTC 零点,无日界分叉。⚠ **分母是「在售天数」不是日历天数**(§7.4,08-15 晚更正):店铺暂停过的那段没有销售额,除以日历天数会把"停过业"读成"卖得差",缺口被高估 ⇒ 停过业的店反而被灌最多货。⚠ 另见下方"历史行的三条语义" |
| **两个「在线数」口径,别混**(2026-08-15 晚) | ⚠ | ①**占用/冲突侧**(`alloc_survey._SQL_ONLINE`)= `missing_since IS NULL` **AND 非 RETIRED** —— 退市商品不占货位(口径 #14);②**容量/产出侧**(`alloc_stores._SQL_ONLINE_NOW`、KPI 表 `items_online`)= `published_status='PUBLISHED' AND missing_since IS NULL`,**不筛 lifecycle** —— 跟的是所有者日报里每天看的那个数,且 KPI 历史行已按此存了两年,改不了。**两个各有其用,不许去"统一"**;同一张报表内必须只用其中一个,否则货位值(÷在线均值)与剩余容量(−当前在线)会建在两个不同的"在线"上 |
| **店铺在售天数 / 在线数** | ✅ | `ops.store_kpi_daily` **逐店逐日一行**(PK `(store, data_date)`),带 `store_status` / `items_online` / `orders_count` / `sales_amount`。在售天数 = 窗口内**有记录且** `coalesce(upper(store_status),'ACTIVE')='ACTIVE'` 的天数;⚠ **没记录的天既不算 active 也不算停用**(那是"没抓到"),所以必须同时算覆盖率 `rec/N` 并在方案表报出来,低覆盖店的比值指标全靠收缩顶上去,不代表真实水平 |
| 店铺配额·健康度 | ❌ | KPI 8 率有列但**阈值零实现**,且 NULL 两义(无合规数据 vs 拉取失败)不可区分 ⇒ **v1 降为二值(ACTIVE=1)**,8 率只进方案表展示列,`need_i` 改二项归一 |
| 店×类目销量 | ⚠ | `order_lines(store,sku) ⋈ walmart_items(store,sku)` → PT → 大类;**sku 大小写敏感**;必须显式过滤在册店(死店冻结行查得出来) |
| 产品/品牌/类目全局销量 | ⚠ | **A1.5 代码就绪 2026-08-15**:`order_lines.asin` 列 + `order_asin_normalize`(规则走 `services/sku_asin` 唯一出处,与 `sku_normalize` 同路径,有回归钉住两者结果一致)。**待生产跑一次**;跑完看覆盖率,解析不了的留 NULL ⇒ 消费方一律 `asin IS NOT NULL` 过滤,**绝不拿 sku 原文当 asin**(三段式与纯数字直连采集库永远查空,那是静默的错误信号) |
| **历史订单行的三条语义** | ⚠ | ①`source='历史数据'`,`sale_status` 一律 `Delivered`(不是真实状态)⇒ 销量口径只能排 `Cancelled`,不能按状态细分;②源表「统计状态」列**未导入** ⇒ **退款/无效单被算作销量**(相对比较影响小,绝对额偏高;要精确得让导入侧补列);③「退款原因」未导入 ⇒ **退款率信号只能来自 API 行与 `return_lines`**,历史期算不出来 |
| 占用落点 | ✅ | `catalog.claims` + `services/claims`(排他由部分唯一索引保证)+ `store_release` + `alloc_backfill` 均已就绪(2026-08-15);`allocation_runs/items` 随 A2 建 |
| 上架表写入 | ⚠ | 现有五个写函数**全部按行号定点写,无一能新建行**——追加要照 `services/maint_sheet.append_records` 的水位模式另写;且写列权责与审核批次 D 冲突(§九.2) |

## 五、占用台账设计

**`catalog.claims`**(品牌占用 + 产品占用,一行一个占用):

| 列 | 说明 |
|---|---|
| kind | `brand` / `product` |
| claim_key | 品牌键(`services/brand_key`)或 ASIN |
| store | 占用店铺 |
| status | `active` / `released` |
| walmart_pt / pt_source / audit_version | **决策时快照**——映射表是活表(catmap 在写)、重采会把 approved 翻 pending,不快照就无法回答"当初按什么分的"(08-15) |
| claimed_at / released_at / released_reason | 生命周期与审计(released 行保留不删) |

排他性由 PG 保证,不靠代码自觉:
`UNIQUE (kind, claim_key) WHERE status='active'`(部分唯一索引),
并发领用与 UPC 池同款事务纪律。

**没有 store_categories 表、也没有 store_channel 表**(2026-08-15 定稿):
店铺档案的两项约束——准入大类与配送方式——**权威都在飞书限额表的列里**
(类目1/2/3、配送限制),引擎只读不写。这样定的三个理由:
① 所有者改一格就生效,不需要跑任何 workflow;
② 引擎**永不自行开类目**,"开新大类要不要实证 PT"这个难题直接消失
   (84% 候选是推断 PT,靠证据等级卡门槛会把空店全部卡死);
③ 类目档案本就是运营决策不是系统状态,放系统里反而要造一套同步机制。
判定唯一出处:`services/store_targets.allowed(cfg_row, category)`。

## 六、生命周期与释放

1. **占用产生于分配,不产生于上架**:产品分给某店的那一刻,事务内同时
   占品牌 + 占产品 + 核类目 + 核渠道,占不到就不能分给这家店。之后下架、
   清零、从未真正上架,占用都不动。品牌已属于某店的产品只能"定向"去那家店
   (还得过它的类目/渠道/容量),去不了就淘汰。
2. **暂停 = 什么都不做**,占用天然保持;SUSPENDED 店的在线行仍计入冲突统计。
   停用只是暂时不给它分新货,**不代表别店可以来拿它的品牌与产品**——
   店没在关,它的东西就还是它的(§一 所有者原述)。
   ⚠ 2026-08-15 晚一度被误改成"当作全新店(不建占用)",所有者当即纠正:
   **口径没变,仍是「暂停不释放、占用保持」**。要某店暂时不接新货走 2a,
   那是配额开关,与占用归属是两件事。
2a. **"暂时不给某店分货"的正确做法**:限额表「**单店最大在线数**」填 `0`
   (`services/store_targets.accepts_allocation`,所有者定稿 2026-08-15 晚)。
   与类目、配送限制同一治理方式——改表格一格就改行为,不建表、不改代码,
   **也不动占用**。**三态不许压成两态**:`0` = 所有者按下"不接货";
   `None` = 还没填(报告点名补填);`>0` = 参与。写成 `not max_online`
   会把没填的店一并算成不接货 —— 它于是永远分不到货,而且不报错。
3. **终止 = 显式释放**:workflow `store_release --store X`(dangerous,默认
   dry-run 列清单):该店全部 active 占用置 released(reason=terminated)、
   **同步把该店 walmart_items 行标 missing_since**(§九.4;`-p mark_offline=0`
   可关)。类目与渠道是飞书配置,不归它管。
   **留痕在 claims 表自身**(released 行 + reason + 时间),不写 product_events
   ——事件账本的入账边界是"产品生死与观测事实",占用是决策,两边各记一份
   反而要维护两套口径。
4. **手动个别释放**:同 workflow `--brand` / `--asin` 精确释放。
   除以上两条显式路径外无任何自动释放。
5. **TERMINATED 只触发"可释放资格"**:审计发现"已终止仍有 active 占用"→
   告警列清单 → 人跑 store_release。KPI 是外部观测,误报若触发自动释放,
   品牌被别店占走后不可撤销。

## 七、分配引擎

### 7.1 候选池

从 `catalog.products` 取:审核 approved ∧ 有标题 ∧ PT 有效 ∧ 大类查得到 ∧
产品未被占用 ∧ 渠道已知(FBA/FBM;未知的不参与,计数可见)。
逐层收窄的真实数字由 `alloc_audit` P1 给出。

### 7.2 分配单元 = 品牌组

按 `services/brand_key` 分组:
- 品牌组整组定店;组过大时可只分组内产品分 top-N,其余不占 product 占用
  ——品牌已占用,后批这些品自动定向同店,排他不破;
- 真·无品牌(brand 与 manufacturer 皆占位符)= 逐 ASIN 为单元,只占 product。

### 7.2b 规划范围外的店(不参与,但数据照用)

`registry.alloc_excluded_stores()` 命中的店(当前 = 店名含「谭总」)整体
退出分配体系:不进候选店集合、不占品牌与产品、不判类目与渠道、其在线
商品不进冲突清单、也不参与 list_new 的全局 ASIN 去重(⇒ 规划内的店**可以**
与它们重复上同一品牌/产品)。
**销量数据不受影响**:它们的订单照常进产品/品牌/类目三个全局销量维度,
是别的店选品时的有效信号。排除的是"归属"不是"数据"——这条区分要记牢,
把数据也一起排掉会白白丢掉一大块选品依据。

### 7.3 两条流水

- **定向流**(品牌已被占用):只能去占用店;过该店硬闸(状态 ACTIVE ∧ 在册 /
  类目 / 渠道 / 容量 / 黑名单)→ 淘汰行进方案表并注明被哪道闸拦下(所有者
  可选择先点名释放品牌再重分)。定向流**不分梯队**(品牌绑定是强制路由)。
  ★ **类目按件筛,不整组淘汰**(2026-08-16 所有者追问"定向流淘汰是什么意思"
  时更正):去向店已被品牌占用**固定死**,不存在"该给谁"的竞争,按件筛是
  良定义的、且严格更划算。原来的写法会因为组大类取件数多数派,让"60% 厨房 /
  40% 家居"的品牌被只做家居的占用店**整组拒收**——连那 40% 本来就能上架的
  家居商品一起。剪完要**重算组分与组大类**,否则方案表上写的是被剪掉那批的
  属性。⚠ **自由流不许这么做**:那边组的完整性参与竞争(组分与 size 都会变),
  按件筛等于让同一个品牌在不同店之间被拆开,品牌排他就破了。
  ★ **定向流也吃批量**,且容量**累计**判(见 #16f)。
  ★ 淘汰摘要按「**店 × 缺的大类**」摊开:光给总数没法动手,摊开后所有者一眼
  看出"给 A085 开厨房能救回多少件"——那是他真能做的决定。
- **自由流**(品牌未占用 / 无品牌):走 7.4~7.5 打分匹配。

### 7.4 店铺经营水平(所有者定稿 2026-08-15 晚,**取代原"固定除以 30"**)

> **原文这里写的是**「近 30 天日均 GMV 分母固定用 30(与日目标自洽),不用
> "有记录天数"」。**这条是错的,所有者当日指出**:三个月里店铺暂停过,
> 那段时间根本不存在销售额,拿日历天数当分母会把"停过业"读成"卖得差"。
> 后果是反向的 —— 缺口被高估 ⇒ 配额被放大 ⇒ **停过业的店反而被灌最多货**。

**分母一律是「在售天数」,不是日历天数。** 数据现成:`ops.store_kpi_daily`
逐店逐日一行,带 `store_status` / `items_online` / `orders_count` /
`sales_amount`。

```
窗口 = [as_of − N 天, as_of)             # N 默认 90;与冲突判定共用 sales_window
rec_i     = 窗口内该店**有 KPI 记录**的天数
active_i  = 其中 coalesce(upper(store_status),'ACTIVE') = 'ACTIVE' 的天数
cover_i   = rec_i / N                     # 覆盖率,低了下面的比值都不可信
items_i   = 窗口内 active 天的 items_online **均值**(不是最后一天的值)
```

⚠ **没有 KPI 记录的那些天既不算 active 也不算停用**——那是"没抓到",不是
"没营业"。所以 `active_i` 只从有记录的天里数,并且 `cover_i` 必须一起报出来:
覆盖率低就说明这家店的指标本来就不该拿来跟别人比(处理见下面的收缩)。

三个归一化指标(都用 `active_i` 当分母):

| 指标 | 算式 | 回答什么 |
|---|---|---|
| **日均净销售额** | `净额_窗口 / active_i` | 规模:这家店一天能净卖多少(退款已扣,见 7.4e) |
| **单品日产出** | `净额_窗口 / active_i / items_i` | **效率:给它一个货位值多少钱** |
| **日均订单** | `orders_窗口 / active_i` | 需求密度 |

**单品日产出是分配最该看的那个**,理由:一家 500 SKU 做 500 美元/天的店,
每个货位只值 1 美元;一家 100 SKU 做 300 美元/天的店,每个货位值 3 美元。
后者才是好货该去的地方,而只看总 GMV 会得出相反的结论。

**样本不足要收缩,不能直接用。** 与 §八 层级回退同一套写法:

```
active_i <  14  → 三个指标**一律取全店中位数**(所有者拍板:少于 14 天
                  就不信它的数据),方案表标记「样本不足」
active_i >= 14  → shrunk_i = w·本店 + (1−w)·全店中位数,
                  w = active_i / (active_i + 14)
```

为什么 14 天以下是**硬切**而不是继续收缩:那个区间的比值噪声大到没有
信息量(一单大额就能把单品日产出抬十倍),而收缩公式在 `active=2` 时
仍会留 12.5% 权重给它。**新店 active_i=0 落在同一条规则里**,
天然冷启动,不需要特判。

`cover_i < 0.3` 的店在方案表单独标记「KPI 覆盖不足」——不是不给货,是提醒
所有者这家店的三个指标都是中位数顶上来的,不代表它真实水平。

### 7.4a 配额(目标缺口 + 效率,双驱动)

```
gap_i   = clamp01((日目标销售额_i − 日均GMV_i) / 日目标销售额_i)   # 分母已是在售天数
room_i  = max(0, 单店最大在线数_i − (在线数_i − 待下架数_i))       # ★ 预支下架名额
eff_i   = 单品日产出_i(收缩后)/ 全店单品日产出中位数,clamp 到 [0, 2]
need_i  = w_gap·gap_i + w_room·min(1, room_i/批量) + w_eff·min(1, eff_i/2)
quota_i = min(room_i, ceil(批量 × need_i / Σ need))
```

**`w_eff` 是新增项,它防的是"只看缺口"这个陷阱**:一家店缺口大,可能是因为
它卖不动,再猛给货只是把库存压过去。gap 与 eff 有意拉反方向 —— 要找的是
**低于目标、但有能力**的店。权重初值 **`w_gap=0.6 / w_room=0.25 / w_eff=0.15`**(所有者拍板 2026-08-15 晚:
**把货给离目标最远的店** ⇒ 缺口主导);进配置文件,首批 dry-run 对着方案表砍,
要纯缺口驱动就把 `w_eff` 设 0。

**★ 容量预支**:`room` 要减掉「待下架数」(类目不符 + 渠道不符 + 冲突输家
三份清单里属于该店的行)。所有者是**一边下架一边上架**的,不预支的话,
一家马上要空出 300 个货位的店会被算成"满了",这一批一件都分不进去。
待下架的货**已经不产生占用**(§三.16 类目闸在 `claimable` 里),所以那些
品牌与 ASIN 此刻就能分给别人,**不必等实际下架完成**。

### 7.4b 反堆积:分层轮转发牌(所有者定稿 2026-08-15 晚)

§7.5 的警告说了"不许按分数顺序分配",这里是它的具体做法。

```
1. 自由流候选按**组分降序**切成分数层。★**按排名切,不按分数区间切**:
   每层恒定装 层厚×组数(初值 10%),层数 = 1/层厚,与店铺数无关;
2. **逐层**发牌;层内不按"这一组最适合谁",而按
       已接量_i / quota_i  升序轮转 —— 谁离自己的配额最远谁先拿;
3. 该店过不了硬闸(品牌/类目/渠道/容量/黑名单)→ 顺延下一家,不回头;
4. 每发一组即时更新 已接量_i,下一次挑人看的是更新后的世界;
5. ★**层内上限** `cap_i = ceil(quota_i × 层厚 × 松弛)`(松弛初值 1.3):
   本层已接满 cap 的店跳过(**跳这一家,不是放弃这一组**)。被上限压回来的组
   排进**下一层的队头**优先重试;最后一层还压着的走**收尾扫描**(取消上限
   再走一遍,只剩配额与容量闸)—— 不丢货。
```

⚠ **压回来的组必须排在下一层前面,不能攒到最后再扫**(2026-08-16 回归测试
逼出来的):攒到最后的话,它想去的那家店会先被后面几层的货填满配额,
上限于是从"晚一点拿"变成"永远拿不到",**净发牌量下降**。两种写法都
"看起来在均衡",但一种是倒扣 —— `tests/test_alloc_engine.py` 钉死了这一条。


★ **为什么按排名而不是按分数区间**:实测分数堆在 40~70(P10 28 / 中位 50.6 /
P90 63.4),按"每 10 分一档"切,50~60 一格能装三分之一的货,90~100 一格
凑不出 10% —— "最好的那层"根本不成层。按排名切则层厚随分布自适应:
中间密的地方层薄(2.6 分宽),两端疏的地方层厚(28 分宽),**每层货量恒等**。

★ **为什么还要层内上限**(2026-08-16 模拟发现,不是拍脑袋):光靠轮转不够。
轮转管的是**顺序**,管不住**某张牌只有一个人能接** —— 大类覆盖稀时(某个
大类只有一两家店的类目闸放行),那些组无视轮转顺序全落到它们头上;配额小的
店被这么喂几次,就把配额的一大截花在了顶层。用真实店铺数据模拟(批量 3000、
10 家店),顶层验收比值 H007=1.41 / M001=1.37,**双双越界**;加上限后
1.28 / 1.26 全部落回区间,而**全批发牌量一张不少**(2841 → 2841),
只是把它们在顶层多吃的挪到后面几层。

为什么分层:光靠"超配惩罚"只能保证**数量**分散,保证不了**质量**分散 ——
一家店可能先把 top 10% 吃到配额上限,后面全是尾货给别人。分层之后,
**每一层都按配额比例分给所有店**,好货天然摊开。

**验收指标(必须进方案表,不是"希望如此")**:

| 指标 | 期望 |
|---|---|
| 每个分数层的**店铺分布直方图** | 与各店 quota 占比接近 |
| 每店「拿到的 top 层数量占比」 ÷ 「quota 占比」 | 落在 [0.7, 1.3];超出就是参数没调对 |
| 单店本批接货量 / 全批 | 不超过 `1.5 × quota_i/Σquota` |

**首批 dry-run 必须人眼看这三个数**。一家独大 = 参数错了,不是"模型判断"。

### 7.4c 产品没有销量怎么办(常态,不是异常)

实测绝大多数候选没有任何订单史。三条纪律:

1. **缺失不等于 0**,禁止 `or 0`(§三.8)。该项**不计入**,权重按比例
   **摊回其余信号** —— 不是给它 0 分,那等于惩罚"新品"。
2. **层级回退取代理信号**(§八):
   `该 ASIN 销量 → 该品牌销量 → 该大类需求基线 → 全局基线`,
   逐级按 `w = n/(n+N0)` 收缩,N0 初值 30。
   ⚠ 前两级**依赖 A1.5**(`order_lines` 加 asin 列),没做之前只有
   「该大类基线」这一级可用 —— 这是 A2 的硬前置,不是可选项。
3. **销量只加分不减分**(§三.8):卖过且卖得动是强信号;没卖过只是没信息。

### 7.4d 货位值:**量出来的历史均值,不是预测**(所有者追问 2026-08-15 晚)

「好货放进去能产出多少」**预测不了**,也不该假装能。能量的是这个:

```
货位值(店 i, 大类 c) = 净销售额(i,c,窗口) / active_i / 在线数(i,c)
                                 ↑            ↑         ↑
                            退货已扣      在售天数   该店该大类的在线件数
```

三条纪律:

1. **按大类分,不按整店分。** 整店单品产出会被该店的品类结构污染 ——
   一家店 Home 卖得好、Fashion 卖得烂,整店均值对两边都不对。要回答的是
   "在这家店放一个 **Home** 货位值多少钱"。
2. **净销售额,不是 GMV。** 见 7.4e。GMV 不扣退货是虚高,而退货率是店铺
   履约能力的直接体现 —— 两家店同样卖 1000 美元,退掉 400 的那家实际只
   产出 600,还多赔一次运费和一个差评。
3. **它是历史均值,不是对新品的预测。** 样本不足(新店、新开的类目)按
   `w = n/(n+N0)` 收缩回**全店同大类中位数**,n = 该店该大类近窗订单数。
   真正的预测能力要等 §7.8 的决策快照攒够 2-3 个月,A3 回归出来才有。

**产品分与货位值不相乘。** 相乘等于假装能算出"这个品在这家店能卖多少钱",
那是没有依据的。两者分工明确:

| 量 | 决定什么 |
|---|---|
| 产品分 | **发不发、先发谁**(淘汰线 + 排序) |
| 货位值(店×大类) | **发给谁**(同一组货,哪家店的这个类目货位更值钱) |

### 7.4e 退货:销量口径一律用净额(所有者追加 2026-08-15 晚)

「不止看销量,还要看退货」。落到三处:

| 用处 | 口径 | 数据源 |
|---|---|---|
| 店铺货位值 / 日均 GMV | **净销售额 = GMV − 退款额** | `orders.return_lines`(PK `(return_order_id, order_line_id)`,按 `order_line_id` 回连 `order_lines`;`refund_total` / `refunded_qty`) |
| 产品分·退货率 | 该 ASIN 的 `退货件数 / 售出件数`,**减分项** | 同上,依赖 A1.5 的 asin 列 |
| 店铺履约 | KPI 表 `return_rate` / `refund_rate` 列 | `ops.store_kpi_daily`(展示用;阈值未拍板前不进公式,同 8 率) |

⚠ **历史期算不出退货**(§四"历史行的三条语义"):`order_history_import`
只导销售六列,「退款原因」「统计状态」都没进来,`return_lines` 也只有
API 期的数据。所以:
- **窗口跨历史期时,退货率按"没数据"处理,不当"零退货"**(禁 `or 0`);
- 净销售额在历史期 = 毛额,**方案表必须标注该店的窗口有多少天落在历史期**,
  否则 API 期的店会因为扣了退货而显得比历史期的店差,那是口径不同不是经营差。

### 7.4f 「待下架数」怎么来(所有者追问 2026-08-15 晚)

三份处置清单里属于该店的行,**按 `(店, SKU)` 取并集去重**:

```
待下架_i = |{ (店,SKU) : 店 = i } ∩ (
              alloc_类目不符下架清单.csv
            ∪ alloc_渠道不符下架清单.csv
            ∪ 两份冲突处置.csv 里 处置='下架' 的行 )|
```

⚠ **必须去重**:同一行可能同时是"类目不符"和"冲突输家"(很常见 ——
类目闸本来就是冲突判定的硬闸)。不去重会把 room 高估一倍,配额跟着虚高。

⚠ **预支只影响配额,不放宽容量硬闸**:

| 环节 | 用哪个数 | 为什么 |
|---|---|---|
| 算 `quota_i`(谁多分谁少分) | `max_online − (在线数 − 待下架数)` | 乐观:马上要空出来的货位应该参与分配 |
| `list_new` 真上架时的容量闸 | **当前真实在线数** | 保守:下架没做完就上架会真的超 `max_online` |

分配是**计划**,上架是**执行**。计划可以预支,执行时按实际核实 ——
所以方案表要标注「本店 N 件依赖 M 件下架先完成」,让所有者知道先后顺序。
清单是**建议**不是事实:所有者未必全下架,所以预支天然乐观,
方案表把「当前 room」与「预支后 room」两个数都列出来,差多少一眼可见。

### 7.4g 三个参数定稿(所有者拍板 2026-08-15 晚)

| # | 参数 | 拍板 | 落到哪 |
|---|---|---|---|
| 5 | 效率 vs 缺口 | **把货给离目标最远的店** ⇒ `w_gap` 主导 | `w_gap=0.6 / w_room=0.25 / w_eff=0.15`;要纯缺口驱动把 `w_eff` 设 0 |
| 6 | 在售天数下限 | **少于 14 天就不信它的数据** | 硬规则,见下 |
| 6a | 薄样本的**缺口**怎么算(08-15 晚追加,两条决策冲突后拍板) | **缺口用实测、效率用中位数** —— 两条各管各的量 | 「不信薄样本」管的是**比值**(货位值,一单大额能抬十倍);「把货给离目标最远的店」管的是**缺口**,而一件没卖的空店确实最远。拿中位数替换缺口会让它显示成「平均水平」排在真卖得动的店后面 —— 方向反了。生产实测:82杨乾良 在线 0、一件没卖,却和中位数店 M001 并列缺口 66% |
| 7 | 分数层层厚 | **10%** | §7.4b 发牌分 10 层 |
| 7a | 层内上限松弛(08-16 模拟追加,非所有者拍板 —— **默认值待首批 dry-run 核** ) | **1.3** | `cap_i = ceil(quota_i × 层厚 × 1.3)`。数值直接取自验收指标上界 [0.7, **1.3**],所以它不是独立参数:调宽验收区间就得同步调它,否则上限比验收线还严,层层压回收尾扫描 |

**关于 5,已提示过的风险记在这里,不再重复**:缺口大有可能正是因为这家店
卖不动,纯缺口驱动会把货压给最卖不动的店。所有者已知情并选择缺口优先;
`w_eff=0.15` 保留为**同缺口时的次级排序**,首批 dry-run 看方案表再砍。

**关于 6,`active_i < 14` 是硬规则不是收缩**:

```
active_i <  14  → 三个指标**一律取全店中位数**,方案表标记「样本不足」
active_i >= 14  → shrunk = w·本店 + (1−w)·中位数,  w = active_i/(active_i+14)
```

两段而不是一段的理由:14 天以下的比值噪声大到没有信息量(一单大额就能
把单品日产出抬十倍),收缩公式在 `active=2` 时仍会留 12.5% 的权重给它,
而所有者的判断是"这种数据一点都不信"。**新店 active=0 落在同一条规则里**,
不需要特判 —— 天然冷启动。

### 7.5 匹配循环(两梯队 + 事务内占用)

> **⚠ 不许"按分数从高到低顺序分配"**(所有者定稿 2026-08-15 晚)。
> 那样跑,评分最高的一批品会连续命中同一家适配分最高的店,**好货全堆在
> 一家**,其余店只拿到尾货 —— 而分配的目的正是把货**分散到多个店铺**。
> 三条落到实现上的约束:
> 1. **超配惩罚必须是每分配一组就即时生效**(下面第 2、4 步),不能整批
>    算完再罚 —— 罚项滞后一批,前一批照样堆完了;
> 2. **同一轮内按"店铺剩余缺口"轮转**:每次挑下一组时,优先喂**当前缺口
>    占比最大**的店,而不是每次都问"这一组最适合谁";
> 3. **好品要跨店铺打散**:同一大类的高分组不许连续落同一家店,
>    实现见批次 A2 的"轮转 + 超配惩罚"双保险,方案表按店铺分布出直方图,
>    **首批 dry-run 必须人眼看这张分布**——一家独大就是参数没调对。
>
> 这条改的是**顺序与配额**,不改硬闸:品牌排他、类目准入、渠道、容量
> 四道闸任何时候都不许为了"分散"而放宽。


**梯队 1 = ACTIVE ∧ 在册 ∧ 有在线产品**(在营主力)先完整跑一轮;
**梯队 2 = ACTIVE ∧ 在册 ∧ 空店**只分梯队 1 消化不完的剩余池。
(空店目标缺口天然巨大,同池竞争会把最好的品吸走。)

每轮内品牌组按组内最高产品分降序排队;对每组:

1. 候选店 = 过全部硬闸的店。**类目闸**:组主大类 ∈ 店已有类目;店类目 < 2
   时"开新大类"也算候选但适配分吃惩罚项,方案表高亮「将为店 X 开辟第 2
   大类 Y」,一批内一家店最多开 1 个新大类;开新类目额外要求 PT 实证
   (§三.3)。**渠道闸**:产品渠道 = 店「配送限制」列;该列未填的店不接
   自由流分配(摘要点名补填)。
2. 店铺适配分 = w1·类目份额(该店该大类近 90 天销量份额,样本稀按 §八
   层级回退混全局基线)+ w2·gap_i + w3·剩余容量比 − w4·(本批已接量/quota_i)
   (超配惩罚,好货不堆一店)。
3. 取分最高店,**事务内**占品牌 + 逐 ASIN 占产品 + 核类目(占不到 = 并发
   被抢,顺延次优店);打平 → 剩余容量比 → 轮转。
   ⚠ 这里的"分最高"是**罚过之后**的分:第 2 步的 w4 超配惩罚已按该店本批
   已接量扣过,所以连续喂同一家店时它的分会一路走低,自然让位。
4. 每分配一组即时扣该店 quota,后续组看到更新后的世界。
5. quota/容量耗尽后剩余组标「本批未分」留池等下批——与产品分不及格的
   「淘汰」**分开计数**,方案表两个口径都可见。

### 7.6 产品分(0~100,低于淘汰线不分)

| 信号 | 口径 |
|---|---|
| 落地价可算 | price 与 shipping 双非空(任一 NULL = 淘汰,与 list_new 同源) |
| 库存深度 | `stock_count`;NULL + in_stock 按 `AMZ_IN_STOCK_QTY` 记,`< MIN_INVENTORY` 淘汰 |
| 配送时效 | `delivery_days`;超 `MAX_LEAD_DAYS` 是**减分项**不是闸(既有链路是"上架但库存写 0") |
| 历史销量 | 见 §八 维度视图;**缺失时该项不计入、权重摊回其余信号**(§三.8) |
| 退款率 | ⚠ 只有 API 期算得出(历史导入未带退款列);历史期缺失按"没数据"处理,不当"零退款" |
| 黑历史 | `product_risk` 计数列作减分项(黑名单三表在硬闸) |
| 评分/评论 | ✅ 进 v1(P2 实测命中率 100%):`raw->>'rating'` / `'review_count'`,text 解析失败按没采到,禁止 `or 0` |
| 类目饱和度 | v1 不做 |

淘汰线初值 40/100,配置化。

### 7.7 方案表与执行

dry-run 产出**分配方案表**:产品 | 品牌组 | 产品分 | 去向店 | 逐信号得分 |
开新类目/锁渠道标记 | 淘汰或未分原因。所有者审完 `--execute` 才落占用、
写上架表。跑顺且被信任后再议放开自动。

### 7.8 信号快照(A3 的起点)

A2 从第一天起把每次分配的完整信号落库:`catalog.allocation_runs`
(一批一行:参数/权重版本/池规模)+ `catalog.allocation_items`(一品一行:
产品→店→逐信号得分→去向 assigned/directed/eliminated/unassigned + 原因 +
**PT/pt_source/audit_version 快照**)。跑 2-3 个月自然攒出「分配决策 →
30/60/90 天销量」回路,A3 先离线回归校准权重,再谈学习排序。
**不落快照,A3 永远没有起点。**

## 八、销量统计维度化

**事实表 + 维度视图,统计与"店"解绑**(2026-08-12 所有者指正:两年订单里
必有已不存在的店,只按店统计会错很多数据)。

事实表 = `orders.order_lines`(行级:店×SKU×日期×数量×金额)。店名原文永存、
死店行不迁不改不删(店铺本就被刻意排除出行身份)。引擎**不直接按店 JOIN
订单**,只读四个维度聚合视图(A2 建,只读不落表):

| 视图 | 维度 | 范围 | 消费方 |
|---|---|---|---|
| 产品销量档案 | asin | **全店含死店** | 产品分:"卖过且卖得动"是最强选品信号 |
| 品牌销量档案 | brand_key | 全店含死店 | 品牌组排序参考 |
| 类目需求基线 | 大类 × 月份 | 全店含死店 | 需求基线 + 季节性系数(两年数据可算) |
| 店×类目近况 | store × 大类 × 近 30/90 天 | **只算在册店** | 店铺适配分 |

⚠ 前三个视图**依赖 A1.5**(order_lines 加 asin 列,§十);店×类目视图不依赖
(走 walmart_items 主路)。

**类目归属链**:主路 `order_lines(store,sku) ⋈ walmart_items(store,sku)`
(PK 对齐,sku 大小写敏感)取 `product_type` → `risk_product_types.category`;
兜底 sku → `sku_asin.extract_asin`(numeric 形态先按 `walmart_items.item_id`
倒查)→ `products.walmart_pt` → 同 JOIN。两路都提不出 → **未归类桶,计数
可见,绝不静默丢**。

**店铺适配分层级回退**(解决店×类目样本稀):
`score = w·店×类目份额 + (1−w)·全局类目基线`,`w = n/(n+N0)`
(n = 该店该大类近窗订单数,N0 初值 30,配置化)。新店零样本 → 全靠全局
基线 + 目标缺口,天然冷启动。

**死店三答**:
1. **数据层**:订单/KPI/快照历史行全部永存原文——死店的两年销量**全部沉淀
   在产品/品牌/类目/季节四个维度继续起作用**,只有"店×类目"这一层天然
   只属于在册店,一条数据都不报废;
2. **占用层**:死店的品牌/SKU 走 store_release 显式释放,释放后其品曾经
   "卖得动"的历史反而是回到自由池后的加分项;
3. **映射层**:死店**不需要**店名映射;需要映射的只有"还在营但改过名"的店。
   库内 store 列已混三个来源(order_sync=凭证表现名 / order_history_import=
   excel 旧名 / kpi_history_import=旧 sheet 标题),无程序化映射——旧名行
   自动只进全局三视图,不污染店×类目视图;真需要时以一张小映射表在**视图层
   折叠**,永不改写事实表。

## 九、与现有链路的接缝

### 9.1 list_new 闸门链

现"全局 ASIN 去重"读在线快照(`walmart_items`),产品下架即失守,且被死店
冻结行污染 ⇒ 改为**占用闸**(读 claims),快照只做辅助;品牌占用检查同轮加入。
分配是计划层,**不受 list_new 日配额闸影响**(配额只约束执行节奏)。

### 9.2 上架表写入(两处硬约束)

1. 现有五个写函数全部按行号定点写,**无一能新建行** ⇒ 追加新行要照
   `services/maint_sheet.append_records` 的水位模式另写(`ops.cursors` 水位 +
   逐 50 行验空防漂移 + 500 行/块 + **每块成功即落水位**)。
2. 分配要写的 A/B/D/E 四列**正是审核批次 D 投影的目标列**(`product_audit`
   明写"投影在批次 D 切换日开闸")⇒ **写列权责必须与批次 D 同一次拍板**,
   否则投影器与分配器互相覆盖。E 列写 `pass`(飞书侧词表,不是 approved)。

### 9.3 事件账本

`services/product_events` 的 `record_many` 对未登记事件码抛错 ⇒ 分配留痕前
**先登记新码**。

### 9.4 死店冻结行(已知缺陷,处置归 A1)

`catalog_sync` 只扫在册店 ⇒ 已从凭证表删除的店,其 `walmart_items` 行
**永久冻结为"在架"**,污染三处:①飞书在线产品总表投影;②**list_new 全局
去重闸**(不分店);③maintenance 每轮空转生成意图再"凭证缺失跳过"。

处置:`store_release --store X --execute` 时同步
`UPDATE catalog.walmart_items SET missing_since = now() WHERE store = X AND
missing_since IS NULL`——店铺终止后商品事实上已全部下架,这是**校正观测**
不是伪造(dry-run 先列行数)。存量清单由 `alloc_audit` A4 给出。

⚠ 判"不在册"必须用 `stores.registered_names()`(凭证表全集),**不能用
`load_stores()`**(它按启用/ClientId/代理筛过,会把"代理没配的在营店"
误判成死店,而死店清单直通整店释放)。

## 十、批次与当前状态

| 批次 | 内容 | 状态 |
|---|---|---|
| **A0 数据接线** | 产品全量入库 / 审核结论 / 订单历史 / 店铺目标四列 | ✅ 收口(见下) |
| **A0.5 存量审计** | `alloc_audit`:控制台六节结论 + 明细 csv **5 份** | ✅ 首跑完成 2026-08-15;**订单入库后需重跑**——冲突处置的"留销量大的店"首跑时无订单数据,只能按件数打平(报告会显式点破,不会装作判过了) |
| **A1 占用台账** | claims 表 + services/claims + services/alloc_survey(判定口径共用)+ list_new 占用闸 + store_release + alloc_backfill 存量回填 | ✅ **代码就绪 2026-08-15**,待生产回填 |
| **A1.5 订单 ASIN 归一** | `order_lines.asin` 列 + `order_asin_normalize`(规则走 `services/sku_asin` 唯一出处)+ **写入侧当场填**(`extract_order_lines` / `order_history_import`;`upsert_order_lines` 配 `COALESCE(EXCLUDED.asin, t.asin)` 守卫,防同步把扫尾填好的值冲回 NULL) | ✅ **生产完成 2026-08-15**,实测见 §十一.4 |
| **A2 分配引擎** | §七 全部 + 四个维度视图 + 方案表 | 🔄 **两侧体检已落地 2026-08-15**:店铺侧 `services/store_perf` + `alloc_stores`;产品侧 `services/product_score` + `alloc_products`(硬闸/五信号/权重摊回/黑历史罚分,附**信号覆盖率**一栏 —— 权重再合理信号采不到就是空的)。**A2 主体已落地 2026-08-16**:`services/alloc_engine`(分层轮转,18 条回归)+ `services/product_pool`(候选池与打分,单一出处)+ `services/alloc_groups`(品牌组组装,8 条)+ `workflows/alloc_plan`(漏斗/配额/定向流/切批/发牌/方案表/三张验收指标,默认 dry-run,15 条)。**飞书上架表写入(§9.2)仍待所有者裁定**——它的 A/B/D/E 列与审核批次 D 的投影相撞 |
| **A3 学习型** | 权重离线校准(远景) | ⬜ 快照从 A2 第一天就落 |

**A0 收口明细**:
- 产品:全量已重采进 v4 → 经既有 `product_ingest` 泵入;验收 = 连续跑至
  一轮 0 条、核对行数(v4 事件保留期 30 天,**别拖**);
- 审核:**无需专门工作流**——main 已把审核系统整库迁入本仓,`product_audit`
  直接写 `catalog.products`(原计划的 `audit_sync` 已撤销,见附 B.2);
- 订单历史:**已导入**(`order_history_import`,main PR #35 的实现——与本分支
  同期各写了一版,2026-08-15 合并时按 main 版收口,本分支那份删除);
  15 万行 / 2024-03~2026-07 / 494 家店,`source='历史数据'` 标记残缺行,
  `order_center_push` 据此不推飞书。语义限制见 §四"历史订单行的三条语义";
- 店铺目标四列:限额表已建列 + `services/store_targets` loader 已建;
- 未完成的既有待办:daily_report/product_ingest 挂调度(分配依赖这两条链保鲜)。

## 十一、A0.5 首跑实测(2026-08-15)与由此产生的未决

### 11.1 实测数字(`alloc_audit` 生产首跑)

| 项 | 实测 | 含义 |
|---|---|---|
| 产品库 | 120.4 万,approved **51.4 万**,**引擎可分候选 51.2 万** | 候选面足够大,四道谓词损耗极小(标题 0、PT 0.4%、大类 0.06%) |
| PT 来源 | 实证 7.9 万 / **推断 43.3 万(84%)** | §三.3 的"开新类目需实证"门槛在实践中会挡下绝大多数——**这条要重新拍板**(下 11.2①) |
| 评分/评论 | **命中率 100%** | 进 v1 产品分 |
| PT 字典 | 两本 7,008 行完全一致,大类 **26 个** | 漂移担忧解除,权威之争消解 |
| 在线行 | 32,634(已发布 26,842) | 47 家店有货 |
| **同 ASIN 跨店** | **687 个 ASIN** | 多为同一人名下多店(谭总 2/4/6/7)铺同款 |
| **同品牌跨店** | **1,657 个品牌**(最多 7 店 30 件) | 品牌排他的存量冲突面 |
| **超 2 大类的店** | **41 / 47 家**,最多 21 类 | 与"一店两大类"差距极大(下 11.2②) |
| 死店冻结行 | **0** | §九.4 的缺陷当前无存量,机制仍要做(防将来) |
| 渠道不符 | 10 家店,各 1~9 件;**渠道值认不出 611 行** | 不符面很小;611 行 `N/A` 需查采集侧 |
| 在册店 | **505 家**,其中有货 47、填了配送限制 43、空店 458 | 分配的店铺范围需界定(下 11.2③) |
| 非 ACTIVE 有货店 | 24 家 SUSPENDED | 占用按设计保持,在线行仍计入冲突 |

### 11.2 所有者当日答复(2026-08-15,八条)

| # | 问题 | 拍板 |
|---|---|---|
| 1 | 开新类目的实证门槛(84% 推断 PT) | **消解**——类目档案改为飞书三列人工填写(§三.1a),引擎不开类目,证据门槛问题不存在 |
| 2 | 一店两大类对存量怎么落 | **保留在线数量最多的两类**;脚本先给建议清单(C1),所有者填进「类目1/2/3」;**只准入表里填的,没填=不限制** |
| 3 | 分配的店铺范围 | 由类目/渠道/目标四列共同界定:没填的店不受类目限制但仍需渠道与容量才能参与自由流(A6 点名缺列) |
| 4 | 存量冲突(687 ASIN / 1,657 品牌跨店) | **留销量大的店**;`alloc_audit` C3/C4 出保留/下架逐行清单。⚠ 实测 96%/86% 的组**两边该商品都零销量** ⇒ 按**降级阶梯**判(该商品销量 → 该店该大类销量 → 该店整体销量 → 在线件数 → 店名),判定依据逐行写进 csv;**只有落到最后一级(店名)才需人眼**——在线件数是库里查得到的客观数据,算机器判得出(所有者 2026-08-15 晚追加,`services/alloc_survey.NEEDS_HUMAN`)|
| 5 | 渠道不符商品 | **给清单自己下架**(C2 逐行 SKU 清单) |
| 6 | 评分/评论、大类、PT 覆盖 | 库里都有,按实测用 |
| 8 | SUSPENDED 店怎么处理 | **口径不变,仍是「暂停不释放、占用保持」**,在线行照常计入冲突。原话「暂时不分配」说的是配额不是占用 —— 所有者随后用限额表「**单店最大在线数**」填 **0** 解决(当日已对不需要分配的店设好)。⇒ 口径总表 #13、§六.2/2a。⚠ 中途曾被实现成「当作全新店、不建占用」,所有者当即纠正,已 revert |
| 7 | 报告可读性(「太难看了,我都看不明白」) | **控制台只放结论 + 下一步动作,逐条明细全进 csv**:六节(要你做的事/数据体检/在线商品/冲突/店铺/要留意),每节至多四五行;新增 `alloc_店铺总览.csv` 承接原来控制台里逐店点名的那些串。查不了的项(`-p channel=0`、限额表读不到、订单没导入)必须显式写「跳过/本轮没查」及原因——**拿 0 冒充「全合规」是这份报告最容易骗人的地方** |

### 11.3 仍未决

| # | 事项 | 阻塞谁 |
|---|---|---|
| 1 | **上架表 A/B/D/E 写列权责** —— 与审核批次 D 投影同一次拍板 | A2 落地 |
| 2 | 611 行渠道 `N/A` —— 采集侧没采到还是解析退化(这批产品在渠道闸下不参与分配) | 精度 |
| 3 | 冲突判定**降级阶梯**是否合意(零销量时用"该店该大类/整体销量"当代理指标),以及落到最后两级(连店铺整体销量都相同)的那批怎么处置 | A1 回填 |
| 4 | KPI 8 率阈值(定了 health 才能进配额公式);权重与淘汰线终值(首批 dry-run 砍) | 精度,不阻塞 |

---

# 第二部分 决策留痕(历史,不作实现依据)

### 11.5 产品分生产实测(2026-08-15 20:30,两轮校准)

候选池 **512,077**(approved ∧ 有标题 ∧ PT 有效 ∧ 大类查得到)。

| 信号 | 作用 | 覆盖率 | 说明 |
|---|---|---|---|
| 评分 / 评论数 | 口碑基础分 60/40 | **84.3%** | 缺的那 15.7% 口碑段记 0 分 |
| 历史销量 | 加分 最多 +25 | **1.0%** | 候选池绝大多数从没在沃尔玛上过架,加分项几乎只在这 3,897 个上生效 |
| 配送时效 | 罚分 最多 −15 | 98.2% | ⚠ 其中绝大多数 ≤8 天 ⇒ **零区分度**,所以只能当罚分不能当正向信号 |
| 退货率 | 罚分 最多 −20 | **0.9%** | 只有 API 期算得出,历史期一律不罚 |

硬闸淘汰:落地价算不出 **61,163**、库存不足 **38,781**。

**两个被实测抓出来的实现错**(都违反已写口径,已修):
1. 旧的五信号加权平均 + 权重摊回 ⇒ "只有配送时效有值"的品**独占 100% 权重拿
   100 分**;而 98.4% 的品配送都 ≤8 天,等于给所有缺口碑数据的品白送满分
   (第一轮 P75=82.3 / P90=95.5 全被这批顶上去);
2. 销量参与加权(.35)⇒ 卖过 3 件的品比没订单史的**低 17.6 分**,违反
   口径 #8「只加分不减分」;而销量覆盖率仅 1.0%,等于把**唯一有正面证据的
   那批品系统性压分**。

**淘汰线基本没在筛**:第二轮 P25=42.3、中位 50.6,40 这条线只切掉两成。
真正的闸是**容量** —— 14 家参与店合计剩余约 3 万个货位,对 27 万可分候选。
⇒ 引擎按**分数排序取到配额为止**,淘汰线只当兜底扫明显烂品。

### 11.4 A1.5 生产实测(2026-08-15 20:00)

**「sku = asin」这条约定由数据确认**(所有者 08-11 曾给样推翻过全局版本,
本次实测给出了准确比例):

| 形态 | 个数 | 占比 | 处置 |
|---|---|---|---|
| `asin` 裸 ASIN | 92,293 | 99.06% | 直接命中 |
| `wrapped` 三段式 | 141 | 0.15% | 模式提取 |
| `numeric` item_id | 337 | 0.36% | **倒查 walmart_items 一个都没中** |
| `other` 自定义 SKU | 396 | 0.43% | 无 ASIN 可提 |
| **可解析** | **92,434 / 93,167** | **99.2%** | |

**行覆盖率 151,826 / 152,992 = 99.2%** —— 与 SKU 覆盖率相同,说明解析不了的
那批**没有集中在高频老品上**(否则行覆盖率会明显更低),对销量维度的影响
就是它看起来的那么小。

两批永久 NULL,不必再等规则扩充:
- `numeric` 337 个:item_id 不在 `catalog.walmart_items` 里,多半是早已退市、
  从未被同步过的老 listing。**重跑也不会变**——`catalog_sync` 不扫退市店的旧品;
- `other` 396 个:样本 `05110/youxiji/blue/128G`、`051120/烟雾净化器`
  (看前缀是 2020-05-11 那批卖家自定义 SKU),**里面根本没有 ASIN**。

这 733 个 SKU 退出产品/品牌销量维度,但照常进店×SKU 维度(那一层用 sku)。

## 附 A 拍板记录

### A.1 所有者 2026-08-07(立案四条)

1. 大类目 = Walmart Category(与 risk_product_types 同口径);
2. 店铺状态从数据库读(ACTIVE/SUSPENDED/TERMINATED),TERMINATED 触发的是
   "可释放资格"不是自动释放;
3. 存量回填推迟,先出冲突审计报告;
4. 引擎第一版透明打分,不用 ML 不用 LLM。

### A.2 所有者 2026-08-12(Q1~Q8 八条)

| # | 问题 | 拍板 |
|---|---|---|
| Q1 | 店铺三态权威源 | 直接读数据库状态字段;"释放某店全部品牌与产品"允许手工操作;不引入手工终止登记列 |
| Q2 | v3 存量用途 | 导出入候选池 → **当晚作废**(全量已重采进 v4,见附 B.1) |
| Q3 | 两年订单 excel | 文件在生产 Mac;表头当日提供,精确匹配实现 |
| Q4 | 店铺目标 | 已在限额表新增三列,销售额与订单为**日目标** |
| Q5 | 开新大类 | 方案表提议制可接受;在营店存量按数据定保留类目与产品,不合规存量走**下架过渡** |
| Q6 | 权重/淘汰线 | 按草案跑首批 dry-run,对着方案表砍 |
| Q7 | 品牌归一化 | casefold + 去噪声词;**不要别名表** |
| Q8 | 存量冲突处置 | 审计报告出来后议(仍未决,§十一.2) |

### A.3 所有者 2026-08-12 晚(两条)

① 全量产品已重采进 v4 ⇒ 候选池方案作废(附 B.1);
② 新增硬约束"一店一配送方式"。

### A.4 所有者 2026-08-13(三条)

① 渠道权威 = 限额表「配送限制」列(⇒ store_channel 表方案作废,附 B.3);
② 分配两梯队,有在线产品的 ACTIVE 店优先;
③ 没出过订单是常态,历史销量只加分不减分。

## 附 B 已作废方案(留档防重复讨论)

| # | 方案 | 作废日 | 理由 |
|---|---|---|---|
| 1 | **v3 存量导出 → `catalog.candidate_pool`** | 08-12 晚 | 所有者已把全量产品重采进 v4,候选宇宙 = `catalog.products`(保鲜数据,零二手搬运)。另有三条独立理由:v4 迁移计划明文"不搬数"、绕过 API 直灌不产生事件流(增量导出看不见)、v4 主表覆盖式。`candidate_import` 工作流与表已移除(未上生产) |
| 2 | **`audit_sync`(跨库拉审核结论)** | 08-15 | main 把审核系统整库迁入本仓(audit schema,614 万行),`product_audit` 直接写 `catalog.products`,跨库回流失去意义。工作流与 `WALMART_AUDIT_DSN` 已删 |
| 3 | **`catalog.store_channel` 表 + 首次接货锁定提议制** | 08-13 | 所有者直接在限额表建「配送限制」列,人工配置比引擎锁定更直接(改渠道 = 改一格) |
| 4 | **"pass 有 45 天 TTL,每轮全量对齐"** | 08-15 | TTL 已废除,改 hash 驱动重审、rejected 永不自动重审;重采致 slow_hash 变更会把 approved 翻 pending ⇒ claims 必须快照 audit_version |
| 5 | ~~"评分/评论数已随 raw 落库"存疑~~ | **08-15 撤销存疑** | P2 探针实测命中率 100%,原判断成立;该信号进 v1 产品分(§七.6) |
| 6 | **"27 个大类"** | 08-15 | 实测 **26 个**(P3);取值域以实测为准 |
| 7 | **health_i 进 v1 配额公式** | 08-15 | 8 率阈值零实现且 NULL 两义,给恒为 1 或 NULL 的量分权重无意义 |

## 附 C 结论词表三套(实现时按落点分别用)

| 落点 | 取值 |
|---|---|
| `audit.audit_runs.verdict` | pass / reject / pending |
| **`catalog.products.audit_status`** | **approved / rejected / pending**(候选池按这套) |
| 飞书上架表 E 列 | `pass`(大小写不敏感) |

## 附 D 摸底事实(2026-08-12 四外部仓库,仍有效的部分)

- **采集器 v3 vs v4**:v4 就是 v3 的 PostgreSQL 迁移版,增量导出契约实现在
  v4;v3 已把该次迁移整体 revert 回纯 SQLite。本侧对接的采集服务以 v4 为准。
- **历史后台报错库**:2026-08-11 三笔已全部入库(error_items 48.5 万行 →
  product_events 23.9 万条时间线、asin_blacklist 5.68 万、品牌渠道表 2,012),
  产品"黑历史"信号直接读 `product_risk` 视图与三张黑名单表,零新工作。
- **类目映射表**(amazon-walmart-category-mapping):v5.5 主表 15,770 行,是
  审核链 L1 的输入;分配链不直接依赖(分配只读 `products.walmart_pt`)。
  ⚠ main 之后映射表已成为**本仓在写的活表**(catmap_mine/catmap_fix)⇒
  同一 ASIN 跨批分配可能拿到不同 PT/大类,claims 与 allocation_items 必须
  快照 PT。
- **订单**:API 只拉 45 天窗口,两年历史靠 `order_history_import` 一次性导入,
  与 API 行同源行标识(PO+SKU),重叠单天然去重、只插不改。
