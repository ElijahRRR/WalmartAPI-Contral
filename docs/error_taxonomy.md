# 沃尔玛报错解析与归类方案(第一步:只解析归类,不接处置)

> 所有者定稿流程(2026-09-01):①先把报错**解析→归类**做准做闭环;②归类稳定后按类接
> 处置(拉黑/修 PT/改文案/改价/补信息…);③政策类整体喂给 L3 优化(不做单条政策专项)。
> 本文档只覆盖第①步。设计依据 = 2026-08-31 生产全量报错实查(数字与原文见
> `tests/fixtures/reason_corpus.jsonl` / `feed_error_corpus.jsonl`,全部取自生产原文)。
>
> 分工:本方案由规划侧定稿;执行侧照 §八清单实现,**不得自行更改码表/判据/优先序**,
> 发现方案与仓库现状冲突时停下来记录,不许自作主张绕过。

## 〇、第一步的范围与非目标(红线)

**做**:
1. 新模块 `services/error_taxonomy.py` —— 纯函数解析归类引擎,零 DB、零副作用;
2. `registry/resources.py` 新增类别码等常量(§二);
3. 只读对照报告工作流 `workflows/error_reclass_report.py` —— 新旧归类对照,给所有者验收;
4. 语料回归测试(夹具已由规划侧写好,执行侧让引擎通过它们)。

**不做**(第二步之后、且需所有者过完对照报告才动):
- **不改** `services/problem_products.categorize()`(生产归类照旧跑);
- **不改** `listing_sheet.classify_receipt` / `list_new` / 黑名单 / 事件 / 任何 feed 行为;
- **不建**处置映射、不动 `PERMANENT`、不动调度表(不跑 skill_export)。

**双轨说明**(conventions 双轨禁止的对表):过渡期内新引擎的**唯一消费者是对照报告
工作流**,生产判定路径未变 —— 这不是两条并行的生产路径,而是 NRTL 先例的"先补"阶段;
"删"(单提交把 problem_products 换轨到新引擎)在第二步,门 = 所有者过对照报告。

## 一、码表(16 码,所有者 2026-09-01 认可的归类表)

| 码 | 中文 | 覆盖(生产实测量级) | 第二步处置方向(本步不接线,仅注记) |
|---|---|---|---|
| `PROHIBITED_FINAL` | 禁售不可申诉 | 853 在架 + 950 历史 | 拉黑 ASIN |
| `IP` | 知识产权 | ~100+ | 拉黑 ASIN + 品牌 |
| `BRAND` | 品牌未授权 | ~320 | 拉黑品牌 |
| `POLICY` | 违反禁售政策(**全部政策类都在这一类**:单品类政策/冒犯内容/儿童产品…,不单列;具体政策名作 `policy_name` 附注) | ~700 在架 + feed 侧 ~240 | 喂 L3 优化,不拉黑 |
| `PT_WRONG` | 类目选错(沃尔玛明示"把 product type 选对") | **2,957** | 修 L1 类目判定 |
| `CONTENT` | 内容问题(含 AI 内容审查,`via_ai` 标记区分) | ~120 + feed 3,555 | 改文案图片 |
| `PRICE` | 价格规则 | ~800 | 改价 |
| `GATED` | 类目需预审批/资质 | ~90 | 申请资质 / 类目表标禁售 |
| `INFO` | 信息缺失(无价/无物流/无配送中心) | ~40 | 补信息 |
| `EXPIRED` | End Date 过期 | 9,024 | 现行删除;回补策略后续单独议 |
| `STAGE` | Stage 未上线(中性,非问题) | 8,311 | 不处理 |
| `FLAGGED` | 沃尔玛内部标记 | 历史 2,509 | 人工(根因待查) |
| `RECALL` | CPSC 召回/安全警告 | 1-2 | (第二步议,倾向拉黑) |
| `SPECIAL` | 特殊计划(Resold/Preorder/Pre-owned) | ~15 | 不上这类 |
| `SYSTEM` | 沃尔玛系统错误 | ~20 | 重试/无操作 |
| `OTHER` | 未识别 + 显式杂项清单 | 目标 <50 | **强制告警**,人工看过再补表 |

**判例定稿**(语料里已按此标注,执行侧不得改):
- 儿童产品/冒犯内容/万圣节/食品/刀具/军警…一律 `POLICY`,不造子码;政策名进 `policy_name`;
- `Jewelry…Covered Goods` 那两条(正文是 "require pre-approval")→ `GATED`(政策名照记);
- 图片 logo/占位图/Listing Quality 内容标准 → `CONTENT`;
- "business decision" / "Trust & Safety policies"(各 1-2 条)→ `OTHER` 显式清单,不造码;
- `PT_WRONG` 是**升格码**:原子同时含通用政策拒 + "appropriate product type selected"
  时压过 `POLICY`(该原子的病根是我方类目选错,不是商品违禁)。

## 二、registry 常量(唯一出生地;执行侧照抄)

生在 `registry/resources.py` 错误码登记区之后:

```python
# ── 报错归类(第一步:引擎与对照报告用;换轨接线在第二步)────────────
ERROR_TAXONOMY_VERSION = "t.2026-09-02.1"   # 码表/判据变更时手动递增
ERROR_CATEGORY_CODES = {                     # 码 → 中文名(全大写码,与旧 A-L 单字母码同列可辨)
    "PROHIBITED_FINAL": "禁售不可申诉", "IP": "知识产权", "BRAND": "品牌未授权",
    "POLICY": "违反禁售政策", "PT_WRONG": "类目选错", "CONTENT": "内容问题",
    "PRICE": "价格规则", "GATED": "类目需预审批", "INFO": "信息缺失",
    "EXPIRED": "过期下架", "STAGE": "未上线", "FLAGGED": "内部标记",
    "RECALL": "安全召回", "SPECIAL": "特殊计划", "SYSTEM": "系统错误",
    "OTHER": "未识别",
}
# 记录级主码序:终局优先,非中性永远压过中性(J 吃 42.9%、A 盖 641 条的病根按性质钉死)
ERROR_CATEGORY_SEVERITY = (
    "PROHIBITED_FINAL", "IP", "BRAND", "RECALL", "PT_WRONG", "POLICY",
    "GATED", "FLAGGED", "CONTENT", "PRICE", "SPECIAL", "INFO",
    "SYSTEM", "OTHER", "STAGE", "EXPIRED",
)
# feed 报错的政策族锚:field 稳定、error_code 一次性(生产实证:Offensive 171 次
# 散在 35 个互不相同的码上)。QARTH/OFFER/sku 等多义 field 不入此集合。
WALMART_ERR_FIELD_POLICY = frozenset({"Defects Platform", "RNA"})
```

正则、句式判据表、别名表是实现细节,生在 `services/error_taxonomy.py` 模块内
(先例:`problem_products._RULES` 就在 services)。

## 三、解析规范

### 3.1 输入形态

- **来源 1/2(下架原因)**:库里是 `"; "` 拼接串(api/items.py `"; ".join`)。
  引擎收 `atoms: list[str]`;拆串由调用方用引擎提供的 `split_reasons(text)` 完成:
  按 `"; "` 拆,**但整行含 AI 尾句(§3.4)时不拆整行短路**(AI 文案自身可能含分号)。
  拆分安全性已生产实证:最短原子 36 字符是完整句。
- **来源 3(feed 报错)**:`ops.feed_item_errors` 已逐条,输入 `(code, field, description, status)`。

### 3.2 规范化 `normalize_atom(text)`

1. `html.unescape` 循环至不动点(≤3 次,处理 `&amp;amp;` 双重转义);
2. 剥 HTML 标签:`<[^>]{1,80}>` → 空(生产实见 `<p>…</p>` 包裹整句);
3. 链接标记 `||链文本@@@url||` → 保留链文本、丢 url(与 problem_scan 现有探针同形态);
   **残缺标记**(如 `- )` 结尾、`@@@` 后无闭合)按同规则尽量剥,剥不净不报错;
4. 空白折叠为单空格、去两端;
5. 产出两个副本:`casefold` 副本供句式匹配,原大小写副本供政策名抽取。

### 3.3 原子判定次序(表序即优先级,首命中即码;全部 casefold 子串/正则)

| 序 | 码 | 判据(casefold) |
|---|---|---|
| 0 | `CONTENT`(via_ai=True) | 含 `generated by ai`(整原子短路,正文不再进任何规则) |
| 1 | `PT_WRONG` | 含 `appropriate product type selected` |
| 2 | `PROHIBITED_FINAL` | 含 `not eligible for appeal` |
| 3 | `RECALL` | 含 `cpsc recall` 或 `safety warning` |
| 4 | `FLAGGED` | 含 `flagged by our internal team` |
| 5 | `IP` | 含 `intellectual property` 或 `copyright`,或正则 `\bip\b\s+(policy|claim|related)` |
| 6 | `BRAND` | 含 `brand restrictions` / `not authorized to list this brand` / `partnered with select brands` / `biz-cn`(旧 C 口径;biz-cn 行在处置层单独标 biz_cn 维度、只拉 ASIN 不拉品牌 —— 第二步细化) |
| 7 | `GATED` | 含 `requires? pre-approval`(正则,单复数皆收 —— 生产原文有 "these categories **require** pre-approval")/ `restricted to certain sellers` / `request approval to sell` / `enhanced vetting program` / `health & compliance page` |
| 8 | `PRICE` | 含 `pricing rule` / `price gouging` / `unintended price` |
| 9 | `CONTENT` | 含 `content issues` / `content policy` / `does not meet our content standards` / `content quality standards` / `image guidelines` / `placeholder images` / `missing a primary image` / `main image url` / `authenticity claims` —— ⚠ 不许用裸 `content standards`:政策文本里有 "do not meet **offensive** content standards"(sex toys 条),裸串会把政策拒误抢成内容问题(语料有反例钉死) |
| 10 | `SPECIAL` | 含 `resold program` / `preorder program` / `pre-owned` / `restored program` |
| 11 | `INFO` | 含 `no price was found` / `shipping information was not added` / `active distribution center` / `missing required logistics` / `tax code` |
| 12 | `SYSTEM` | 含 `internal error occurred` / `glitch` |
| 13 | `STAGE` | 含 `stage status until you go live` |
| 14 | `EXPIRED` | 含 `end date has passed` |
| 15 | `POLICY` | 含 `prohibited product` 且含 `policy`(覆盖 Products/Product 两形、缺首字母 typo) |
| 16 | `OTHER`(显式) | 含 `business decision` / `trust & safety` / `currently under review`(审查中会自愈,不造码)—— 计入 `unlisted` 清单,不进 unknown 告警 |
| 17 | `OTHER`(未识别) | 兜底 —— **必进 unknown 告警清单,原文全文保留** |

判据纪律:**不锚句首、不依赖 "This"**(缺首字母 `his item…` 的 29 条 typo 免疫);
判据串一律来自生产原文,禁止拟写。

### 3.4 政策名抽取 `extract_policy(atom_raw)`(对每个原子独立跑,填 `policy_name`,与码正交)

1. 正则(对原大小写副本):
   `Prohibited\s+Products?\s+Polic(?:y|ies)\s*:\s*([^.;|@*]{2,160})` —— **冒号必需**,
   冒号后取候选;无冒号 = 无候选(生产反例:`*Prohibited Product Policy* for Toys`
   类别在标记外、`Children's Products Prohibited Products Policy` 结构反置 —— 都不猜);
   `*…*` 星号包裹形态(feed 侧)与 `||…||` 链文本形态经 §3.2 已剥,统一落这个正则;
2. 候选清洗:去两端标点/空白(**配对的尾 `)` 保留**,孤 `)` 才剥 —— 官方名自带括号
   后缀 `…(Covered Goods)`,§十一);剥前置停用词 {`Walmart`, `Walmart's`, `Our`, `The`, `This`};
   截断在首个 `.` `;`;**逗号截断有条件**:仅当逗号后是续句(小写起头且首词非
   and/or/&)才截 —— 政策名自身带逗号是常态("Jewelry, Watches, …Precious Metals"、
   "Tobacco, E-Cigarettes, and Vaping Products"),无条件截会腰斩;要防的
   "Policy: X, please review…" 恰是小写续句形态;
3. 主/子类拆分:候选含逗号已被截,子类形态 `Offensive Content, Halloween Items` 由
   截断前先查:若逗号后文本 ∈ 已知子类词形(Halloween/Inappropriate/Intolerance/
   Violence/Sex Toys/Politics 家族)则 `policy_sub` 收下、主名取逗号前;否则按 2 截断;
4. 无候选的兜底:原子命中 `POLICY` 词根但抽不出类别名(通用政策拒 3,363 条的常态)
   → `policy_name=None`,合法,不告警;
5. 抽出候选但**与政策表 join 不上** → 保留候选原文,进"政策表缺口"清单(报告显示,
   不算 unknown)。join 用 `audit.walmart_prohibited_policy.category_en`,由调用方注入
   字典(引擎纯函数不连库);别名表(如 `Auto and Motor Vehicles`→`Auto & Motor
   Vehicles` 之类的词形差)生在引擎内,**别名的目标值必须在注入字典里,运行时校验**。

### 3.5 记录级归类 `classify_reasons(atoms, policy_names) -> Result`

```python
Result = namedtuple("Result",
    "code name policy_name policy_sub via_ai atom_codes unknown unlisted")
# code      : 主码 = 各原子码按 ERROR_CATEGORY_SEVERITY 首命中
# atom_codes: tuple[(原子码, policy_name|None), ...] 逐原子,供病历附注/报告
# unknown   : 落 OTHER(未识别) 的原子原文列表 —— 调用方必须告警,引擎不吞
# unlisted  : OTHER(显式清单) 命中的词条计数
```

### 3.6 feed 侧 `classify_feed_error(code, field, description, status) -> FeedResult`

```
通道判定(次序):
  1 code ∈ registry 既有 WALMART_ERR_CONTENT,或 description 含 AI 尾句
                                      → channel="content_ai"(码 CONTENT, via_ai)
                                        (code 锚必须保留:生产明细的 description 常被
                                        截断,AI 尾句可能不在截断内)
  2 field ∈ WALMART_ERR_FIELD_POLICY 且 status == "failed"
                                      → channel="policy":正文过 §3.3 序 1-15
                                        (通常落 POLICY/IP,policy_name 照抽)
  3 code ∈ registry 既有 WALMART_ERR_ASYNC_REVIEW → channel="async"(审查中,非报错)
  4 其余                              → channel="operational",原样带回 (code, field)
                                        —— 已结构化,不强行进 16 码表
```

⚠ `status=="failed"` 这道闸是硬的:生产契约里 SUCCESS 回执可以同时带
ingestionErrors(带警告的成功),field 锚不许把它们判成政策拒。
`ops.feed_item_errors` 本身**没有 status 列** —— SKU 级终态权威在
`ops.feed_items.status`,报告用 `(feed_id, sku)` LEFT JOIN 供数;JOIN 不上按
非 failed 处理(判不准就不判成政策拒,方向保守)。
本步 `classify_feed_error` 只被对照报告消费,**不接 `classify_receipt`**。

## 四、模块与接口

`services/error_taxonomy.py`(新):模块 docstring 首行"输入→输出";公开面 =
`split_reasons / normalize_atom / extract_policy / classify_atom / classify_reasons /
classify_feed_error / RULES`(RULES = 可枚举判据表,供守门测试断言全码有判据)。
纯函数纪律:不 import db/api/workflows,不读环境变量;政策字典由调用方注入。
常量按 §二进 registry;引擎从 registry 取码表与主码序(铁律 3)。

## 五、对照报告 `workflows/error_reclass_report.py`(只读,手动跑,不进调度)

`run(params) -> 摘要`;`DANGEROUS = False`。所有者在生产机跑:
`python cli.py error_reclass_report`。

1. **数据面**:`catalog.walmart_items`(下架且有原因的行,含 `missing_since` 非空的)、
   `catalog.product_events` 的 `status_changed`(`detail->>'reasons'`)、
   `ops.feed_item_errors` 全量;政策字典读 `audit.walmart_prohibited_policy`;
2. **对每条**:旧 `problem_products.categorize()` 结果 vs 新引擎结果并排;
3. **输出**:
   - 迁移矩阵(旧码 × 新主码,计数)+ 按新码分布(必须能复现实查:Z 725 逐条去向、
     被 STAGE/EXPIRED 盖住的 641 条组合全部翻成非中性、PT_WRONG ≈ 2,957);
   - unknown 全文清单(目标 <50,超了说明判据漏);
   - 政策表缺口清单(抽到类别名 join 不上的,逐名计数);
   - feed 侧:政策族逐条分类 + 政策名命中率;未知 field 榜;
   - 摘要打印 + 全文报告落盘 —— **落点从 `registry/paths.py` 取**(有现成报告/导出
     目录常量就复用,没有就在 paths.py 新增一个,不许写死路径)。
4. 连接一律走 `registry/db.py`;只 SELECT,零写库。

## 六、语料与测试(夹具已由规划侧写好,是本方案的机器可验形式)

- `tests/fixtures/reason_corpus.jsonl`:每行
  `{text, expect_code, expect_policy, expect_sub?, expect_via_ai?, atoms_expect?, note, provenance}`;
  `text` 为记录级原文(可含 `"; "` 组合);**全部原文取自 2026-08-31 所有者贴回的生产
  查询结果,逐字未改**;`provenance:"synthetic-guard"` 的少量条目只做反例守门,不算语料。
- `tests/fixtures/feed_error_corpus.jsonl`:每行
  `{code, field, status, description, expect_channel, expect_code?, expect_policy?, note, provenance}`;
  description 有截断的标 `truncated:true`(判据只用前缀,不受影响)。
- 测试(`tests/test_error_taxonomy.py`,新):
  1. 语料逐行断言(两份夹具全过,一行不许跳);
  2. **旧行为快照**:同一批 reason 语料跑现 `problem_products.categorize()`,断言其
     现状输出(冻结旧行为,第二步换轨时 diff 一目了然)——快照期望值执行时实跑生成后
     **人工核对写死**,不许动态比对;
  3. 守门:`RULES` 覆盖全部 16 码;`ERROR_CATEGORY_SEVERITY` 与码表集合一致;
     别名表目标值 ⊆ 注入字典(见下条:清单已换成官方 42 名);
     normalize 边角(`&amp;` 双转义 / `<p>` / `- )` 残标记 / 缺首字母)逐一钉死。
- ⚠ 我方连不上生产库:`category_en` 对照清单原以仓内唯一权威出处兜底(`services/
  audit_l3` 政策路由表 + `audit_reason._L3_NORMALIZE` 目标值,合计 30 条)。
  **2026-09-02 起换成目标态**(定稿 `docs/policy_sync.md` §十.7:官方政策类别名 =
  全链唯一键):测试 `KNOWN_POLICIES` = `refdata/policy_pages/en/*.md` 头注 H1 的
  **官方 42 名**,并加守门测试证明二者逐字一致(`policy_sync` 真跑后生产表就长这样)。
  join 不上的候选进"政策表缺口"清单由报告显示,不判错。
- ⚠ **`POLICY_ALIASES` 不再手写**(2026-09-02):从 `registry.resources.POLICY_LEGACY_NAMES`
  (仓内唯一一份「表内旧名 ↔ 官方名」映射)**反向派生** —— `{归一化(官方名): 旧名}`。
  它是**过渡期产物**:报错正文里的政策名一直是官方全称,而政策表存量行是旧仓搬迁时的
  缩写名,两边对不上会把一堆真实存在的政策算成"政策表缺口"。`policy_sync` 把表内名改成
  官方拼写之后,直接键就命中了,这张表连同 `POLICY_LEGACY_NAMES` 一起随第三步 L3 批删除。
  ⚠ 那时 `alias_gaps()` 会一次性报出全部 7 个旧名"指不到表" —— 那**不是失效告警**,
  是别名功成身退的信号(报告头部那句提示要按这个读)。
- ⚠ **`_norm_key` 已归并**(2026-09-02 补批,原「三种写法仍 join 不上」的条款作废):
  它现在**就是** `services/policy_names.norm_category`(仓内唯一一份政策名归一化),
  于是 `&`↔`and`、去逗号、削 `(Covered Goods)` 括号后缀、词尾单复数四条一并生效,
  `Plants & Seeds` / 牛津逗号 Tobacco / 不带后缀的 Jewelry 三种报错写法**都 join 得上**。
  实测(语料 19 个 distinct `expect_policy`):改名前 16/19、改名后 **19/19**;
  旧手写实现是 15/19 与 16/19 —— 测试钉住"两种表形态下都只许升不许降"。
  放宽的代价评估过:`policy_join` **只喂报告、不参与判定**(`policy_name` 一律保留原文),
  而 42 个官方名两两归一化不碰撞由测试钉死,"合错两条政策"的前提本就不成立。
- ⚠ 连带:目标态下 `alias_gaps()` 报的是 **5 条**不是 7 条 —— `Auto & Motor Vehicles`
  与 `Textiles & Apparel` 两条旧名与官方名只差 `&`↔`and`,归一化后同键,别名本身已多余;
  剩下 5 条是真正的语义缩写(`Electronics & RF` 那种),归一化永远打不平。

## 七、验收(第一步完成的定义)

1. `python -m pytest -q` 全绿(含全部语料行);
2. 生产机跑 `python cli.py error_reclass_report`,所有者核对:迁移矩阵、unknown<50、
   Z 桶 725 条逐条去向合理、PT_WRONG 量级对上 2,957、政策名命中率;
3. 所有者点头 → 才开第二步(处置接线与换轨,另出方案)。

## 八、执行侧清单(照做,不发挥)

1. `registry/resources.py` 加 §二常量(位置:错误码登记区后;不动任何既有常量);
2. 写 `services/error_taxonomy.py`(§三/§四);
3. 写 `workflows/error_reclass_report.py`(§五;paths 落点按 §五.3 处理);
4. 写 `tests/test_error_taxonomy.py`(§六;夹具已在 `tests/fixtures/`,只许读不许改 ——
   **引擎迁就语料,不是语料迁就引擎**;真过不了 = 停下来把冲突记进 PR 描述);
5. 旧行为快照:实跑 `problem_products.categorize()` 生成期望,人工核对后写死进测试;
6. `python -m pytest -q` 全绿;`python cli.py error_reclass_report --dry-run` 不适用
   (只读工作流),但要确认 `python cli.py` 能列出并加载该工作流不炸;
7. 同步 `README.md` 测试计数;**不动** docs/db_schema.md(没动表)、不跑 skill_export
   (没动调度);
8. 不做任何 git 操作(提交/推送由主会话统一处理)。

## 九、执行核准记录(2026-09-01,规划侧验收)

执行侧上报 4 处方案与语料/仓库现状的冲突,裁决如下(均已回写上文对应节):
序 7 单复数(§3.3)、逗号截断条件化(§3.4)、政策清单出处退 30 条仓内权威(§六)、
feed status 走 feed_items JOIN(§3.6)—— **四处全部核准**,以语料与判例定稿为准。

疑点处置(决定权记录):
1. 政策名正则**维持大小写敏感**(方案字面)。生产存在小写 "Prohibited Product
   policy" 写法,但当前无"小写+冒号+类别名"实例;首跑报告若见再议 IGNORECASE。
2. `classify_reasons` 的 `policy_names` 参数本步只服务 join(`policy_join`/
   `alias_gaps` 供报告);第二步若要把"join 上/没上"带进病历,`Result` 需扩字段。
3. feed 政策族正文全不命中 → 落 `OTHER`,报告单列"政策族判不出码"一节提醒补判据。
4. 别名表只收词形差,不做语义合并;政策表缺口由首跑报告自证,不预判。
5. README §11 文档索引本就非全集,不动。

## 十、首轮生产对照的判据补丁(2026-09-01 轮次二,t.2026-09-01.2)

生产全量对照(28,637 + 11,485 + 216,723 条)结论:事件面 unknown=0;在架面 unknown
316 条共 7 种文本,其中 5 种是**旧判据有、新判据漏收**的(content policy / 主图缺失 /
authenticity claims / tax code / BIZ-CN+partnered brands),已按上表补进 §3.3;
`currently under review`(2 条,自愈态)进 OTHER 显式清单;政策子类词形补 Politics。
版本随判据递增为 t.2026-09-01.2。

待所有者数据确认的两件事(引擎外):
1. **政策表疑似缺武器族**:Firearms/Firearm Accessories/Knives/Firearm Ammunition/
   Jewelry 合计 ~70 条政策名 join 不上当时那张 37 行表 —— 若表里真没有,这是 L3 的判据盲区
   (第三步输入),该补的是**政策表数据**,不是引擎;
2. `Cosmetics Products`(1 条)待确认是否表内 `Cosmetic Products` 的词形差,是则加别名。

## 十一、真跑改名后首份对照的补丁(2026-09-02,t.2026-09-02.1)

`policy_sync` 真跑落地(表 42 条、17 行改为官方拼写)后的对照:事件面 46/46、feed 226/226
join 全中,在架面 715/729 —— 剩 14 条全是同一个名字被截成
`Jewelry, Watches, Precious Gemstones, Currency, Coins and Precious Metals (Cover`。
根因两处都在引擎、不在数据:

1. §3.4 正则候选上限 80 字符,而官方最长政策名 89 字符 → 上限提到 160;
2. 候选两端剥标点把配对的尾 `)` 也剥了(`_TRIM` 原含 `)`),`(Covered Goods)` 剥成
   `(Covered Goods`,`norm_category` 的括号后缀削不掉 → `_strip_edges`:右括号比左括号
   多才算孤 `)`。

同批把报告头的别名表告警分成两种读法(`alias_gaps` 文档早写了,报告没照做):旧名指不到
表但官方名已在表里 = 改名落地、别名退役(平述,不打 ⚠);官方名也不在才是真失效。
§十第 1 条(武器族)已由政策表数据补齐,第 2 条(Cosmetics)由 `norm_category` 单复数归一吃掉。
