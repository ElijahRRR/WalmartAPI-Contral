## 附录 A|四路审查意见(修订前的原始 findings;已修订包的处置写在各 item 的 why/risks 里)

### A.1 完整性

## 覆盖结论(逐条核对)

**§3.2 SQL 硬等号 6/6 全覆盖**(行号已实测复核):maintenance_intents.py:192→0a-11;:202→0a-12;:233→0a-13;:322→0a-14;product_audit.py:411→0a-15;schema.sql:527 视图→0a-04(**与横切 C0-DDL-5 重复且规格冲突,见 finding**)。

**§3.3 按形态倒推 ASIN 14/14 全覆盖**:services/order_audit.py:358-361→0b-15+0b-16;workflows/order_audit.py:423→0b-17、:461-462→0b-18、:479→0b-19、:1239→0b-20;blacklist.py:99→0b-12;:157→0b-13;order_lines.py:169→0b-09+0b-10;product_events.py:167→0b-11;audit_rules.py:176-181→0a-18;alloc_survey.py:291/796→0a-19;alloc_push.py:72→0a-20、alloc_plan.py:127→0a-21、alloc_products.py:101→0a-22;feed_track.py:179-190→0b-14(只改注释,实收口点在 0b-12,已实测 feed_track:181-184 确实传了 store,方案成立);product_refresh.py:58/89→0a-17;sources_backfill.py:46/66/90→0b-21;sku_normalize/order_asin_normalize→0b-01~0b-08。「不改的」五处(order_history_import/pt_backfill/brand_scrape/asin_blacklist_import/product_query)在 0a-26②与 C0-GUARD-2/3 的白名单里保留 ✓。

**§3.4 上架链以 ASIN 当 SKU 9/9 全覆盖**:去重闸→0a-23;重试上限→0a-24(+B2-23 核验);变体组→0a-25;回执找行:542→B1-11;heal_unknown→B1-10;_mark_upc_conflicts→B1-09(+B2-16 接弃码);sku_locked_heal:79/102→B1-12(+:233→B1-13,实测 :63/:64/:79/:92/:102/:125/:233 共 7 处全被 B1-12/13 覆盖);mark_used(list_new:259→B1-14、listing_sheet:482/490→B1-10,B2-06 改传真码);载荷 :603/:608→B2-04、:1053/:1057→B2-05。

**§3.5 飞书表 6/6 有归属**:LISTING_SHEET V 列→B1-01(+C0-REG-1 重复);ORDER_SALES/RETURNS ASIN→0b-22/23/24(+C0-REG-2 重复);ONLINE_PRODUCTS 来源码→0b-25/26(+C0-REG-3 **批次冲突**);UPC_SHEET E 列口径→B1-16 决策 D3(+C0-REG-4、0a-08 重复);RETIRE_SHEET / MAINT_SHEET 只有 C0-DOC-4⑤ 的文档注记,两条「建议」未进 decisions(见 missing)。

**§3.7 会失效的测试**:全部十四个文件由 C0-TEST-3 统一给了三类处理原则(①加平行用例不删原用例 ②只改被改动的那句 SQL 文本断言 ③夹具 sku=asin 必须改成 sku≠asin),并给了机械检出法。各批次的 items 另有各自的 tests 字段呼应。覆盖完整。

**synthesis required_changes 13 条:11 条完整覆盖,2 条缺半边** —— #1 缺 product_clear.py:20-21 注释与 sku_plan §4 保守策略标注(见 missing);#6 的 alloc_survey._SQL_ONLINE 半条包内自相矛盾、无人落实(见 finding)。#2→0a-02/03+C0-DDL-1~4;#3→0a-07+B2-13/15/16+W3/C2;#4→四份守门文件(**见 blocker**);#5→0a-23;#7→0a-24+B2-11/12/23;#8→B2-02/11/12;#9→B1-01~14+B2-09/10;#10→W1~W6/C1/C2/O1~O8/X1;#11→0a-07(abandon 分流)+B1-22+B2-18~20;#12→四个包的 decisions A;#13→B2 depends_on+risks。

**cases ①~⑧ 的 our_registry / relist 要求 8/8 全覆盖**:①RETIRE 不弃码→B2-21 反向守门 + 0a-23 无 lifecycle 条件 + B2-12 冷却闸;②DELETE→B2-14/15(观测而非回执)+ burned_delete;③清零→守门(maintenance 不得 abandon);④缺席→守门(walmart_catalog.mark_missing 不得 abandon)+ mint 复用;⑤提交失败→码不撤销(B2-03)+5c 弃码(B2-16);⑥SKU_LOCKED→B2-13 + B1-08(不清 V)+ B1-12(退役发真码);⑦SkuUpdate→批次 3 全套;⑧unpublish→不动 + 守门。

## 自查 grep 另外发现(工作包与 sku_plan 都没列)
`services/blacklist.py:205-243` 的 coalesce(asin,sku) 黑名单键推导、`services/alloc_survey._SQL_SALES` 按 (store,sku) 的销量归属在批次 3 改码后断裂、`problem_scan._SQL_INFLIGHT` 的在途防重在改码后失效、`services/variant_group.group_id` 仍把 ASIN 递给沃尔玛(B2 risks 点名但无 item)、`workflows/audit_history_fold.py:71-86` 是第 4 个平台级事件写入点 —— 五条全部进 missing。另核过安全的:risk_trace._SOURCES_SQL(:139-144,已按 source_type='amz' 反查 source_key)、product_pool._SQL_SALES(已按 order_lines.asin 聚合)、upc_pool.claim(键 (store,asin) 不动)、settlement_sync/perf_problems 的 (po,行号)→sku 反查、alloc_plan/alloc_stores 的 _SQL_ONLINE_NOW(纯计数)、dispositions/maintenance 的 (store,sku) 自关联 —— 均为 sku↔sku 或已按 ASIN,不需改。

## 优先处置顺序
先解三条 blocker/major 的**包间协调**问题(索引名与条件三份不一致、四份守门文件、registry 四处重复登记),它们必须在写第一行代码前拍平;再补 missing 里的 M1(product_clear 注释)、M2(blacklist 回填键)、M3(alloc_survey 销量归属)——前两条应并入 0b,第三条并入批次 3。

**blocker**(2):

- [批次 0a / 批次 3 / 0a-02 · S1 · C0-DDL-2] catalog.listing_sources 的「活码部分唯一索引」在三个工作包里有三个不同的索引名和三种不同的局部条件,批次 3 的 DROP 打不中任何一个:0a-02 建 listing_sources_live_key_uk(条件 abandoned_at IS NULL AND source_key IS NOT NULL AND sku~不透明码);横切 C0-DDL-2 建 listing_sources_live_uidx(条件 abandoned_at IS NULL AND source_key IS NOT NULL,包在 DO 块里条件建);批次 3 S1 却写 `DROP INDEX IF EXISTS catalog.listing_sources_active_uidx;` 再按 `WHERE abandoned_at IS NULL AND replaced_by IS NULL` 裸建同名索引。三个名字互不相同 ⇒ S1 的 DROP 是 no-op,旧索引原样留着;而 S1 裸建的新索引没有 C0-DDL-2 的 DO 块守护,存量若有重复活行(match 行同 GTIN 挂多个 PHUMWMT 码是现实可能),db_init 一次 execute 整份回滚。更致命的是:若落地的是 C0-DDL-2 版(不限形态),批次 3 pending 期间「旧 legacy 码行未弃码 + 新不透明码行已 INSERT」两行同 (store, source_type, source_key) 会撞唯一索引 ⇒ C1 mint_replacement 的 INSERT 直接失败,而 S1 本该解除这一条的 tightening 因为 DROP 名字错了没生效。
  - 修法:三个包统一到**一个**索引名与**一份**最终条件,并在批次 0a 一次建到位:CREATE UNIQUE INDEX listing_sources_live_uidx ON catalog.listing_sources (store, source_type, source_key) WHERE abandoned_at IS NULL AND replaced_by IS NULL AND source_key IS NOT NULL AND sku ~ '^[23456789ABCDEFGHJKMNPQRSTVWXYZ]{12}$'(replaced_by 列一并在 0a 加,批次 3 不再动索引)。0a-02 保留、C0-DDL-2 改成「由 0a-02 交付,本包只做体检视图 + DO 块守护」、S1 降级为「核验索引名与条件已含 replaced_by IS NULL,不再 DROP/CREATE」。db_init 幂等连跑两次 + `SELECT indexdef FROM pg_indexes WHERE tablename='listing_sources'` 作为验收硬闸。
  - 证据:0a-02 ddl 段:CREATE UNIQUE INDEX IF NOT EXISTS listing_sources_live_key_uk …;横切 ddl ③:CREATE UNIQUE INDEX IF NOT EXISTS listing_sources_live_uidx …;批次 3 ddl 第一条:DROP INDEX IF EXISTS catalog.listing_sources_active_uidx; CREATE UNIQUE INDEX IF NOT EXISTS listing_sources_active_uidx …。仓内现状 refdata/schema.sql:227-228 只有 listing_sources_key_idx 一条索引,三个名字都不存在。
- [批次 0a / 0b / / 0a-26 · 0b(new_modules) · B2-21 · C0-GUARD-1] 同一批守门断言被拆进四个不同的新建测试文件:tests/test_sku_identity_guard.py(0a-26)、tests/test_sku_asin_consumers.py(0b)、tests/test_sku_codec_guard.py(B2-21)、tests/test_sku_guard.py(横切 C0-GUARD-1)。extract_asin 调用点白名单出现 3 次(0a-26②、0b test_extract_asin_callsite_whitelist、C0-GUARD-2),`abandoned_at IS NULL` 三处白名单出现 4 次(0a-26③、B2-21(3)、C0-GUARD-5、批次 3 guard),`UPDATE catalog.listing_sources` 白名单出现 3 次(0a-26⑤、B2-21(4)、C0-GUARD-6c),不透明码正则 ↔ 字母表一致性出现 2 次(0a-26④、C0-GUARD-1 经 C0-DDL-3)。这本身就是 conventions §六「每个能力只有一条实现路径」要禁的双轨,而且白名单已经开始互相打架:C0-GUARD-2 把 services/audit_rules.py 标成「→0b 收口」,而 0a-18 在 0a 就把它收口了 —— 按 C0-GUARD-2 的白名单执行,0a 合并当天这条守门就是假绿。
  - 修法:定一个文件名(建议 tests/test_sku_guard.py,与仓内既有 tests/test_feishu_guard.py 同族),由批次 0a 建齐五张白名单 + 「白名单不许烂掉」用例;0b/1/2/3 的对应 item 全部改成「只改这一份的白名单条目」,并在每条白名单理由里写死收口批次。0a-26、0b new_modules、B2-21、C0-GUARD-1 四条 item 合成一条,其余三条改为「维护白名单」。
  - 证据:仓内 tests/ 现只有 test_feishu_guard.py 一份守门文件(ls tests/ | grep guard)。0b 的 guard_tests 自己已经写了「若 0a 已建 tests/test_sku_identity_guard.py 承担此职责,则改那一份,不要建第二份」,但横切包又独立提出了第四个文件名 tests/test_sku_guard.py。

**major**(6):

- [批次 0a / 横切 / 0a-04 · C0-DDL-5] refdata/schema.sql:527 的 audit_listing_conflicts 视图被两个包各给了一份**不同的** SQL 规格:0a-04 要求 `LEFT JOIN catalog.listing_sources ls ON ls.store=w.store AND ls.sku=w.sku AND ls.source_type='amz'`,C0-DDL-5 要求 `LEFT JOIN … ON ls.store=w.store AND ls.sku=w.sku`(无 source_type)。两版在存量上确实等价(实测:sources_backfill 对非 ASIN 形态写 source_key=NULL,match 行 source_key=GTIN,两种情形下 p.asin 都命中不了,与今天 p.asin=w.sku 结果相同),所以不是行为差异,但 0a-04 自己配的新测试 `test_conflicts_view_reads_identity_through_the_registry` 明确断言视图文本含 `source_type = 'amz'` —— 若执行者照 C0-DDL-5 落地,这条测试当场红,而两个包都说自己负责这一处。
  - 修法:以 0a-04 为准(带 source_type='amz',与 0a-11..0a-25 其余十四处身份表达式同形),C0-DDL-5 改成「本处 DDL 由 0a-04 交付,横切包只负责 docs/db_schema.md:781-821 的同步说明」;或反过来统一去掉 source_type,但那要同步改 0a-04 的测试断言与另外六处的写法。无论选哪边,`coalesce(ev.asin, ev.sku)` 的 LATERAL 段一个字不许动(与 product_events_identity_idx 逐字一致)。
  - 证据:0a-04 change:「先 LEFT JOIN catalog.listing_sources ls ON ls.store = w.store AND ls.sku = w.sku AND ls.source_type = 'amz'」;C0-DDL-5 change:「LEFT JOIN catalog.listing_sources ls ON ls.store=w.store AND ls.sku=w.sku」。仓内 refdata/schema.sql:527 现为 `JOIN catalog.products p ON p.asin = w.sku AND p.marketplace = 'US'`;services/listing_sources.py:29-42 register 对非 ASIN 形态传 source_key=None(workflows/sources
- [批次 0a / 2 / 0a-19 · B2-24] synthesis required_changes #6 明确要求 alloc_push._SQL_ONLINE **与** alloc_survey._SQL_ONLINE 一起对齐去重闸口径(不再按 lifecycle 排 RETIRED),但工作包对 alloc_survey 给了两条互相矛盾的指令:0a-19 的 change 明说「176-183 的『必须排 RETIRED』注释原样保留」且 SQL 里 `coalesce(upper(w.lifecycle_status),'ACTIVE')='ACTIVE'` 不动;B2-24 的 decisions C default 写「alloc_push._SQL_ONLINE **与 alloc_survey._SQL_ONLINE** 改为…不再按 lifecycle 排 RETIRED」,而同一 item 的 change 正文又写「同步 services/alloc_survey._SQL_ONLINE 与 services/risk_trace.still_listed 的**口径注释**……不要跟着改」。执行者对同一个文件拿到两条相反指令,没有判据可依。
  - 修法:显式拍一边并写进 decisions C:建议保留 alloc_survey 的 lifecycle 条件不动(它是占用/冲突口径,不是派工口径,alloc_survey.py:176-183 的 2026-08-15 结论仍成立),并在 decisions C 里把 required_changes #6 的这半条显式驳回 + 写明理由;若所有者要对齐,则在批次 2 增一条独立 item 改 services/alloc_survey.py:184-190 并给行为差额清单。B2-24 的 change 正文与 decisions C 的措辞必须逐字一致。
  - 证据:仓内 services/alloc_survey.py:184-190 `_SQL_ONLINE` 含 `AND coalesce(upper(lifecycle_status),'ACTIVE')='ACTIVE'`,注释 176-183 写「必须排 RETIRED(2026-08-15 所有者质疑…查证属实)」;workflows/alloc_push.py:49-53 同款条件。synthesis required_changes #6 原文:「workflows/alloc_push._SQL_ONLINE(:46-53)与 services/alloc_survey._SQL_ONLINE(:184-190)的『在架/待派工』定义对齐去重闸口径」。
- [批次 0a / 横切 / 0a-09 · C0-TEST-1] 事件码常量 SKU_ABANDONED / SKU_REPLACED 的登记被排进两个不同批次:0a-09 说在批次 0a 改 services/product_events.py:105-121 的常量区与 EVENTS 集合;横切 C0-TEST-1 说「**批次 2 同批**加两个事件码」并改同一段、同一条既有测试 test_product_events_registry.py::test_constants_match_ledger_strings。若按横切的批次排,0a-07 交付的 sku_codec.abandon 在 0a 就会引用未登记的事件码,而 record_many 对未登记码 fail loud(services/product_events.py:156-159),0a 的 tests/test_sku_codec.py::test_abandon_records_the_ledger_event 必红。
  - 修法:统一到 0a-09(批次 0a 登记,提前登记零副作用:EVENTS 只做入参校验白名单);C0-TEST-1 改写为「核验 0a-09 已登记 + 补 detail 结构约定(abandoned 记 {old_sku, reason, burned_upcs};replaced 记 {old_sku, new_sku, feed_id})」,不再重复改常量区。
  - 证据:0a-09:「新增常量 SKU_ABANDONED…并加入 EVENTS 集合」,acceptance 为 pytest tests/test_product_events_registry.py;C0-TEST-1:「批次 2 同批加两个事件码:常量区加 SKU_ABANDONED…并加进 EVENTS 集合(113-121)」。仓内 services/product_events.py 的 record_many 对未登记码抛错。
- [批次 3 / 横切 / X1 · C0-DDL-6] 「同 (store, po_id, line_number) 多个 order_line_id」的订单双算体检做了两份实现:批次 3 X1 在 services/order_lines.py 新增 duplicate_po_lines(conn, days=120) 函数(带 120 天窗口 + LIMIT 200),横切 C0-DDL-6 在 refdata/schema.sql 建视图 orders.v_order_line_dupes(无时间窗口,带 `WHERE line_number IS NOT NULL`)。同一个能力两条实现路径,口径还不同 —— 两者报出的数字对不上时没有判据说该信哪个,而这条体检正是批次 3 唯一能发现「改码后销量双算」的手段。
  - 修法:二选一并删掉另一条。建议保留视图(C0-DDL-6:catalog_health F 段⑤、W6 摘要、手工 psql 三个消费方共用同一份 SQL),X1 改成薄壳 `SELECT * FROM orders.v_order_line_dupes` 或整条删除、W6 直接查视图;窗口条件统一(建议视图不带窗口,消费方自己加 order_date 过滤)。
  - 证据:X1 change/ddl:`SELECT store, po_id, line_number, count(*) … WHERE order_date > now() - make_interval(days => %s) … HAVING count(*) > 1 … LIMIT 200`;C0-DDL-6 ddl:`CREATE OR REPLACE VIEW orders.v_order_line_dupes AS SELECT … WHERE line_number IS NOT NULL … HAVING count(DISTINCT order_line_id) > 1`。C0-HEALTH-1 的 F 段⑤查的是视图,W6 查的是函数。
- [批次 0b / 1 /  / 0b-25 · C0-REG-3(另:B1-01/C0-REG-1、0b-22/C0-REG-2、0a-08/C0-REG-4)] registry 的四处飞书列登记被两个包重复排 item,其中 ONLINE_PRODUCTS_SHEET 一处还**批次冲突**:0b-25 把「columns 末尾追加 source_key」放在批次 0b,与同批的 0b-26(walmart_catalog._PROJECTION_SQL 加 ls.source_key,共 17 列)锁在一起;横切 C0-REG-3 做同一件事,而横切的 estimated_pr_split 把 C0-REG-3 排进 **PR-1**。若照横切的排法执行,0b 会先落地 17 列的投影 SQL 而 registry 仍是 16 列,既有守门测试 test_catalog_sync.py::test_projection_columns_match_registry 当场红。另外三处(LISTING_SHEET 加 sku:B1-01 vs C0-REG-1;ORDER_SALES/RETURNS 加 asin:0b-22 vs C0-REG-2;upc_pool.STATUS_CN 加 burned_delete/burned_lock:0a-08 vs C0-REG-4b,后者被排进 PR-1 而前者在 0a)同样是一改两处、批次不一致。
  - 修法:每处 registry 改动只留一条 item,并与它的消费方改动锁在同一个 PR:ONLINE_PRODUCTS_SHEET → 0b-25(registry)+0b-26(SQL)+0b-27(docs)同 PR,删 C0-REG-3;LISTING_SHEET → B1-01,删 C0-REG-1;ORDER_SALES/RETURNS → 0b-22,删 C0-REG-2;STATUS_CN → 0a-08,C0-REG-4 只保留 UPC_SHEET E 列口径注释(并与 B1-16 的决策 D3 合并成一条决策)。
  - 证据:0b-25 file=registry/resources.py lines=256-267(实测 ONLINE_PRODUCTS_SHEET 在 256-267);C0-REG-3 file=registry/resources.py lines=252-268,横切 estimated_pr_split 的 PR-1 列表含「C0-REG-3(来源码列)」而 0b-26 在 PR-0b。0a-08 与 C0-REG-4b 都写 STATUS_CN 补两项(实测 services/upc_pool.py:27-28)。
- [批次 0a / 横切 / 0a-02 · C0-DDL-3] 「不透明码全局唯一」索引也是两个名字两套条件:0a-02 的 listing_sources_new_sku_uk 条件为 `sku ~ '^[23456789ABCDEFGHJKMNPQRSTVWXYZ]{12}$' AND sku ~ '[A-Z]'`;C0-DDL-3 的 listing_sources_opaque_sku_uidx 条件只有前半段,**漏了 `AND sku ~ '[A-Z]'`**。两条都写进 schema.sql 的话 db_init 会建出两条近似重复的索引;而守门测试(0a-26④ / C0-GUARD-1 经 test_opaque_sku_regex_matches_the_codec_alphabet)只对其中一条的文本成立。漏掉 `sku ~ '[A-Z]'` 还有实质后果:12 位纯数字的沃尔玛 item id 若恰好不含 0/1 就会落进「不透明码」索引,与 sku_codec.is_opaque「至少含一个字母」的判据不一致。
  - 修法:统一为一条:名字取 listing_sources_opaque_sku_uidx(或 0a-02 的名字,二选一),条件保留两段 `sku ~ '^[23456789ABCDEFGHJKMNPQRSTVWXYZ]{12}$' AND sku ~ '[A-Z]'`,并让守门测试同时断言字符类与 `[A-Z]` 两段;C0-DDL-3 改为「由 0a-02 交付,本包只同步 docs/db_schema.md」。
  - 证据:0a-02 ddl:`… WHERE sku ~ '^[23456789ABCDEFGHJKMNPQRSTVWXYZ]{12}$' AND sku ~ '[A-Z]';` 并注明「末尾 sku ~ '[A-Z]' 与 sku_codec.is_opaque 的『至少一个字母』同口径,防 12 位纯数字 item id 混进来」;横切 ddl ④:`… WHERE sku ~ '^[23456789ABCDEFGHJKMNPQRSTVWXYZ]{12}$';`(无第二段)。

**minor**(5):

- [批次 0a / 2 /  / 0a-26③ · B2-21(3) · C0-GUARD-5 · 批次3 guard_tests] `abandoned_at IS NULL` 允许出现的位置在四个包里是三种口径:0a-26③「只允许 sku_codec/list_new/alloc_push 三个 .py,schema.sql 与 tests 除外」;B2-21(3)「三处」;C0-GUARD-5「四处,含 refdata/schema.sql 的索引条件」;批次 3 的 test_abandoned_at_is_null_appears_in_exactly_four_places「三处 .py + sku_migrate._SQL_CANDIDATES」。四份守门若都落地会互相判红。另外 0a-25 的 _FAMILY_LISTED_SQL 按 required_changes #5「同口径经登记簿反查」应否加 abandoned_at 没有结论(0a-25 未加,也没写为什么不加)。
  - 修法:在 docs/conventions.md §九(C0-DOC-6)写死一句权威口径:「消费方 .py 三处(sku_codec.mint / list_new 去重闸 / alloc_push._SQL_ONLINE),批次 3 起加 sku_migrate._SQL_CANDIDATES 为第四处;refdata/schema.sql 的部分索引条件是 DDL 不是消费方过滤,不计入」;四条守门 item 全部改成引用这一句,断言写在唯一那份守门文件里。0a-25 补一行注释说明 _FAMILY_LISTED_SQL 为何不加(变体同族查的是在架事实,与码是否弃用无关)。
  - 证据:0a-26 change ③:「全仓 *.py 里出现 'abandoned_at' 的文件只允许 services/sku_codec.py、workflows/list_new.py、workflows/alloc_push.py(schema.sql 与 tests 除外)」;C0-GUARD-5:「文件必须在四元素集合里:…refdata/schema.sql(listing_sources_live_uidx 的部分索引条件)」;批次 3 guard_tests:「批次 2 定的三处…加本批次的 _SQL_CANDIDATES,共四处」。
- [批次 0a / 0b / 0a-24] 0a-24 的 _SQL_ATTEMPTS 代际过滤用 `coalesce(e.asin, e.sku) = t.asin` 去认 sku_abandoned 事件,但 catalog.product_events.asin 要到 0b-11 才改成经登记簿反查(0a 阶段 record_many 仍走 services/product_events.py:167 的 extract_asin)。在 0a 已合并、0b 尚未合并的窗口里,abandon 写出的 sku_abandoned 事件(sku 是不透明码)asin 恒为 NULL ⇒ coalesce 回落成不透明码 ⇒ 代际过滤永不命中,静默退化成跨码累计。0a 的 depends_on 只写「无代码依赖:0a 是第一个批次,可直接在 main 上开分支」,没写这条对 0b-11 的时序依赖,而 0a 的 PR 拆分明确允许 0a 单独先合并上生产。
  - 修法:两选一:① 在 0a-24 的注释与 0a 的 depends_on 里写明「代际过滤的正确性依赖 0b-11(product_events.asin 经登记簿反查),0b 未合并前它恒退化成跨码累计 —— 这是 fail-safe 方向,可接受」;② 更稳:让 sku_codec.abandon 在 record_many 的 detail 里显式带 source_key,_SQL_ATTEMPTS 改读 `e.detail->>'source_key'`,不依赖 record_many 的自动填充。
  - 证据:0a-24 SQL:`LEFT JOIN LATERAL (SELECT max(occurred_at) … WHERE e.store = t.store AND coalesce(e.asin, e.sku) = t.asin AND e.event = 'sku_abandoned')`;仓内 services/product_events.py:167 现为 `extract_asin(r["sku"])`,0b-11 才改。
- [批次 0a / 0a-23] 0a-23 给去重闸的 LEFT JOIN 加了 `AND ls.source_type = 'amz'`,而 synthesis rule 4 与 required_changes #5 给的写法是不带 source_type 的 LEFT JOIN。加了之后,非 amz 行的 ls.abandoned_at 因 JOIN 未命中恒为 NULL ⇒ **已弃码的 match 行永远拦着新码**,与 §5.3「已弃码的僵尸行不再挡新码」的字面契约不符。对 list_new(只处理 amz 行)无实害,但 0a-23 的三条新测试都没覆盖这种行,契约与实现的偏差没有任何东西钉住。
  - 修法:要么去掉 source_type 条件与 synthesis 对齐;要么在 _SQL_LISTED_ASINS 注释里写死「本闸只按 amz 身份键去重,match 行的码寿命由 match_listing 自己的通道管」,并加一条测试 test_dedup_gate_ignores_non_amz_registry_rows 把这个有意的偏差钉住。
  - 证据:0a-23 change:「LEFT JOIN catalog.listing_sources ls ON ls.store = w.store AND ls.sku = w.sku AND ls.source_type = 'amz' WHERE w.missing_since IS NULL AND ls.abandoned_at IS NULL」;synthesis rule 4:「改为 walmart_items w LEFT JOIN listing_sources ls ON (w.store, w.sku) WHERE w.missing_since IS NULL AND ls.abandoned_at IS NULL(LEFT JOIN:未登记行也拦)」。
- [批次 3 / 横切 / R4 · R2⑤] sku_plan §7 批次 3 白纸黑字写着 `ops.cleanup_seen_categories`「按 replaced_by 迁一次」,工作包 decision G 把它改判成「不迁」(有实证:全仓只有 workflows/cleanup_history_import.py 写它、零读者),但负责改写 sku_plan §7 的 R4 没有把这条结论写进去 —— 结论只落在 docs/db_schema.md(R2⑤)。sku_plan 是这次改造的总账,留着与实现相反的旧稿,下一轮盘点会有人照旧稿去写迁移。同类问题:sku_plan 批次 3 列的 `maintenance` drop_recent 防重键(plan 判「自然过期」)在整个工作包里一次都没出现,批次 3 也没有对应的核验动作。
  - 修法:R4 的 change 补两句:①「ops.cleanup_seen_categories 不迁(2026-09-02 复核零读者;迁数据会让 (sku, category) 唯一对的累计计数多算),依据见 docs/db_schema.md:684-691」;②「maintenance drop_recent 防重键随改码自然过期,批次 3 验收时确认其 TTL 窗口短于一轮观测期」。批次 3 的 acceptance_commands 加一条 drop_recent 窗口核验。
  - 证据:docs/sku_plan.md §7 批次 3:「受牵连的 (store, sku) 键表:ops.dispositions 在途行…、maintenance 的 drop_recent 防重键(自然过期)、ops.cleanup_seen_categories(按 replaced_by 迁一次)…」;批次 3 decisions G default「不迁」;R4 change 只列了「三态判据表 / 四条抑制型改动清单 / 节奏硬闸 / 决策 A/B/C / 四个来源字母 / 试点七步」。
- [全部批次 / B1-01 · C0-SCHED-1 · B1-16 · 0a-24] 工作包自称「每条改动的行号都逐个打开文件核对过」,但抽查有多处漂移,执行者若照写 sed/patch 会打错位置:B1-01 说 LISTING_SHEET columns 元组在 518-523、`"upc_match"),` 在 523 —— 实测定义在 514-524,`"upc_match"),` 在 524;C0-SCHED-1 说 registry/schedule.py 的 product_chain job 在 191-207 —— 实测 `job("product_chain",` 在 167,note 参数在 174-183;B1-16/C0-REG-4 说 UPC_SHEET 在 935-943 / 936-943 —— 实测 937-944;0a-24 说 _retry_rows 在 684-696 —— 实测 672-701(cur.execute 在 690、阈值比较在 695)。
  - 修法:执行前对每条 item 重新用锚点字符串定位(grep -n),不照抄行号;更稳妥的做法是把 items 的 lines 字段改成「锚点字符串 + 函数/常量名」,行号只作参考。至少把上述四处修正,并在每个包的 risks 里保留「行号可能已漂,动手前再 grep 一次」这条(批次 1 的 risks 最后一条已有,其余三个包没有)。
  - 证据:实测 grep -n:registry/resources.py:514 `LISTING_SHEET = Spreadsheet(`、:524 `"upc_match"),`;registry/resources.py:937 `UPC_SHEET = Spreadsheet(`;registry/schedule.py:167 `job("product_chain",`;workflows/list_new.py:672 `def _retry_rows(`、:690 `cur.execute(_SQL_ATTEMPTS`、:695 阈值比较。

**遗漏项**:
- required_changes #1 的 `workflows/product_clear.py:20-21` 注释更正 —— 无任何 item 承载。实测该注释在 workflows/product_clear.py:20-21:「动作映射(2026-08-06 所有者定稿):停用/下架 → RETIRE_ITEM(可恢复)」。synthesis 要求加注「现行 problem_scan 策略下可恢复窗口 ≈ 下一轮扫描之前」,而 problem_scan._SQL_ITEMS(实测 :77-83)的扫描面是「published_status 非 PUBLISHED 且 missing_since IS NULL」、无 lifecycle 豁免 ⇒ 停用品一到两轮就被自动链 DELETE。工作包里它只出现在 B2 decisions A 的 **alternative** 分支(「若所有者选简化版…措辞必须同改」)与横切 risks 的最后一条(risks 不是 item,没有 file/lines/tests/acceptance)。而决策 A 取**默认值**(RETIRE 不弃码、不加豁免)时这条注释照样必须改 —— 默认值恰恰是「可恢复只是个窗口」的那种情况。建议:批次 2 或横切 PR-C1 增一条 item,file=workflows/product_clear.py, lines=20-21,tests=无(纯注释),acceptance=grep。
- `services/blacklist.py:205-243` 的黑名单键推导(`_LATEST_CTE` / `_BACKFILL_ASIN_SQL` / `rebuild_asin_blacklist`)—— sku_plan §3.3 漏列、四个工作包全都没覆盖。实测 :209 `SELECT DISTINCT ON (coalesce(asin, sku)) coalesce(asin, sku) AS asin, …` 从 catalog.product_events 取 problem_categorized 的最新类别,:271-280 的 `_BACKFILL_ASIN_SQL` 把它 INSERT 进 catalog.asin_blacklist(asin 是唯一键),`rebuild_asin_blacklist`(:260-268)先 `DELETE FROM catalog.asin_blacklist` 再整表重灌。切码后凡是 product_events.asin 为空的行(0b-11 落地前写入的行、或登记簿反查不到的行),coalesce 会回落成 12 位不透明码 ⇒ 随机码被写成黑名单键,与 §3.3 点名的三大最危险失效之一「黑名单键被灌随机码 ⇒ list_new 的黑名单闸拦不住违禁品」完全同形,而且 rebuild 路径是一次性把好键换成坏键。建议 0b 增一条 item:_LATEST_CTE 改为经登记簿取键(LEFT JOIN listing_sources ls ON ls.store=e.store AND ls.sku=e.sku AND ls.source_type='amz',键取 coalesce(ls.source_key, e.asin, e.sku)),并加一道「不透明码不得入键」的硬闸 + 计数告警;同时把这一处补进 sku_plan §3.3 表。
- 批次 3 改码后 `services/alloc_survey._SQL_SALES`(实测 :236-245)与它的三个消费点(:515 `for (store, _sku), (o, g) in sales.items()`、:523 与 :652 `sales.get((r["store"], r["sku"]))`)按 (store, sku) 把历史订单销量挂到在架行上。改码 confirmed 之后 catalog.walmart_items.sku 变成新码,而 orders.order_lines 的历史行 sku 仍是旧码 ⇒ **迁过码的品销量/GMV 恒 0**。后果:resolve_conflicts 按销量选冲突赢家、store_metrics 的店铺产出、alloc_audit 的明细全部对迁移过的品失真,而且不报错。sku_plan §7 批次 3 的「受牵连的 (store, sku) 键表」清单没列 alloc_survey,四个工作包也没有任何 item 覆盖。对照组:services/product_pool.py:49-58 已经是按 orders.order_lines.asin 聚合的,不受影响 —— 说明正确写法仓内已有先例。建议批次 3 增一条 item:_SQL_SALES 改按 asin 聚合(0b-10 已保证写侧填得出 asin),或在 sales 查询里经 catalog.sku_aliases 视图把旧码映射到新码(与 O5 同一条继承链)。
- `workflows/problem_scan._SQL_INFLIGHT`(实测 :104-114)按 (store, sku) 把 ops.feed_items 关到 catalog.walmart_items 做在途防重;改码 confirmed 之后新码行在 feed_items 里没有任何历史,旧码上在途的 MP_ITEM / MP_MAINTENANCE feed 就拦不住新码行了(该 SQL 的在途口径**有意不分 feed 类型**,注释里写明「上架 feed 在途也算在途…复审期内追发 DELETE_ITEM 属于过早」)。批次 3 的 W2 前置闸只挡三样:dispositions executing、retire_cooldown pending、sku_migrate 自己的 pending feed,不挡上架/维护在途。sku_plan 把 ops.feed_items 归类为「历史,不动」,漏掉了它同时是在途防重的数据面。建议 W2 的 _preflight 增第 6 道闸:该 (店, 旧码) 在 ops.feed_items 里无 `status='submitted' AND submitted_at > now() - interval '48 hours'` 的行,口径与 problem_scan._SQL_INFLIGHT 同源。
- required_changes #1 的另半条:sku_plan §4「退役后旧 UPC 永久烧号」与「24h 冷却」要在文档里标注为**本仓保守策略 / 旧仓实证、非官方规则**。只在代码注释里落地了(B2-02 的 RETIRE_COOLDOWN_HOURS 行内注释「官方无明文,按旧仓实证 legacy_survey.md:1649 保留」),docs/sku_plan.md §4 的文本更正没有任何 item 承载 —— R4 的 change 只写了「§4 实测清单从三件扩到六件」,C0-DOC-5 只管 docs/api_blueprint.md。建议 R4 或 C0-DOC-5 补这一处 sku_plan §4 的文本更正。
- sku_plan §3.5 的两条「建议」与 §8 对应的两条待决项没有进入任何工作包的 decisions:①退役表 RETIRE_SHEET B 列(运营从此手填随机码)是否要程序回显来源码;②维护记录表 MAINT_SHEET 是否加来源码展示列。工作包只在 C0-DOC-4⑤ 写了一句文档注记(「SKU 列从此可能是随机码,查产品先经登记簿或来源码列」),两条待决项就此从待办里掉出去,没有默认假设、没有 alternatives、没有 impact。建议收进横切 decisions(建议默认「不回显,靠在线产品总表的来源码列反查」),并在 R4 改写 sku_plan §8 时保留勾选状态。
- `services/variant_group.group_id`(实测 :125-155,由 parent_asin 或 min(家族 ASIN) 派生)派生出的 variantGroupId 经 mp_conform 写进 Visible.variantGroupId 发给沃尔玛 —— 换成不透明 SKU 之后,变体品的货源仍可从变体组 ID 直接倒推 ASIN,与本次改造的核心目标(货源隐匿)直接冲突。批次 2 的 risks 第一条已经点名了这件事并说「本批不改,应作为独立议题交所有者定」,但**没有任何 item、没有 decision 编号、没有进 sku_plan §8 的待决清单** —— 一条已识别的目标级漏洞只活在 risks 文本里,交付后没人会回来处理它。建议:在横切 decisions 里立一条(建议编号 D11)写清默认假设(暂不改,单品口径反而变好:占位组 ID 从 ASIN 变成不透明码)与代价,并由 R4 写进 sku_plan §8 待决清单。
- `workflows/audit_history_fold.py:71-86` 直接 `INSERT INTO catalog.product_events (sku, asin, store, …) SELECT asin, asin, NULL, …`,绕过 services/product_events.record_many —— 这是第 4 个「平台级事件(store 为空、sku=asin)」写入点,而 0b-11 的注释与守门白名单只列了三个(product_ingest / product_audit / cleanup_history_import)。切码后无实害(审核域产品本来就没有沃尔玛 SKU),但:①它绕过 record_many 的事件码校验,守门测试 test_extract_asin_callsite_whitelist / test_record_many_falls_back_to_shape_for_platform_events 的口径说明会与事实不符;②文档(0b-28 要改的 docs/db_schema.md:287-291 product_events.asin 列注释)写「record_many 补填」是不完整的。建议:0b-11 的注释与 0b-28 的文档改动把 audit_history_fold 算进平台级来源,守门白名单显式登记它(理由:直接 INSERT、asin 列直填、无 SKU 语义)。

### A.2 铁律/红线

审了五个工作包(0a / 0b / 1 / 2 / 3 / 横切)共 100+ 条 item,逐条对照 CLAUDE.md 三条铁律与安全红线、conventions §一/§三/§五/§六/§八,并把工作包里的行号与断言逐个回仓核对。\n\n**没问题的部分先说清**:api 层零违规——0a/0b/1/2 全不碰 api/,批次 3 的 M4 与横切 C0-REG-6 只加注释与测试,SkuUpdate 骑既有 MP_MAINTENANCE / MP_ITEM 桶、不自创 feedType,业务判断全在 services/workflows;去重闸口径正确——0a-23 明确「不加 lifecycle 条件」并配了 `test_dedup_gate_has_no_lifecycle_condition` 反向守门,`_FAMILY_LISTED_SQL` 同口径;弃码点确实是四处(catalog_sync / sku_locked_heal / listing_sheet / sku_migrate)且有反向清单钉死 product_clear 等五处不得调;弃码点 1 与 verify_deletions 同事务(核过 workflows/catalog_sync.py:230 的 with 块),弃码点 2 在 `_relist` 的 execute 早返回之后(services 侧安全);批次 2 的 mint 放在 `_prep_rows`,而 `_prep_rows` 在 list_new.py:1583 的 `if not execute: return` 之后才被调到,dry-run 不会 mint;写操作无自动兜底(批次 3 rollback 明确「不做任何补交」);「判不准就判活」在批次 3 三态机里落实得不错(stalled 不自动定案)。\n\n**20 条 finding,5 条 blocker**:① 活码唯一索引在三个包里三个名字三种条件,批次 3 的 DROP 打空、CREATE 无守护,会让 db_init 整份回滚;② 批次 2 把 abandon 放进 `sync_from_ledger` 这条 --dry-run 管不住的路径(feed_poll DANGEROUS=False ⇒ cli 强制 execute=True),空跑会真烧 UPC;③ 批次 3 `_settle` 没有 dry-run 分支,却是全包写得最重的一段,规格与自己的验收命令打架;④ 批次 3 提交 feed 的事务边界没定,按字面实现是在未提交事务里 POST,违反「防重状态先落库再调接口」;⑤ 不透明码字母表被登记四份、形态判据两份,两条守门测试互斥。\n\n主线问题是**跨包一致性**:守门测试四个家、`abandoned_at IS NULL` 白名单三种数目、mint 签名三种写法、DDL/docs 所有权重复、write_submit_cols 第 9 值批次 1 定可选批次 2 改必填。这些不是单包内的设计缺陷,而是五个包各自成文、没人做过合并态推演的产物——建议在开工前先把横切包升格为 DDL / registry / docs / 守门的唯一出处,其余四包对应 item 一律降级成「核验在位」,再逐包重跑一遍行号。另有一条**流程性 major**:批次 2 是唯一有行为变化的批次,却把 1→10→整店 的节奏交给人手工删飞书表行(验收命令里的 `-p limit=` 是个不存在的功能),而同一份工作包的批次 3 已经把同样的节奏写成了 `_stage_cap` 代码闸——批次 2 应照抄。

**blocker**(5):

- [批次 3 / S1 + ddl 第 1 条] 活码部分唯一索引在三个工作包里有三个名字、三种条件,批次 3 的 DROP 打空、CREATE 无守护。批次 0a-02 建 `listing_sources_live_key_uk`(条件含「不透明码形态」);横切 C0-DDL-2 建 `listing_sources_live_uidx`(用 DO 块条件建);批次 3 S1 却 `DROP INDEX IF EXISTS catalog.listing_sources_active_uidx` 再无条件 `CREATE UNIQUE INDEX ... listing_sources_active_uidx ... WHERE abandoned_at IS NULL AND replaced_by IS NULL`。后果两条:① DROP 不命中任何真实存在的索引,0a/横切 建的那条仍在,批次 3 声称的「收紧」根本没发生;② 新建的无条件唯一索引会在存量重复活行(match 行同 GTIN 挂过多个 PHUMWMT 码)上建失败,而 db_init 是整份 schema.sql 一次 execute，一条失败全份回滚 ⇒ 生产建库/改表直接停摆。
  - 修法:索引名与条件收成唯一出处(建议横切包)：名字定死一个(如 listing_sources_live_uidx)，条件定死一套；批次 3 的 ddl 改为对该名字做 DROP/CREATE，并沿用横切 C0-DDL-2 的 DO 块条件建 + `catalog.v_listing_sources_dupe_live` 体检，绝不无条件 CREATE UNIQUE。三个包的 ddl 段落改为互相引用而非各写一份。
  - 证据:工作包：批次3 S1 与 ddl 第 1 条；批次0a items 0a-02；横切 C0-DDL-2/D1。仓内现状：refdata/schema.sql:213-227 只有 listing_sources_key_idx，无 *_active_uidx / *_live_uidx / *_live_key_uk。规则出处：CLAUDE.md 常用命令「db_init 幂等建表」；conventions §五「DROP 未连库核对一律不执行」；0a-02 自述「db_init 一条索引建失败会整份回滚」。
- [批次 2 / B2-16] 把不可逆的 abandon(弃码 + 永久烧 UPC)放进了一条 --dry-run 管不住的路径。`_mark_upc_conflicts` 由 `services/listing_sheet.sync_from_ledger()` 调用，该函数没有 execute/dry_run 参数、恒写；它的宿主 `workflows/feed_poll.py:38 DANGEROUS = False`，而 cli.py:307 对非危险工作流强制 `params["execute"] = True`，只把 `dry_run` 透传下去(cli.py:311)——sync_from_ledger 根本不读它。于是 `python cli.py feed_poll --dry-run` 会真的弃码并把 UPC 永久烧掉，且 feed_poll 在 product_chain 里每轮自动跑。
  - 修法:`_mark_upc_conflicts(pairs, execute=True)` 与 `sync_from_ledger(execute=True)` 都加 execute 形参，feed_poll 按 cli 传下来的 `params["execute"]`/`params["dry_run"]` 传递，execute=False 时只 logger.info 计划不落任何写；同批重新评估 feed_poll 的 DANGEROUS(它从此会做不可逆业务动作)。测试钉：`feed_poll --dry-run` 全程 abandon/burn 零调用。
  - 证据:工作包：批次2 B2-16(未提任何 execute/dry-run 分支)。仓内现状：services/listing_sheet.py:516 `def sync_from_ledger() -> str | None`、:557 `n_conflict = _mark_upc_conflicts(conflicts)`；workflows/feed_poll.py:38 `DANGEROUS = False`；cli.py:304-311。规则出处：CLAUDE.md 安全红线「缺省即真跑；空跑用 --dry-run；AI 改完代码先 dry-run、人眼确认后才跑真的」；conventions §一。
- [批次 3 / W3(与 W6 执行序、guard_tests 冲突)] `_settle` 没有 dry-run 分支，却是全包写得最重的一段：同事务做 `settle_replacement`(abandon)、`upc_pool.retag_sku`、`UPDATE ops.dispositions`、`DELETE catalog.item_node_inventory`、`UPDATE listing.sku_migrations`，事务外还写飞书 V 列。W6 的执行序把 `_settle` 排在 `_preflight` 之后第一个跑，W4 明确写了 dry-run 分支而 W3 一个字没写；W3 自己的验收命令还是 `sku_migrate -p store=X -p settle_only=1 --dry-run`，guard_tests 里也挂着 `test_dry_run_touches_neither_db_nor_walmart`——规格与验收互相打架，执行者必须自己补设计决定。
  - 修法:W3 改签名 `_settle(conn, store, rows, execute) -> (计数, 摘要行)`，execute=False 时只算判决、只打印，abandon/retag/dispositions/node/ledger/飞书六处写全部跳过；在 item 正文里逐条写死「dry-run 下这六处不发一条 SQL」，并把 guard test 落到 W3 名下。
  - 证据:工作包：批次3 W3(全文无 execute 判断)对照 W4(「dry-run:不 mint、不写 ledger、不提交」)与 W6 执行序、guard_tests `test_dry_run_touches_neither_db_nor_walmart`。规则出处：CLAUDE.md 安全红线「空跑用 --dry-run」；批次3 W1 头注自述「危险:缺省即真跑，空跑用 --dry-run」。
- [批次 3 / W4] 提交 feed 的事务边界没定，「防重状态先落库再调接口」在最坏路径上不成立。W4 写的是「① mint_replacement + INSERT sku_migrations(同事务，feed_id 留空)；② 组载荷；③ feeds.submit_feed」，全程没说 commit 在 ③ 之前。registry/db.py:19-30 的 `pg_conn` 默认是「正常退出才 commit」，所以按字面实现就是在一个未提交事务里 POST。进程若死在 POST 之后、外层 with 退出之前，新码行与 pending 台账全部 rollback，而沃尔玛那边 SkuUpdate 已经受理——登记簿说旧码、沃尔玛说新码，新码成了没有出身的孤儿行(正是 W4 决策 F 自己论证要避免的形态)。
  - 修法:在 W4 正文写死：mint_replacement + ledger INSERT 的 `with db.pg_conn()` 必须在组载荷之前退出并 commit，submit_feed 一律在事务之外调用；落 feed_id 另开一次短事务。照抄批次 2 B2-18 已经写对的表述(「该 with 在循环结束…退出并 commit…仍然在 feeds.submit_feed 之前」)。加测试：桩记录调用序，断言 commit 早于第一次 submit_feed。
  - 证据:工作包：批次3 W4 步骤 ①②③；对照批次2 B2-18 的正确表述。仓内现状：registry/db.py:19-40 `pg_conn` 退出才 commit。规则出处：CLAUDE.md 安全红线「防重状态先落库再调接口:feed 先写 pending 后提交」；conventions §一。
- [批次 0a / 0b / / 0a-01 + 0b-03 + 0a-07 + 横切 D4 + 批次3 _SQL_CANDIDATES] 12 位不透明码的字母表被登记了四份、形态判据两份，两条守门测试互斥。0a-01 把 `SKU_ALPHABET` 放 registry；0b-03 又在 services/sku_asin 建 `OPAQUE_ALPHABET`/`OPAQUE_LEN`/`is_opaque_form`;0a-07 在 sku_codec 建 `is_opaque`;横切 D4 明确裁决「字母表落 services/sku_codec，registry 只放 SKU_SOURCE_LETTERS」;批次 3 `_SQL_CANDIDATES` 再抄第四份正则。守门层面直接对撞:0a-26④ 断言 schema.sql 字符类 == `registry.resources.SKU_ALPHABET`,横切的 `test_opaque_sku_regex_matches_the_codec_alphabet` 断言 == `sku_codec._ALPHABET`——两条不可能同时绿。
  - 修法:按横切 D4 定唯一出处 services/sku_codec(`_ALPHABET` / `_LEN` / `is_opaque`);删掉 0a-01 的 SKU_ALPHABET/SKU_LEN 与 0b-03 的 OPAQUE_* 与 is_opaque_form(sources_backfill 直接调 sku_codec.is_opaque,services→services 合法);registry 只留 SKU_SOURCE_LETTERS;schema.sql 与 _SQL_CANDIDATES 的正则各由同一条守门对齐到那一份常量。
  - 证据:工作包:0a-01、0a-07、0a-26④、0b-03、横切 D4 与 C0-GUARD(test_opaque_sku_regex_matches_the_codec_alphabet)、批次3 ddl `_SQL_CANDIDATES`。规则出处:CLAUDE.md「每个能力只有一条实现路径(双轨禁止)」;conventions §六;0b-03 自述「字母表一旦有两份,剔掉 0/O/1/I/L/U 这条纪律漏改一处就静默失效」。

**major**(10):

- [批次 0a / 0b / / 0a-26 + 0b new_modules + B2-21 + 横切 C0-GUARD-1] 同一批守门规则被拆进四个测试文件,白名单口径互不相同:tests/test_sku_identity_guard.py(0a)、tests/test_sku_asin_consumers.py(0b)、tests/test_sku_codec_guard.py(批次 2)、tests/test_sku_guard.py(横切),批次 3 又把 abandon 守门塞进 tests/test_sku_migrate.py。同一条 `extract_asin 调用点白名单` 至少出现在三份里,取值各不相同;0b 自己在 depends_on 里写了「不许再建第二份」却仍在 new_modules 建了一份。
  - 修法:定 tests/test_sku_guard.py(横切包)为守门唯一之家,其余包的守门 item 一律降级为「在该文件里增删白名单条目」;批次 3 的 abandon 守门也搬进去,tests/test_sku_migrate.py 只留行为测试。
  - 证据:工作包:0a items 0a-26;0b new_modules(tests/test_sku_asin_consumers.py)与其 depends_on 第 2 条;批次2 B2-21;横切 C0-GUARD-1~7;批次3 guard_tests 前三条。规则出处:conventions §六「每个能力只有一条实现路径」。
- [批次 0a / 2 /  / 0a-26③ + B2-21③ + 横切 C0-GUARD-5 + 批次3 _SQL_CANDIDATES] `abandoned_at IS NULL` 的白名单三包不一致,批次 3 又静默加了一处。synthesis 的消费方契约是「只允许三处」(sku_codec.mint / list_new 去重闸 / alloc_push._SQL_ONLINE);0a-26③ 按三处(schema 与 tests 除外);横切 C0-GUARD-5 按四元素(含 refdata/schema.sql);批次 3 的 `_SQL_CANDIDATES` 里带了这个谓词,其守门却自称「共四处、.py 里第五处即红」。哪一份先合,另一份合并当天就红,而最省事的修法是有人去注释掉断言——那正是这条守门要防的事。
  - 修法:在 docs/conventions.md §九 把口径一次写死:消费方 SQL 三处 + DDL 局部索引条件(schema.sql)+ sku_migrate 候选选取,共四类,并逐类写清为什么它不是「消费方过滤」;唯一那份守门测试的白名单逐条对应这份清单,批次 3 只做「往白名单加一行 workflows/sku_migrate.py 并写明理由」。
  - 证据:工作包:0a-26③、批次2 B2-21③、横切 C0-GUARD-5、批次3 ddl `_SQL_CANDIDATES` 与其 guard_tests。规则出处:synthesis rule §6「全仓 SQL 文本里 abandoned_at IS NULL 只允许出现在…三处」;sku_plan §5.3 消费方契约。
- [批次 0a / 2 /  / 0a-07 + B2-02 + B2-03 + B2-18 + 批次3 C1] mint 的签名与 dry-run 形态三包互斥,按字面实现必 TypeError。0a-07 定 `mint(conn, store, source_type, source_key, workflow, *, dry_run=False)`,workflow 是必填位置参数、且自带 dry_run 模式;B2-02 反过来裁决「placeholder 单独成函数,写库函数不该有『这次不写』的模式」却没说删掉 0a 的 dry_run kwarg(于是两条 dry-run 路径并存);B2-03 与 B2-18 的调用都只传四个位置参数、漏了 workflow。占位码同样两份:registry `SKU_DRYRUN_PLACEHOLDER="DRYRUN000000"` vs `sku_codec.placeholder()` 返回 letter+"DRYRUN00000"。
  - 修法:三包同步定稿一份签名(建议 `mint(conn, store, source_type, source_key, *, workflow)`,无 dry_run kwarg)与一份占位码来源(`sku_codec.placeholder(source_type)`,删 registry 常量);0a-07 与 B2-03/B2-18 的调用示例逐字改齐;批次3 C1 的 mint_replacement 跟着对齐。
  - 证据:工作包:0a items 0a-07 与 new_modules(services/sku_codec.py 的 api 段)、批次2 B2-02/B2-03/B2-18、批次3 C1。规则出处:conventions §六「双轨过渡遗留…新系统一律禁止,每个能力只有一条实现路径」。
- [批次 2 / acceptance_commands 第 7 条(试点第 2 步)] 唯一有行为变化的批次,却把节奏交给人手工删飞书表行,没有任何代码闸。验收原文是「上架表只留一行待上,真跑 1 个品(list_new 无 limit 参数,要么加 -p limit=,要么手工只留一行)」——`-p limit=` 是一个 items 里根本不存在的未定义工作。而 list_new 缺省即真跑、一跑就是全店 ready 行全部 mint + 提交 MP_ITEM,码已发、UPC 已 used,不可逆。同一份工作包的批次 3 W5 恰恰用 `_stage_cap` 把 1→10→整店 写成了代码闸,理由写的是「纪律没有默认值替你挡」。
  - 修法:批次 2 增一个 item:list_new 加首轮上限(`-p limit=` 或「该店尚无成功新码时本轮至多 N 行」的硬闸,照 W5 的形状),给出函数签名、摘要文案、测试名与验收命令;不要把它留在验收命令的括号里当口头约定。
  - 证据:工作包:批次2 acceptance_commands 第 7 条与 items(无 limit 相关条目);对照批次3 W5。规则出处:CLAUDE.md 安全红线「缺省即真跑…这条纪律,没有默认值替你挡」;conventions §一。
- [批次 1 / 2 / B1-07 与 B2-10] `write_submit_cols` 的第 9 值在批次 1 定为可选、在批次 2 改成必填,直接打红批次 1 立的守门测试。B1-07 写 `if len(vals) > 8 and vals[8]` 并把 `test_write_submit_cols_eight_values_is_byte_identical` 列进 guard_tests(自述是「批次 2 接线前的护栏」);B2-10 改成 `title, rest, sku = vals[0], vals[1:8], vals[8]`——八值调用当场 IndexError,那条守门必红,而批次 2 的 items 里没有任何一条说要删它或改它。
  - 修法:批次 2 保留 B1-07 的可选写法(第 9 值缺省或为空即不写 V 段),B2-09 三分支照旧传 9 值即可;若确实要改成必填,批次 2 必须显式列一条 item:删除/改写 B1 那条守门,并写清为什么护栏可以撤。
  - 证据:工作包:批次1 B1-07(change 第 ③ 点、guard_tests 末条)与批次2 B2-10(函数体改写)、B2-09。仓内现状:services/listing_sheet.py:147 `title, rest = vals[0], vals[1:]`。规则出处:CLAUDE.md「每个能力只有一条实现路径」;批次 1 自称零行为变化的护栏不能被下一批默默打掉。
- [批次 0b / 横切 / 0b-25 与 横切 C0-REG-3] 在线产品总表新列的取值两包冲突,而这个元组会被整行当飞书表头写出去。0b-25 要求 `columns` 末尾追加 `"来源码"` 并配测试 `columns[-1] == "来源码"`;横切 C0-REG-3 要求追加 `"source_key"`。catalog_sync.py:274 是 `rows = [list(sheet.columns)] + data_rows`——元组元素就是写进飞书的表头文字,现有 16 项全是英文字段名(store/sku/itemId/…)。两包哪一份后合,另一份的断言与飞书表头都错。
  - 修法:二选一并写进 docs/feishu_tables.md;建议取 `source_key`(与既有英文表头同风格、也与 0b-26 的 SQL 列名一致),中文表头若要另议就单独提。同时补一句注释:registry 该元组注释写的「列序 = catalog.walmart_items 的字段序」从此不再成立(尾部多一个投影列)。
  - 证据:工作包:0b items 0b-25(含其 tests 与 D-0b-5)、横切 C0-REG-3。仓内现状:registry/resources.py:256-266;workflows/catalog_sync.py:274。规则出处:CLAUDE.md「飞书字段名只准引用 registry 字段常量」与「动了表同步文档」。
- [批次 3 / W3(confirmed 分支)+ ddl 第 11 条] 改码落定时用裸 SQL 改处置表的身份键,绕过处置状态机;同一事务还裸 DELETE 节点库存表。`UPDATE ops.dispositions SET sku=%(new_sku)s WHERE store=… AND sku=… AND status='suggested'` 直接改 (store, sku, action) 这个业务键——而 schema.sql:1313 上有 `dispositions_open_uidx (store, sku, action)` 局部唯一索引,撞上就抛,W3 没有分支;`asin` 列也没跟着更新。同一包已经为读侧新建了 `services/dispositions.open_executing_count`,却把写侧留在工作流里裸写。`DELETE FROM catalog.item_node_inventory` 同理。
  - 修法:在 services/dispositions 加具名函数(如 `rekey_suggested(conn, store, old_sku, new_sku) -> int`,内部处理唯一索引冲突:撞到即不迁、返回 0 并让调用方点名人工),在节点库存的所属 services 里加 `drop_rows(conn, store, sku)`;workflows/sku_migrate 只调这两个函数,不写这两条 SQL。
  - 证据:工作包:批次3 W3(confirmed 分支五步)与 ddl 第 11 条;同包 O8 已把读侧收进 services/dispositions。仓内现状:refdata/schema.sql:1260-1276 与 1313-1314。规则出处:conventions §三「处置建议…破坏动作只有一个出口,压制在 dispositions.claim() 里判」;CLAUDE.md「每个能力只有一条实现路径」。
- [批次 3 / 横切 / X1 与 横切 C0-DDL-6] 订单双算体检写了两份等价实现。横切 C0-DDL-6 建视图 `orders.v_order_line_dupes`(GROUP BY store,po_id,line_number HAVING count(DISTINCT order_line_id)>1),并由 catalog_health F 段⑤ 消费;批次 3 X1 又在 `services/order_lines.duplicate_po_lines()` 里把同一条 GROUP BY/HAVING 重写一遍,由 sku_migrate W6 每轮调。两份判据(视图口径按 order_line_id 去重、函数口径按 count(*))迟早漂,而漂了不报错。
  - 修法:视图定为唯一判据,`duplicate_po_lines` 改成 `SELECT * FROM orders.v_order_line_dupes`(带 days 参数就在视图里留);两处的 HAVING 口径必须逐字相同,并加一条测试断言函数 SQL 文本里出现的是视图名而不是 GROUP BY。
  - 证据:工作包:横切 C0-DDL-6 与 C0-HEALTH-1 第⑤项;批次3 X1 与 ddl 最后一条、W6 执行序。规则出处:conventions §六「每个能力只有一条实现路径」。
- [批次 0a / 0b / / 0a-02+0a-04+0a-05+0a-09 与 横切 C0-DDL-1/5 + C0-DOC-1 + C0-TEST-1] DDL 与文档同步的所有权在多个包里重复:登记簿三列(0a-02 vs C0-DDL-1)、audit_listing_conflicts 视图改写(0a-04 vs C0-DDL-5)、docs/db_schema.md 同步(0a-05 vs C0-DOC-1 vs 批次3 R2)、两个新事件码的登记时点(0a-09 说批次 0a、C0-TEST-1 说批次 2)。虽然都带 IF NOT EXISTS 不会炸,但两份规格一旦出现措辞差异(比如视图里 LEFT JOIN 是否带 source_type='amz' 条件:0a-04 带,C0-DDL-5 不带,两者结果集在 match 行上不同),后合的那份会静默改掉先合那份的语义。
  - 修法:横切包声明为 DDL / registry / docs 的唯一出处;0a/0b/1/2/3 里对应的 item 全部降级为「核验横切包已落地的对象在位」,并在各自 depends_on 里点名依赖哪一条。视图 JOIN 是否限定 source_type='amz' 必须二选一定稿(建议带上,与 0a-11/0a-12 的 amz 口径一致)。
  - 证据:工作包:0a items 0a-02/0a-04/0a-05/0a-09;横切 C0-DDL-1/C0-DDL-5/C0-DOC-1/C0-TEST-1;批次3 R2。规则出处:CLAUDE.md「动了表同步 docs/db_schema.md」;conventions §六。
- [批次 0a / 0a-07 / new_modules(services/sku_codec.py 的 mint)] mint 的撞码重抽是个 catch-all 兜底,会把并发双 mint 误诊成随机源故障。规格写的是「INSERT … ON CONFLICT DO NOTHING RETURNING sku;拿不到行 = 撞了三条唯一约束之一,重抽,至多 5 次,仍撞抛 RuntimeError(『随机源坏了,不是运气』)」。无 target 的 ON CONFLICT DO NOTHING 会吞掉任意唯一冲突,但这三条约束语义完全不同:撞 sku 全局唯一该重抽;撞 (store,source_type,source_key) 活码唯一说明另一个进程刚给同一个品发了码,重抽 5 次必然次次失败,最后抛出的诊断词指向随机源——排障方向全错,而这正是并发场景下最可能发生的那一种。
  - 修法:不用无 target 的 DO NOTHING:对 sku 唯一走 `ON CONFLICT (sku) DO NOTHING RETURNING`(拿不到 ⇒ 重抽,记 `_sku_redraws` 计数);活码唯一的冲突单独用 `ON CONFLICT ON CONSTRAINT <活码索引> DO NOTHING` 或重查活行 —— 命中即返回对方刚写入的那个码(与 mint 的复用语义一致),并 logger.warning 记一次并发计数。两条分支各记各的日志,满足真兜底三要件。
  - 证据:工作包:0a items 0a-07 与 new_modules 的 mint 段。规则出处:conventions §六 真兜底三要件「藏在同一个函数内、触发必须记日志计数、触发条件明确而非 catch-all」。

**minor**(5):

- [批次 0a / 3 / 0a-07 + 0a-10 + 0a-26⑤] 登记簿的 INSERT 从此有两条写路径(`listing_sources.register` 与 mint 内联 INSERT),而守门只扫 UPDATE。0a-26⑤ `test_only_sku_codec_writes_the_abandon_columns` 扫的是 `UPDATE catalog.listing_sources`,INSERT 完全不设防;将来谁在工作流里补一条 INSERT 就长出第三条登记路径,而且不报错。另外 0a-10 的 docstring 口径「三列只准由 sku_codec.abandon 写」在批次 3 变成假话——C1 的 mint_replacement 也写 replaced_by/replaced_at。
  - 修法:守门同时扫 `INSERT INTO catalog.listing_sources`,白名单只允许 services/listing_sources.register(backfill 与人工号)与 services/sku_codec(mint/mint_replacement);0a-10 的 docstring 改成「三列只准由 services/sku_codec 写(abandon / mint_replacement)」。
  - 证据:工作包:0a items 0a-07(mint 内联 INSERT)、0a-10、0a-26⑤;批次3 C1。仓内现状:services/listing_sources.py:29-42 是当前唯一写入点。规则出处:conventions §六。
- [批次 0a / 0b / 0a-06 与 0b-01] SKU→ASIN 反查出现三个入口:`resolve`(单条)、`resolve_many`(批量,0a)、`resolve_pairs`(批量+纯数字倒查,0b),调用方得自己挑。0b-01 步骤 (a) 里 resolve_pairs 又内嵌调 resolve_many,两个批量入口语义高度重叠,守门(0b 的 `test_registry_hop_lives_in_services_only`)只禁 workflows 用 resolve_many,没说 services 内部谁该用哪个。
  - 修法:在 services/sku_asin 模块 docstring 里写死分工:`resolve_pairs` = 清洗类工作流的唯一批量入口(含纯数字倒查那一跳),`resolve_many` = services 内部的纯登记簿批量反查,`resolve` = 单条壳;三个函数 docstring 首行各自点名调用方,并在守门白名单里按这个分工登记允许的调用文件。
  - 证据:工作包:0a items 0a-06;0b items 0b-01、0b-02 与 guard_tests 第 1 条。规则出处:CLAUDE.md「services 新增积木前先通读现有函数查重」;conventions §六。
- [批次 3 / W3(飞书 V 列回写)] confirmed 之后的飞书 V 列回写放在事务外、一次性,没有补写路径。W3 自述「事务外再做飞书 V 列回写」,但一旦这一次写失败(飞书频控 99991400、表未登记、行号找不到),该行的 ledger 已是 confirmed、不再进 `_settle` 的 pending 集,V 列就永远停在旧码上——而批次 1 定的 `row_sku` 就是靠 V 列认这一行的 SKU,回执找行/退役从此对不上,且不报错。
  - 修法:listing.sku_migrations 加一列 `sheet_synced_at timestamptz`,`_settle` 每轮把 `status='confirmed' AND sheet_synced_at IS NULL` 的行补写一次(仍走 batch 1 的 `listing_sheet.write_sku_col` 这一条通道),写成功才置时间戳;摘要里报未同步条数。
  - 证据:工作包:批次3 W3(confirmed 分支最后一段)、S2(sku_migrations 列清单无该列);批次1 B1-05 row_sku。规则出处:conventions §八「当轮写完…攒到下一轮 = 悄悄少写」;CLAUDE.md「飞书读写只准走 api/feishu 标准通道」(补写也必须走同一条)。
- [批次 2 / B2-11(_SQL_RETIRE_COOLDOWN)] 事件码写成了字面量,与同一轮改造自己定的纪律不一致。B2-11 的冷却 SQL 里直接写 `e.event = 'retire_feed_success'`,而 0a-24 明确要求「事件名用 product_events.SKU_ABANDONED 常量拼进 SQL,不写字面量」。回执类事件码在仓内是 `f"{kind}_feed_{st}"` 拼出来的,没有具名常量,于是纪律在这一处天然破功——一旦 `_FEED_KIND` 的取值改名,这条闸静默返回空集、冷却形同虚设。
  - 修法:在 services/product_events 里补一个 `feed_event(kind: str, status: str) -> str` 的具名 helper(或 RETIRE_FEED_SUCCESS 常量),B2-11 与其它需要拼回执码的 SQL 一律引用它;守门加一条:生产代码里不得出现 `_feed_success` / `_feed_failed` 字面量。
  - 证据:工作包:批次2 B2-11 的 `_SQL_RETIRE_COOLDOWN`;对照 0a items 0a-24。仓内现状:services/product_events.py:113-121 EVENTS 由 `{k}_feed_{st}` 派生,无具名常量。规则出处:services/product_events.py:116 自述「事件码常量(唯一出处;新增先在此登记)」。
- [批次 1 / 2 / B1-09 与 B2-16] `_mark_upc_conflicts` 的入参语义在相邻两批各改一次,各配一套测试,平白多一个中间态。B1-09 把它从 `asins: list[str]` 改成 `(store, ASIN)` 对并改按领号键反查;B2-16 又改成 `(store, 当前 SKU)` 对、函数内先经登记簿把 SKU 翻回 ASIN。中间态(批次 1 已合、批次 2 未合)期间,调用方 `sync_from_ledger` 传的是 `r["asin"]`,而批次 2 之后传的是行上的 SKU——两批的测试断言互相不兼容,评审时很难看出哪一版是终态。
  - 修法:批次 1 一次到位:入参直接定为 `(store, 行上的 SKU)`(存量 V 空时 row_sku 回落 B 列 ASIN,与今天等价,仍是零行为变化),函数内经登记簿反查 ASIN 再按 (store, asin) 查池;批次 2 就只剩「在烧号处加一次 abandon 调用」这一件事。
  - 证据:工作包:批次1 B1-09(签名改 pairs=(store, ASIN))与批次2 B2-16(签名再改 pairs=(store, 当前 SKU))。仓内现状:services/listing_sheet.py:336-360 与 :555 `conflicts.append(r["asin"])`。规则出处:conventions §六「双轨过渡遗留…新系统一律禁止」。

**遗漏项**:
- 变体组 ID 仍从 parent ASIN 派生(services/variant_group.group_id → mp_conform 写进 Visible.variantGroupId,随载荷发给沃尔玛),货源仍可从组 ID 倒推——批次 2 与批次 3 都只在 risks 里提了一句,没有任何包给出 item、所有者决策项或待办登记。整个改造的目标是货源隐匿,这是唯一一条被识别却无人认领的泄漏路径。
- feed_poll 的 DANGEROUS 标记没人重新评估。批次 2 之后它经由 listing_sheet 做不可逆动作(弃码 + 永久烧 UPC),但 workflows/feed_poll.py:38 仍是 DANGEROUS=False,cli.py:307 因此强制 execute=True。没有任何 item 提出改它或论证为什么不改。
- 安全红线「新旧系统严禁对同一破坏性任务并跑」只在批次 2 R7 与批次 3 depends_on 里以「必须确认」的口气出现,没有落成 item、验收命令或前置闸(比如上线前跑一条检查旧仓 auto_listing / retire_and_relist / product_clear / daily_cleanup cron 是否已停)。synthesis open_questions #15 至今未闭合。
- 沃尔玛 spec 里 Orderable.sku 的长度上限与字符集仍未核(sku_plan §8 待办),而批次 2 就要按 12 位真跑。没有任何包把它列成批次 2 的阻塞前置(批次 2 的 depends_on 里只列了四个来源字母与决策 A/B/C)。
- 批次 3 confirmed 之后,受 (store, sku) 键牵连的其余表只处理了 dispositions 与 item_node_inventory;sku_plan §7 点名的 `maintenance` drop_recent 防重键、`ops.dedupe`、`claims`、`ops.cleanup_seen_categories` 里,只有 cleanup_seen_categories 有结论(横切 R2 判「不迁」),其余三张表既没结论也没「为什么不用管」的论证。
- abandon 是单向不可逆,全套工作包没有任何「撤销弃码」的人工入口或纠错路径(synthesis open_questions #13 未闭合)。中间窗口内运营在 Seller Center 手工改 Site End Date、或形态实测推翻判据时,只能靠人裸 UPDATE 登记簿——而所有守门都禁止 sku_codec 之外的 UPDATE。
- 批次 3 的 feed 形态(决策 E:MP_MAINTENANCE 最小载荷 vs MP_ITEM 全量)未决,而两者的副作用差别极大(后者会用我们再生成的标题/属性覆盖线上内容)。工作包把它列为决策点,但没有把「实测未通过前 sku_migrate 只准 dry-run」写成代码闸(W1 只在头注写了前置清单)。

### A.3 零行为变化证伪

逐条核对 0a/0b/1 的\"零行为变化\"论证后,找到 4 个 blocker、4 个 major、2 个 minor。\n\n**两条 0a 的论证漏洞是同一件事的两半,都指向 `pick_asin` / `coalesce(ls.source_key, w.sku)` 这个新身份键**:(1) 工作包自称\"唯一理论缺口\"的 R1 其实是必然产物 —— refdata/schema.sql:230-236 的回填正则 `^B0[A-Z0-9]{8}` **右端没有锚点**,所以任何 B0 开头、长度 >10 的存量 SKU(重上后缀 `B0XXXXXXXX-2` 这类)都被登记成 amz 且 `source_key = left(sku,10) ≠ sku`;这批行今天对整条维护链不可见,改完之后第一次进入 **_SQL_VARIANT_OFFSET / _SQL_LONG_OOS / product_audit mode=online 三条删除意图**的产出面。体检① 必须升级成合并硬闸,不能\"看一眼再决定\"。(2) 一个 R1 完全覆盖不到的新缺口:`pick_asin` 的 source_key 那条腿不做 `.strip().upper()`、不校验 ASIN 形态,而 `extract_asin` 两件都做;`listing_sheet.read_rows()` 只 strip 不 upper,`list_new` 又把 `r[\"asin\"]` 原样写进 source_key —— 上架表 B 列一个小写 ASIN 就足以让 alloc_push 的 online 集合里多一个垃圾键、真 ASIN 仍然缺席,于是**已在架的品被重新派工写回上架表**(alloc_push 是 DANGEROUS=True)。体检① 对这一类恒返回 0。\n\n**0b 的\"零行为变化\"在纯数字倒查那一跳上不成立**:今天 `_ITEMID_SQL` 不带 store、映射键是裸 sku,所以任意一家店的 walmart_items 有 item_id 行就能补全所有店;`resolve_pairs` 按店收窄 + 对 store 为 NULL 的对整条跳过,是严格的\"少补\",而工作包的 risks 只提到计数变动、把它写成\"修复\"。\n\n**批次 1 的零行为变化完全押在一个代码无法自检的手工建列上**:B1-03 让 `_COLS` 从 registry 元组派生,于是 B1-01 落地那一刻 `read_rows()` 立即读 A2:V,而它是 list_new / feed_poll / sku_locked_heal / product_audit(from_sheet) 的公共入口;决策 D4 明确拒绝加任何校验,飞书对越界列区间的行为又只是假设。建议把 B1-01 拆成最后一个提交。\n\n另外单列了三条工作包已知但被低估或错置的:决策 C 第二步(去 lifecycle 过滤)在存量上就是真行为变化、被写在 0a-20 的 change 字段里容易连着做掉;B1-09 把撞库反查从 used 放大到 claimed+used 且 missing 计数会变负、状态条件与 burn_for_retire 不一致;两条清洗工作流的摘要口径从\"不同 sku\"改成\"(店,sku) 组合\"后可解析率/覆盖率/样本全部改数且样本会重复。\n\n**核对通过、不构成 finding 的**:飞书加列的指纹重推(`_sync_stateful` 先 `_adapt_rows` 丢列再算 `_row_hash`,建列前确实零重推);去重闸 LEFT JOIN 后未登记的在架行不会漏拦(coalesce 回落 w.sku、abandoned_at 对未匹配行恒 NULL);`resolve` 对三段式/纯数字与 `extract_asin` 逐字相同(三段式的 `-` 使它永远不匹配回填正则,必落 unknown/NULL 走形态腿);`_FILL_SQL` 加 store 后覆盖的行集合本身不变(变的是倒查那一跳,见 F3);`listing_sheet` 读 22 列对 21 格返回值有 `+ [\"\"] * width` 补位;`upc_pool.release` 会清 store/asin,所以 B1-09 不会误伤已回收的号。"

**blocker**(4):

- [0a / 0a-11 / 0a-12 / 0a-13 / 0a-14 / 0a-15 / 0a-17 / 0a-23 / 0a-24(全部 6 处 SQL 硬等号 + 依赖同一身份键的 Python 侧)] 零行为变化论证的唯一缺口(工作包 R1)不是理论缺口,而是 db_init 回填正则的必然产物,而且它的方向是把行**从破坏动作看不见变成看得见**。refdata/schema.sql:230-236 的存量回填条件是 `sku ~ '^B0[A-Z0-9]{8}'` —— **右端没有锚点**,所以任何以 B0 开头、长度 > 10 的存量 SKU(最典型的就是重上后缀 `B0XXXXXXXX-2`、`B0XXXXXXXX-relist`)都会被判成 source_type='amz',并写入 `source_key = left(sku, 10)`,即 source_key ≠ sku。对这批行:今天 `p.asin = w.sku` / `vo.asin = w.sku` / `o.asin = live.sku` / `w.sku = p.asin` 全部不命中,它们对整条维护链、审核候选、采集推送、去重闸都是不可见的;改成 `coalesce(ls.source_key, w.sku)` 之后全部命中。受影响的不是只有"少做事"的读侧:`_SQL_VARIANT_OFFSET`(services/maintenance_intents.py:231-246)与 `_SQL_LONG_OOS`(:279-331)产出的是**删除意图**,`workflows/product_audit.py:409-411` 的 online 候选喂给 problem_scan 的 rejected_still_listed(也是删除建议)。也就是说这批行会在合并当天第一次进入自动删除面。次生的还有:`workflows/product_refresh.py:63-90` 的 `_ASIN_RE.fullmatch` 今天把它们过滤掉(计入 dropped),之后 source_key 通过过滤 → 新增采集推送;`workflows/list_new.py:302-306` 的去重闸今天用 `(store,'B0XXXXXXXX-2')` 拦不住重上 `B0XXXXXXXX`,之后会拦住;`workflows/list_new.py:659-669` 的 `_SQL_ATTEMPTS` 会把这批 feed_items 归并进同一个 (店, ASIN) 的重试计数(工作包体检⑥说的正是这一类,它是体检①的子集)。工作包把体检① 写成"不为 0 就逐行给所有者看后再决定",严重低估了后果 —— 这不是"身份键与今天不同",是自动删除面扩大。
  - 修法:1) 把体检① 升级成**合并硬闸**:`SELECT count(*) FROM catalog.listing_sources WHERE source_type='amz' AND source_key IS NOT NULL AND source_key <> sku;` 必须为 0 才允许合并,非 0 一律先修数据(把这批行 UPDATE 成 source_type='unknown',或按人工判定改正 source_key)再合并 —— 不要"看一眼再决定"。2) 同批把 schema.sql:230-236 的回填正则右锚成 `'^B0[A-Z0-9]{8}$'`,并把 `left(sku,10)` 改成 `sku`(与 workflows/sources_backfill.py:46 的 `^B[0-9A-Z]{9}$` 口径对齐),否则下一次在新库跑 db_init 会再造一批同样的行。3) 体检① 之外补一条方向性体检,直接量化删除面变化:改前改后各跑 `python cli.py maintenance_scan -p preview=1`,按 kind 分组比对条数(工作包已列),但必须**把 delete 类的差额单列出来给所有者看**,而不是只看总条数。
  - 证据:refdata/schema.sql:228-236 回填 `CASE WHEN sku ~ '^B0[A-Z0-9]{8}' THEN left(sku, 10) END`(无右锚);对照 workflows/sources_backfill.py:46 `_ASIN_RE = re.compile(r"^B[0-9A-Z]{9}$")`(有右锚、整串入 source_key)。受影响的删除路径:services/maintenance_intents.py:236-237 `JOIN catalog.walmart_items w ON w.sku = vo.asin`、:322 `FROM live JOIN obs o ON o.asin = live.sku`;workflows/product_audit.py:409-411 `WHERE w.sku = p.asin AND
- [0a / 0a-06(services/sku_asin.pick_asin)+ 0a-19 / 0a-20 / 0a-21 / 0a-22 的 Python 侧调用点] `pick_asin(source_key, sku)` 的定义是「source_key 归一后非空则返回它,否则 extract_asin(sku)」,但 `extract_asin` 的第一件事是 `str(sku).strip().upper()` **并且要求 `_PLAIN` 全匹配**(services/sku_asin.py:32-38),而 source_key 这条腿只"归一"、不校验形态。两条腿口径不同,对**任何 source_key 不是规范大写 10 位 ASIN 的登记行**就会给出与今天不同的键。这不是假设:`workflows/list_new.py:260-263` 写的是 `source_key = r["asin"]`,而 `r["asin"]` 来自 `services/listing_sheet.read_rows()`,那里只做 `str(c).strip()`(:69),**从不 upper()**,也从不校验 ASIN 形态。运营在上架表 B 列填一个小写 ASIN、或粘进一个带零宽字符的串,这个原样值就进了 listing_sources.source_key。今天 alloc_push 走 `extract_asin('b0abcdefgh')` → `_PLAIN` 大写类不匹配 → None → 该行被 `online = {(s,a) for s,a in online if a}` 滤掉,于是 `(store, ASIN)` 不在 online 集合里……但 `held`(claims)里的键是规范大写 ASIN,两边都取不到,净效果是这行**既不算在架也没被误判**;改成 pick_asin 之后 online 里多了一个 `(store,'b0abcdefgh')` 垃圾键,而真正的 `(store,'B0ABCDEFGH')` 仍然缺席 —— alloc_push 于是判定"这家店还没有这个品",把一个**已经在架的品重新派工写进上架表**(alloc_push 是 DANGEROUS=True,直接写飞书上架表)。alloc_plan._listed_asins、alloc_products 的在线店字典同向退化。工作包的 R1 只覆盖 source_key ≠ sku,完全没有覆盖"source_key 与 sku 相等但两者都不规范"这一类 —— 体检① 对它恒返回 0。
  - 修法:1) `pick_asin` 的 source_key 那条腿必须与 extract_asin 同口径:`s = str(source_key or "").strip().upper()`,并且 **`s` 必须先过 `is_standard_asin(s)`(services/sku_asin.py:41-45)才可信**,不过就落到 `extract_asin(sku)`;docstring 里写死"登记簿只是优先级,不是免检通道"。2) 追加一条与体检① 并列的合并前体检:`SELECT count(*) FROM catalog.listing_sources WHERE source_type='amz' AND source_key IS NOT NULL AND source_key !~ '^(?=.*[A-Z])[A-Z0-9]{10}$';` 期望 0,非 0 先清洗。3) 顺手在 `services/listing_sheet.read_rows()` 或 list_new 的登记处把 asin 规范成 `.strip().upper()` —— 但那是行为变化,单列给所有者,不要混进 0a。
  - 证据:services/sku_asin.py:22-24 `_PLAIN = re.compile(r"^(?=.*[A-Z])[A-Z0-9]{10}$")`、:32-38 `extract_asin` 先 `.strip().upper()`;services/listing_sheet.py:67-69 `cells = [(str(c).strip() if c is not None else "") ...]`(无 upper);workflows/list_new.py:260-263 `"source_key": r["asin"]`;workflows/alloc_push.py:70-73 `online = {(s, sku_asin.extract_asin(sku)) ...}` + `online = {(s, a) for s, a in online if a}
- [0b / 0b-01 / 0b-04 / 0b-05 / 0b-06 / 0b-07] `resolve_pairs` 把纯数字 item_id 倒查那一跳**收窄到店维度**,并且对 store 为 None 的对**整条跳过**,这两件事都是严格的"少补",与 0b 的零行为变化承诺直接冲突。今天 `services/sku_asin._ITEMID_SQL`(:63-66)是 `SELECT DISTINCT item_id, sku FROM catalog.walmart_items WHERE item_id = ANY(%s)` —— **不带 store、不带 missing_since**,所以一个纯数字 sku 只要在**任意一家店**的 walmart_items 里有对应 item_id 行,就能倒查出订货号并解析出 ASIN,而 `resolve_skus` 的映射键是裸 sku,`_FILL_SQL` 又只按 sku 匹配,于是**所有店**里这个 sku 的行都会被补上。改成按 (store, sku) 之后:(a) 订单行落在 T2 店、但该 item_id 只在 T1 店的 walmart_items 里有行(item_id 是沃尔玛全局商品号,walmart_items 是按店的截面,这在多店卖同款时是常态),今天补得上、之后补不上;(b) `catalog.product_events` 的平台级事件(store IS NULL)按 0b-01 明文"直接跳过 (b)(c) 两跳",今天只要 sku 是纯数字就走倒查,之后一律留 NULL。工作包的 risks 只提到"`_ITEMID_SQL` 加 store 后 DISTINCT 的语义变化 …… 这是**修复**,但它会让 numeric_resolved 的计数变动",完全没有提到"有一批今天补得上的行之后补不上" —— 而 order_lines.asin 是分配引擎销量/退货率维度的唯一入口,product_events.asin 是 product_risk 归并的唯一入口,少补是静默的。
  - 修法:1) 把倒查那一跳明确定成**两级**:先按 (store, item_id) 查;查不到再按 item_id 全局查一次并在摘要里单列计数(`numeric_resolved_cross_store`),而不是直接放弃 —— 全局那一跳今天就是既有行为,收窄它需要所有者点头。2) store 为 None 的对(product_events 平台级事件)不要跳过倒查,按全局那一级走。3) 合并前跑对拍体检:改前改后各跑一次 `python cli.py sku_normalize` 与 `python cli.py order_asin_normalize`(均预览、零写入),**「可解析 N 个」不得下降**;另加一条 SQL 量化跨店 item_id 面:`SELECT count(*) FROM (SELECT item_id FROM catalog.walmart_items WHERE item_id IS NOT NULL GROUP BY item_id HAVING count(DISTINCT store) > 1) t;` 非 0 就说明 (a) 这一类真实存在。
  - 证据:services/sku_asin.py:63-66 `_ITEMID_SQL`(无 store 条件)、:69-101 `resolve_skus` 的 `mapping` 以裸 sku 为键;workflows/sku_normalize.py:37-41 `_FILL_SQL ... WHERE e.sku = m.sku`;workflows/order_asin_normalize.py:57-61 同款;工作包 0b-01 原文"store 为 None 的对(product_events 平台级事件)直接跳过 (b)(c) 两跳"。
- [1 / B1-01 / B1-03 / B1-04 / 决策 D4] B1-03 把 `_COLS` 从字面量 21 改成 `len(resources.LISTING_SHEET.columns)`,于是 **B1-01 往 registry 元组追加 "sku" 的那一刻,`read_rows()` 的读取范围立即变成 A2:V**,没有任何开关、没有任何代码级校验(决策 D4 明确拒绝加表头核验)。`read_rows()` 是上架链的公共入口:workflows/list_new、workflows/feed_poll(经 listing_sheet.sync_from_ledger / heal_unknown)、workflows/sku_locked_heal:224、workflows/product_audit -p from_sheet=1(经 audit_targets)全部走它。如果飞书对超出实际列宽的 range 返回错误(90204/范围不合法),这四条链在"所有者还没建 V 列"的那段窗口里**全部当场失败**,而其中 list_new / feed_poll 挂在 product_chain 上 —— 整链失败。工作包把这条放在 depends_on 里当"硬前置",但硬前置是人的纪律,代码里没有任何东西阻止 PR 先合、列后建;而且飞书对越界列区间到底是报错还是静默截断,工作包自己也只是**假设**(B1 risks 写"read_rows 读 A2:V{total} 会被飞书拒(90204/范围不合法)",没有实证行号),单元测试里 feishu 全被打桩,这个假设永远测不出来。
  - 修法:1) 合并前先在生产上跑一次实证并把输出贴进 PR:`python -c "from api import feishu; from registry import resources; print(feishu.sheet_values_small(resources.LISTING_SHEET, 'A1:V1'))"` —— 确认是报错还是截断,把结论写进 services/listing_sheet.py 的 read_rows docstring。2) 把"V 列已建"做成 PR 的**阻塞式检查项**(与 db_init 幂等同级),而不是 depends_on 的一行文字;或者更稳:把 B1-01(registry 加列)拆成独立的、在所有者确认建列之后才合的最后一个提交,B1-05/06/07(row_sku / _sku_range / write_sku_col)先合 —— 它们不依赖列宽。3) 在 tests/test_listing_sku_col.py 里补 `test_read_rows_reads_through_column_v`:打桩 `feishu.sheet_values_rows` 捕获实参,断言 `(first_col, last_col) == ("A", "V")`,并**另补一条** `test_read_rows_pads_a_21_cell_legacy_row`:返回 21 格的行,断言 `d["sku"] == ""` 且其余 21 个字段与改造前逐字相同 —— 后者钉住的是"截断"分支,前者钉住的是范围本身。
  - 证据:services/listing_sheet.py:41 `_COLS = 21          # A~U`、:57-70 `width = (cols.index(upto) + 1) if upto else _COLS` → `feishu.sheet_values_rows(sheet, "A", chr(ord("A") + width - 1), 2, total)`;api/feishu.py:684-723 `sheet_values_rows` 只对 90221 做对半兜底,其它 FeishuError 直接 raise;调用方:workflows/sku_locked_heal.py:224 `rows = listing_sheet.read_rows()`、services/listing_sheet.py:412(heal_unknown)、:526(sync

**major**(4):

- [1 / B1-09] `_mark_upc_conflicts` 的反查键从 `upc_pool.sku` 改成 `(store, asin)` 时,顺带把**匹配的状态面**从「只有 used」放大成「claimed + used」,而工作包只把这当成"次生差异…实际集合仍是同一行"。两条硬证据说明这个前提不成立:(1) 今天的 `WHERE sku = ANY(...)` 只可能命中 `mark_used` 写过的行(services/upc_pool.py:210-219 是唯一写 sku 列的地方,status 同时被置 'used'),而 `release()` 会把 store/asin 清成 NULL(:232-236),所以 status='' 的行不会误伤 —— 但 `claimed` 且**尚未提交 feed** 的行 store/asin 都在,新写法会把它一并标 conflict 永久弃用;(2) `claim()` 的复用查询本身写成 `SELECT DISTINCT ON (store, asin) ... ORDER BY store, asin, claimed_at NULLS LAST`(:147-157),即代码明确容忍同一 (store, ASIN) 名下有多个活号 —— 一旦真有,新写法一次烧多个,而 `missing = len(uniq) - n` 会变成**负数**,`if missing:` 分支照样为真,打印一条 "UPC 撞库 -1 个在池中找不到" 的胡话告警。另外新 SQL 用的是 `status <> 'conflict'`,比 `burn_for_retire` 的 `status IN ('claimed','used')`(:198-203)宽,两个烧号出口口径不一致本身就违反单一路径。
  - 修法:1) 新 SQL 的状态条件写成 `AND p.status IN ('claimed', 'used')`,与 services/upc_pool.burn_for_retire:198-203 逐字对齐(而不是 `status <> 'conflict'`)。2) `missing` 改成"零命中的 pair 数":`hit = {(s, a) for _upc, s, a in found}; missing = len([p for p in uniq if p not in hit])` —— 一个 pair 命中多个号不该让计数变负。3) 测试:tests/test_listing_sku_col.py::test_upc_conflict_only_burns_claimed_or_used(池里同 (T1,B0X) 放一个 status='' 的行和一个 'used' 行,断言只烧 used 那个)、::test_upc_conflict_missing_counter_never_goes_negative(同 (T1,B0X) 两个 used 行,断言 missing == 0 且不打 warning)。
  - 证据:services/listing_sheet.py:349-360 现行 `WHERE sku = ANY(%s) AND status <> 'conflict'` + `missing = len(set(asins)) - n`;services/upc_pool.py:210-219 mark_used(唯一写 sku 列处,同时置 status='used')、:228-238 release(清 store/asin)、:188-206 burn_for_retire(`status IN ('claimed','used')`)、:147-157 claim 的 `DISTINCT ON (store, asin)` 复用查询。
- [0a / 0a-20 第二步 / 决策 C] 决策 C 的第二步(把 `workflows/alloc_push.py:49-52` 的 `coalesce(upper(lifecycle_status),'ACTIVE') = 'ACTIVE'` 去掉、只留 `missing_since IS NULL AND ls.abandoned_at IS NULL`)是**存量数据上就立刻生效的真行为变化**,不是等到批次 2 才发生的事:今天 walmart_items 里已经有一批 lifecycle=RETIRED 的行(catalog_sync 显式扫一轮 RETIRED,它们 missing_since 为 NULL),这些 (store, ASIN) 今天**不算在架 ⇒ 会被派工**,去掉 lifecycle 条件后它们变成"算在架 ⇒ 不派工"。工作包把第二步推到批次 2,这是对的;但它同时把这一条写在 0a-20 的 change 字段里、写在同一个 item 里,评审很容易连着一起做掉。此外这一步一旦做,`services/alloc_survey._SQL_ONLINE:184-190` 与 `services/risk_trace` 的 still_listed 三条件里的 lifecycle 判据就出现三种口径并存,而 risk_trace 的注释(:18-22)明确说 alloc_survey 是它的同款先例 —— 口径分叉之后那条注释就成了错的。
  - 修法:1) 把第二步从 0a-20 的 change 字段里**物理移走**,单独立一条 item(编号进批次 2),0a-20 只保留"身份收口 + 落地恒真的 ls.abandoned_at IS NULL"。2) 第二步真要做时,先跑 `python cli.py alloc_push --dry-run` 对比派工行数,并把差额名单(即 `lifecycle=RETIRED AND missing_since IS NULL` 的 (店, ASIN) 清单)贴给所有者。3) 同批必须改 services/risk_trace.py:18-22 那段注释(它把 alloc_survey._SQL_ONLINE 当成"同款判法"的引证),说明 still_listed 为什么**不**跟着改 —— 否则下一个人会顺手统一。4) 守门:tests/test_alloc_push.py::test_online_set_still_excludes_retired(在批次 2 之前反向钉死 lifecycle 条件仍在),批次 2 时再翻转这条断言。
  - 证据:workflows/alloc_push.py:46-52 现行 `_SQL_ONLINE` 含 `AND coalesce(upper(lifecycle_status), 'ACTIVE') = 'ACTIVE'` 及其上方注释"排 RETIRED —— 退市行不算活货位,它对应的占用是待重上的,该派工";services/alloc_survey.py:176-190 同款判法与"必须排 RETIRED"的 2026-08-15 教训;services/risk_trace.py:18-22 still_listed 条件 b 的注释引 alloc_survey 为先例。
- [0b / 0b-02 / 0b-05 / 0b-07] `samples()` 与两条清洗工作流的摘要口径从「不同 sku」变成「(店, sku) 组合」,但工作包只把它当成一句文案改动("待洗 N 个不同 sku"→"待洗 N 个 (店,sku) 组合")。实际连带改数的有四处:`buckets` 的形态计数(今天按 DISTINCT sku 计,之后按 pair 计 —— 同一个 sku 在 5 家店就从 1 变成 5)、`rate = len(mapping)/len(skus)`(可解析率)、`unresolved = len(skus) - len(mapping)`、以及 `_COVERAGE_SQL` 之外的全部摘要数字。而 `samples()` 的实现是 `[s for s in skus if classify(s) == k][:5]`,换成 pairs 之后**同一个 sku 会在样本里重复出现多次**,前 5 个样本可能全是同一个串 —— 而这个样本列表的全部作用就是"新形态先进「其他」桶带样本报出来,人认了再扩规则"。摘要是所有者决定要不要跑 `-p apply=1` 的唯一依据,数字口径变了却没有任何测试钉住。
  - 修法:1) `samples(pairs, buckets)` 内部对 sku 去重后再取前 5:`seen=set(); [s for _st, s in pairs if classify(s)==k and not (s in seen or seen.add(s))][:5]`。2) 形态计数 `buckets` 保持按 **DISTINCT sku** 计(与今天一致),另加一档 `pairs` 记 (店,sku) 组合数;摘要写成"待洗 N 个不同 sku / M 个 (店,sku) 组合",让两个数并存,可解析率仍按 sku 算。3) 测试:tests/test_sku_asin_consumers.py::test_samples_dedupes_a_sku_that_spans_stores(同一 numeric sku 在 3 家店,断言样本里只出现一次)、tests/test_order_asin_normalize.py::test_preview_summary_keeps_the_distinct_sku_count(同一 sku ×3 店,断言"待洗"里的 sku 数仍是 1)。
  - 证据:services/sku_asin.py:103-110 `samples` 的实现 `[s for s in skus if classify(s) == k][:5]`;services/sku_asin.py:87-89 `buckets[kind] += 1` 按传入列表逐元素计;workflows/order_asin_normalize.py:92-97 `rate = len(mapping)/len(skus)` 与 head 文案;workflows/sku_normalize.py:53-62 同款。
- [0b / 0b-11 / 决策 D-0b-7] 决策 D-0b-7 的事实前提写错了:它把 `cleanup_history_import` 列为"store 为空的平台级事件"三个来源之一,并据此说"它们传的 sku 本来就是 asin,extract_asin 恒等返回,零风险"。实际上 cleanup_history_import 从旧库 `error_items` 逐行读 `run_ts, store, sku, category, reason` 并把 **store 原样带进 record_many**,而它的 sku 是旧系统的订货号(正是三段式/纯数字那批,`docs` 里说的 24 万条时间线事件与 41.7 万行导入)。后果有两层:(a) 这 41.7 万行会走**登记簿那条腿**而不是形态腿 —— 对存量结果虽然等价(这批 (店, sku) 多半在 listing_sources 里是 unknown/NULL,回落 extract_asin),但工作包的性能评估"多 42 条批量 SELECT"是按"平台级事件不查库"算的,实际每一批 10000 行都要把 10000 个 (店,sku) 塞进 unnest 去 JOIN listing_sources;(b) 更实质的是,守门测试的 extract_asin 白名单要把 `services/product_events.py` 标注成"仅 store 为空分支",而这个标注与事实不符 —— 依据一个错误的事实写守门注释,下一个人按注释推理会得出错误结论。
  - 修法:1) 改正 D-0b-7 的事实描述:平台级(store 为空)来源只有 product_ingest 与 product_audit;cleanup_history_import 带 store,走登记簿腿。2) 相应地把 0b-11 的性能护栏测试从"一次调用一条 SELECT"加强成"一次调用一条 SELECT 且 unnest 参数与 rows 等长",并在 record_many 里对 `rows` 长度超过某阈值时记一条 info(41.7 万行导入时看得见)。3) 测试:tests/test_cleanup_history.py::test_history_import_events_carry_a_store(断言 services/cleanup_history.py 产出的行 store 非空)、tests/test_product_events.py::test_record_many_issues_one_lookup_per_call(工作包已列,保留)。
  - 证据:services/cleanup_history.py:64 `key = (r.get("store"), r.get("sku"))`、:75 `"sku": r.get("sku"), "store": r.get("store")`;workflows/cleanup_history_import.py:101-103 `src.execute("SELECT run_ts, store, sku, category, reason FROM error_items ...")`、:114/:117 `product_events.record_many(conn, batch)`。

**minor**(2):

- [0b / 0b-13 / 决策 D-0b-3] D-0b-3 决定"collect_brands 的 ops.dedupe 去重键保持 sku 原文"并称零行为变化,这一点本身没错;但它没看到 0b-13 让**折叠后的 store 变成了载荷**。`collect_brands` 第一步是 `cands = {it["sku"]: it for it in items if ...}`(services/blacklist.py:146),同一个 sku 来自两家店时后一条覆盖前一条,`it` 里的 store 是**任意一家**。今天 `asin_of = {sku: (extract_asin(sku) or sku)}` 与 store 完全无关,所以折叠是无害的;0b-13 改成 `resolve_many(conn, [(it.get("store"), sku) for sku, it in todo.items()])` 之后,查登记簿用的是折叠后幸存的那家店。存量数据上两家店的 amz 行 source_key 都等于 sku,所以结果仍相同 —— 但这是巧合,而且 D-0b-3 的守门测试 `test_collect_brands_dedupe_key_is_still_the_sku` 只钉去重键,钉不住这个。
  - 修法:1) 要么把 `cands` 的键改成 `(store, sku)` 并同步改 `_PROCESSED_SQL`/`_MARK_SQL` 的 key(那是行为变化,单列给所有者);要么在 :146 上方加注释写死"折叠后 it['store'] 只用于溯源列,**不得**用作任何查询的键",并把 0b-13 的 resolve_many 入参改成对每个 sku 用**所有出现过的 store** 去查、取第一个解析成功的。2) 测试:tests/test_blacklist.py::test_collect_brands_resolution_does_not_depend_on_which_store_survived_the_collapse(同一 sku 在 T1 登记为 amz、T2 未登记,两种 items 顺序都必须得到同一个 brand 键)。
  - 证据:services/blacklist.py:146-147 `cands = {it["sku"]: it for it in items if it.get("category") in BRAND_CATEGORIES}`、:157 `asin_of = {sku: (extract_asin(sku) or sku) for sku in todo}`、:171 溯源列写 `asin_of[sku]`、:177 `_MARK_SQL` 实参是 `sku`。
- [0b / 0b-22 / 0b-23 / 0b-24 / 决策 D-0b-4] "建列之前零重推"这条论证经核对**成立**(`_sync_stateful` 先 `_adapt_rows` 丢掉表中不存在的列,再对 adapted 载荷算 `_row_hash`,所以 ASIN 未建列时指纹不变),但有一个未被提及的副作用:`_adapt_rows` 每一轮都会打一条 `logger.warning("表「%s」以下列不写入:%s", ...)`,内容是 `{'ASIN': '表中不存在'}`。order_center_push 挂在 order_sync / returns_sync 链尾,也就是**每一轮都刷一条 WARNING**,直到所有者建列为止(D-0b-4 明确说"代码先合,所有者后建列",这个窗口长度不可控)。仓内的纪律是"兜底静默常态化 = 主路径已坏没人知道",反过来"告警常态化"同样会让人对这条 WARNING 脱敏 —— 而它正是"列名建错(建成小写 asin 或「ASIN码」)"唯一的发现渠道。
  - 修法:1) 在 PR 描述与 docs/feishu_tables.md 里显式预告这条 WARNING 的文本与预期持续时间,并把"这条 WARNING 消失"作为建列成功的验收信号写进 acceptance(而不是只看推送量)。2) 更好的做法:0b-22 先只在 registry 登记字段常量、**0b-24 的载荷写入(`f.asin: r["asin"]`)延后到所有者建完列再合** —— 两者拆成两个提交,窗口期就没有 WARNING,也不需要预告。3) 测试:tests/test_order_center_push.py::test_missing_asin_column_is_skipped_not_written(打桩 list_fields 不返回 ASIN,断言 adapted 载荷里没有该键、且指纹与不含 asin 的载荷一致)。
  - 证据:services/order_center.py:159-197 `_adapt_rows`(`ftype is None` → `skipped.setdefault(name, "表中不存在")`,末尾 `logger.warning("表「%s」以下列不写入:%s", ...)`);:292-294 `adapted = _adapt_rows(t, desired)` 之后才 `f[_HASH_FIELD] = feishu._row_hash(f, _HASH_FIELD)`;api/feishu.py:461-466 `_row_hash` 只对传入的 fields 算。

**遗漏项**:
- 飞书对越界列区间(在 21 列宽的工作表上读 A2:V)到底返回错误码 90204 还是静默截断 —— 无法离线验证,api/feishu.py:684-723 只对 90221 有兜底。B1 的"fail loud 不是静默"这句话目前是假设,必须在生产上跑一次 sheet_values_small(LISTING_SHEET, 'A1:V1') 才能定论;这直接决定 F4 是"整链失败"还是"无害"。
- 生产库里到底有多少行满足 `source_type='amz' AND source_key <> sku`(F1)与 `source_key` 非规范大写 ASIN(F2)—— 本次是纯只读代码调研,未连库。这两个数决定 F1/F2 是 blocker 还是 no-op,合并前必须先跑。
- catalog.walmart_items 里跨店同 item_id 的行数(决定 F3 的 (a) 类是否真实存在)。
- 上架表 V 列在飞书侧是否已经存在同名人工列 —— docs/feishu_tables.md 与 sku_plan §8 都点了这条纪律,但需要用 list_fields / 读 A1:V1 实测。
- 批次 2/3 的工作包本次未逐条核对(任务范围限定 0a/0b/1);其中 B1-12 把 listing.retire_cooldown 的写入键改成 row_sku、而 upc_pool.burn_for_retire 仍按 (store, upc_pool.asin) 匹配,这一对在批次 1 里因 V 为空而恒等、批次 2 通电当天分叉 —— B2-13 声称修掉了,未验证。
- tests/ 下夹具里 sku 与 asin 同值的完整清单(横切包 C0-TEST-3 的三分类)未逐个文件核对,只抽查了 tests/test_list_new.py:28 与 tests/test_audit_sheet_loop.py:404-410。

### A.4 可执行性/顺序

串行顺序 0a → 0b → 1 → 2 → 3 本身成立,且行号质量很高:我逐个打开文件抽查了二十余处(schema.sql:213-236/243-256/521-551、list_new:250-306/345-389/659-710/1035-1060/1227-1232/1481-1487、listing_sheet:41/47-74/131-152/287-304/336-361/394-513/516-560、sku_locked_heal:62-64/79/92/102/125/197/208-212/233、maintenance_intents:192/202/231-236/279-291/322-323、product_events:86/113-121/145-173/238-267、order_lines:168/375/451-458、blacklist:51/99/157、wf/order_audit:423/461-463/479-483/1237-1240、registry:256-267/294-312/330-350/515-525/937-943、mp_mapper:641-697、feeds:65-79/257-263、_client:234-243),0a/0b/1/2 的误差都在 ±2 行内,批次 3 系统性漂 3-4 行。验收命令里引用的 workflow 参数(list_new store/check_spec、product_audit mode、maintenance_scan preview/store、order_audit line/scrape/push/wait、node_probe store/sku、sku_normalize apply、alloc_push limit)与 API(db.pg_dsn、feishu.sheet_values_small/_col_letter、feeds.query_pending/iter_result_slices、store_absence.stale_or_note、dispositions.stuck_executing、notify_fmt.head/summary)全部存在;cli 的 dry-run 语义也对得上(DANGEROUS 决定 execute,dry_run 另行透传,_prep_rows 在 :1650 位于 dry-run 早退之后,所以 B2-03 的 mint 不会在空跑里写库)。

真正的问题不在单个包内部,而在五个包之间。它们对同一件事给了互相矛盾的三份或四份规格:活码唯一索引有三个名字(0a 的 live_key_uk / 横切的 live_uidx / 批次 3 要 DROP 的 active_uidx),而批次 3 靠 `DROP INDEX IF EXISTS` 改它——名字对不上就静默 no-op,接着建的那条又去掉了 0a 苦心加的「限不透明码形态」局部条件,在存量重复 source_key 上必然建失败,而 db_init 是整份 schema.sql 一次 execute,一条失败整份回滚。不透明码字母表被安排了三个家,配了两条互斥的守门断言;audit_listing_conflicts 视图被两个包各写了一份不同的 DDL,后合的静默覆盖先合的;守门测试建了四份文件、白名单重复三处。这些错误的共同形态恰恰是这套材料自己反复警告的那一种:不报错、摘要正常、功能悄悄没了。

另有两处会真正伤到生产的具体缺口:一是 workflows/list_new.py 确实没有 limit 参数(:1407-1417 的 params 全集里没有),批次 2 自己在括号里承认了却没给 item,于是「一店一品」的试点闸没有落地物;二是批次 3 形态 B 的 SkuUpdate 会被 mp_conform.strip_unknown(:687)剔掉,结果不是报错而是「按新 SKU 建第二条 listing」,正好是它自己设计来告警的那个「同店双挂」,而且会是每一行。回滚方案只在横切包的散文里存在一份,而横切包按建议应当被拆解并入各批次——拆完回滚方案就无家可归;批次 2 的回滚还漏了一条:revert 后 _retry_rows 会拿 ASIN 重发已用不透明码上架成功的品。工时估算五个包一律没有,而这条改造的关键路径本来就不是写代码,是四个来源字母定值、三项决策拍板、七项单品实测和四列飞书建列。

建议的处置顺序:先把横切包拆解归位并统一字母表归属、索引命名、视图 DDL、守门文件这四件事(否则 0a 一合就有测试红),再给批次 2 补 limit 参数与回滚段,批次 3 重核行号并解决 strip_unknown。0a 建议按它自己给的备选切法落地(积木+schema+守门 一个 PR,15 处收口一个 PR),0b 的 D-0b-2(judge 改读 asin 列)建议强制单独成 PR。

**blocker**(6):

- [批次 3 / 批次 0a / S1 vs 0a-02 vs C0-DDL-2] 三个包给活码部分唯一索引起了三个不同的名字,而批次 3 靠 DROP INDEX IF EXISTS 改它。0a-02 建 listing_sources_live_key_uk;横切 C0-DDL-2 建 listing_sources_live_uidx;批次 3 S1 写 `DROP INDEX IF EXISTS catalog.listing_sources_active_uidx` —— 这个名字谁都没建过,IF EXISTS 让它静默 no-op,旧索引原样留着继续拦 mint_replacement 的第二条活行。更糟的是 S1 接着建的 `listing_sources_active_uidx ... WHERE abandoned_at IS NULL AND replaced_by IS NULL` 去掉了 0a 苦心设计的「限不透明码形态」局部条件,而存量同店同 ASIN 多 SKU、同店同 GTIN 多跟卖码是既成事实(schema.sql:228-236 的回填按 `^B0[A-Z0-9]{8}` 前缀 + left(sku,10) 产出重复 source_key)—— CREATE UNIQUE INDEX 会失败,而 db_init 是把整份 schema.sql 一次 execute,一条失败整份回滚,生产建库直接瘫。
  - 修法:三包统一索引名与条件,写进一处(建议 refdata/schema.sql 唯一定义 + docs/db_schema.md 同步)。批次 3 不要 DROP+CREATE,改成 0a 就把条件定成最终形态:`(store, source_type, source_key) WHERE abandoned_at IS NULL AND replaced_by IS NULL AND source_key IS NOT NULL AND sku ~ '^[<字母表>]{12}$'`(replaced_by 列同时在 0a 建,批次 3 前恒 NULL,条件恒等价)。批次 3 只加 replaces/replaced_at 两列与反查索引,一条索引都不重建。并在 tests/ 里加一条断言:schema.sql 全文中活码唯一索引名只出现一次。
  - 证据:refdata/schema.sql:213-227(现状建表+listing_sources_key_idx);:228-236 回填 `CASE WHEN sku ~ '^B0[A-Z0-9]{8}' THEN left(sku, 10) END`(前缀不锚定末尾,B0 开头长于 10 位的 SKU 会被截成同一前 10 位 ⇒ 同店重复 source_key);workflows/db_init.py 一次 execute 整份 schema
- [批次 0a / 0b / / 0a-01 / 0b-03 / B2-02 / C0-DDL-3+D4] 不透明码字母表被安排了三个代码之家,并配了两条互相矛盾的守门断言。0a-01 放 `registry.resources.SKU_ALPHABET`,守门 0a-26④ 断言 schema.sql 的字符类 == registry 常量;0b-03 放 `services.sku_asin.OPAQUE_ALPHABET` 并要求批次 2 的 sku_codec import 它;横切 D4 明文决定「字母表 _ALPHABET 落 services/sku_codec.py,不落 registry」,守门 `test_opaque_sku_regex_matches_the_codec_alphabet` 断言 schema.sql == sku_codec 常量。两条守门不可能同时绿。形态判据本身也分叉:0a-02 的索引带 `AND sku ~ '[A-Z]'`、0a-07 的 is_opaque 要求「至少一个字母」;0b-03 的 is_opaque_form 与横切 C0-DDL-3 的索引都没有这一条;批次 3 的 _SQL_CANDIDATES 也没有。同一个 12 位串在三处会得出不同判定,批次 3 自己的守门 `test_is_opaque_and_the_candidate_sql_agree` 当场红。
  - 修法:所有者/执行者先定一处:字母表 + 长度 + 「至少一个字母」三条口径全部只出生在 services/sku_codec.py(横切 D4 的理由最强:registry 管的是外部资源不是编码规则),registry 只留 SKU_SOURCE_LETTERS。删掉 0a-01 的 SKU_ALPHABET/SKU_LEN/SKU_RANDOM_LEN 与 0b-03 的 OPAQUE_ALPHABET/OPAQUE_LEN,0b 的 sources_backfill 分桶直接调 sku_codec.is_opaque(0b 在 0a 之后,sku_codec 已存在)。schema.sql 的两条索引条件统一为 `sku ~ '^[<字母表>]{12}$' AND sku ~ '[A-Z]'`,只留一条守门断言比对 schema 字面量与 sku_codec 常量。
  - 证据:0a-01 SKU_ALPHABET 定义 registry/resources.py:103 之后;0b-03 OPAQUE_ALPHABET 定义 services/sku_asin.py:21-27 附近;横切 decisions D4 原文「字母表 _ALPHABET、随机段长度 11、总长 12、查重重试 5 次全部落 services/sku_codec.py」;0a-26④ vs 横切 guard_tests 第 9 条
- [批次 0a / 横切 / 0a-04 vs C0-DDL-5] 同一个视图 catalog.audit_listing_conflicts 被两个包各给了一份 DDL,文本不同。0a-04 的 LEFT JOIN 带 `AND ls.source_type = 'amz'`,横切 C0-DDL-5 的不带。视图是 DROP+CREATE,后合的那份静默覆盖先合的那份;而 0a-04 新增的守门 `test_conflicts_view_reads_identity_through_the_registry` 断言视图文本含 `source_type = 'amz'` —— 横切版本一落地它就红。语义上横切版本也更弱:source_type≠amz 的行(match 的 source_key 是 GTIN、1688 是 offer_id)会被 coalesce 取到一个永远匹配不上 products.asin 的键,今天这些行是靠 `p.asin = w.sku` 落空,改后是靠一个更奇怪的键落空 —— 结论碰巧相同,但等价性论证不成立。
  - 修法:删掉横切 C0-DDL-5,视图改造归 0a-04 一处(它与 0a 的另外五处硬等号同批,才不会出现「一半按登记簿一半按裸 sku」的中间态)。保留 0a-04 的 `AND ls.source_type = 'amz'` 与「LATERAL 段一个字不动」两条纪律。
  - 证据:refdata/schema.sql:527 现状 `JOIN catalog.products p ON p.asin = w.sku AND p.marketplace = 'US'`;LATERAL 段 :536-551 的 `coalesce(ev.asin, ev.sku) = lr.asin -- 与身份键索引逐字一致`
- [批次 2 / acceptance_commands 试点第 2 步] 试点第 2 步写的是 `python cli.py list_new -p store=<试点店>  # …真跑 1 个品(list_new 无 limit 参数,手工只留一行或先加 -p limit=)`。核对属实:workflows/list_new.py 的 run() 只认 execute / gap_wait / workers / submit_jitter_ms / store / check_spec,**没有 limit**。于是「一店一品」这条最重要的止损闸只有两条路:一是人去改生产上架表把其余待上行删掉(荒谬且危险),二是「先加 -p limit=」—— 那是一处没有 item、没有测试、没有验收的代码改动。整个批次 2 的风险控制(「合并后不要立刻放全店,先一家店一个品」)因此没有落地物。
  - 修法:批次 2 补一个 item:workflows/list_new.py run() 加 `limit = int(params.get('limit', 0)) or None`,在 `_gate_by_store` 之后、`_prep_rows`(:1650)之前对 ready 截断(必须在闸门之后截,否则被淘汰行会占名额,与 :1195-1196「配额不在这里切」同源纪律);写清与 quota 的关系(limit 是人工试点闸,不改配额口径);测试 tests/test_list_new.py::test_limit_truncates_after_the_gates_not_before;验收 `python cli.py list_new --dry-run -p store=X -p limit=1` 摘要「共 1 行将进入 领UPC→LLM→提交」。
  - 证据:workflows/list_new.py:1407-1417 的 params.get 全集(execute/gap_wait/workers/submit_jitter_ms/store),:1592 check_spec;对比 workflows/alloc_push.py:59 `limit = int(params.get("limit", 0)) or None` 是既有写法
- [批次 3 / M2 / E(形态 B)] 形态 B(MP_ITEM 全量 + `build_orderable(..., sku_update=True)`)必须经过 `mp_conform.conform`,而 conform 里跑 `strip_unknown(spec, ospec, visible, orderable)`(services/mp_conform.py:687,调用点 :906/:913)会把 Orderable spec 里没有的字段剔掉。SkuUpdate 是不是在 Orderable spec 里,批次 3 自己列为「待实测」。若不在,SkuUpdate 会被静默剔掉,发出去的就是一条普通的 MP_ITEM —— 沃尔玛按新 sku 建一条**新 listing**,旧 listing 原样活着。结果正好是 W3 分支 (b)「同店双挂」,而且是每一行都双挂,不是偶发。批次 3 没有任何 item 处理这一跳。
  - 修法:决策 E 落形态 B 时必须补一个 item:要么把 SkuUpdate 加进 `ORDERABLE_SYSTEM_FIELDS` 之外的 strip 豁免名单(在 mp_conform 里显式登记「系统开关字段不剔」),要么在 conform 之后重新写回 `orderable['SkuUpdate'] = 'Yes'` 并加测试 `tests/test_mp_conform.py::test_sku_update_survives_strip_unknown`。在实测确认 SkuUpdate 在哪份 spec 之前,形态 B 一行都不许真跑。
  - 证据:services/mp_conform.py:687 `def strip_unknown(spec, ospec, visible, orderable)`;:906、:913 两处调用;services/mp_mapper.py:641-647 ORDERABLE_SYSTEM_FIELDS(只管 LLM 输入过滤,管不到 conform 的输出剔除)
- [横切 vs 批次 0a/ / C0-DDL-1..4 / C0-GUARD-1..7 / C0-REG-1..5] 横切包与 0a/2/3 大面积重复而不是补充:三列 ALTER(C0-DDL-1 = 0a-02)、upc_pool 状态注释(C0-DDL-4 = 0a-03)、LISTING_SHEET 22 列(C0-REG-1 = B1-01)、ORDER_SALES/RETURNS 的 ASIN(C0-REG-2 = 0b-22)、来源码列(C0-REG-3 = 0b-25)、SKU_SOURCE_LETTERS(C0-REG-5 = B2-01)、以及四张守门表。两边名字、条件、断言各不相同(见前三条 blocker)。按现状两个包都执行,schema.sql 会长出 5 条语义重叠的索引 + 2 个体检视图,registry 同一段被改两遍,守门测试互相矛盾。
  - 修法:横切包不应作为独立交付物。把它拆开并入:DDL/体检视图 → 0a;catalog_health F 段 → 0a;文档同步(api_blueprint §retire 更正、conventions §九、CLAUDE.md、README、feed_closure_audit、backlog、plan)→ 各自批次的 docs_to_sync;守门测试 → 合并成唯一一份 tests/test_sku_guard.py(见下一条)。横切独有、别处没有的只有四样:api_blueprint §8 retire/unretire 语义更正、conventions §九、orders.v_order_line_dupes 体检视图、catalog_health F 段 —— 这四样保留,其余删。
  - 证据:C0-DDL-1 与 0a-02 的三条 ALTER 逐字重复;C0-REG-1 与 B1-01 都改 registry/resources.py:515-525 的同一条元组;C0-REG-2 与 0b-22 都改 :294-312/:330-350

**major**(9):

- [批次 0a / 0b / / 0a-26 / 0b new_modules / B2-21 / C0-GUARD-*] 四个包各建一份守门测试文件:tests/test_sku_identity_guard.py(0a)、tests/test_sku_asin_consumers.py(0b)、tests/test_sku_codec_guard.py(2)、tests/test_sku_guard.py(横切)。职责大面积重叠:extract_asin 调用点白名单出现在三份;`abandoned_at IS NULL` 三处白名单出现在三份;abandon 调用点白名单出现在两份加批次 3;`x.asin = y.sku` 硬等号出现在两份。白名单是会随批次缩短的清单,维护在三处必然烂 —— 这正是 conventions §六「每个能力只有一条实现路径」要禁的形态,守门测试自己犯了它要守的规矩。
  - 修法:0a 建**唯一**一份 tests/test_sku_guard.py(形态照抄 tests/test_feishu_guard.py 的白名单 dict + `test_the_whitelists_do_not_rot`),后续批次只增删它的白名单条目,不许再建第二份。0b 的 new_modules 删掉 test_sku_asin_consumers.py(它的七条断言全部并进 test_sku_guard.py);批次 2 删掉 test_sku_codec_guard.py(abandon 白名单并进去);横切删掉本条。每条白名单的理由里写死「批次 X 收口后删」,PR 描述里报「本批删了几条、还剩几条」。
  - 证据:0a-26 五条断言 vs 0b guard_tests 七条 vs B2-21 四组 vs 横切 guard_tests 十一条,其中 extract_asin/abandoned_at/abandon 三类各重复 2-3 次;tests/test_feishu_guard.py:42-47、:348-371 是仓内既有的白名单纪律模板
- [批次 0a / 2 /  / 0a-26③ / B2-21(3) / C0-GUARD-5 / 批次3 guard] 「`abandoned_at IS NULL` 只准出现在三处」这条 synthesis 契约,在四个包里被数成三处、四处(含 schema.sql)、四处(含 sku_migrate)三种口径,而实际站点会长到五处以上:sku_codec.mint(0a-07)、list_new 去重闸(0a-23)、alloc_push._SQL_ONLINE(0a-20)、schema.sql 的部分索引条件(0a-02)、批次 3 的 sku_migrate._SQL_CANDIDATES(W4)、批次 3 的 mint 复用查询已含它(C1)。横切说 schema.sql 算一处、批次 3 说 schema.sql 不在扫描面。守门测试会在批次 3 当天红,而红的原因是白名单没跟上、不是有人加了洞 —— 这类假红最容易被人整条注释掉。
  - 修法:在 0a 就把白名单定成「按批次分层」的显式结构:`_ABANDONED_FILTER_OK = {文件: (允许的批次, 理由)}`,并把 schema.sql 明确排除在 .py 扫描面之外(注释写清为什么:DDL 的索引条件不是消费方过滤)。0a 落地时登记 3 个 .py 文件;批次 3 的 PR 里加第 4 条(sku_migrate)并在 conventions §九 把「三处」改写成「三处消费方 + 索引条件 + 改码候选」,规则文字与守门清单必须逐字对得上。
  - 证据:synthesis rule §6 原文「全仓 SQL 文本里 abandoned_at IS NULL 只允许出现在 sku_codec.mint、list_new 去重闸、alloc_push._SQL_ONLINE 三处」;0a-23 的去重闸 + 0a-25 的 _FAMILY_LISTED_SQL 在同一文件(workflows/list_new.py:302-306、:704-710)
- [批次 2 vs 批次 1 / B2-16 vs B1-05] B2-16 要求把 services/listing_sheet.py:555 改成 `conflicts.append((r[store], r[sku] or r[asin]))` —— 这是把批次 1 刚刚收口成 `row_sku()` 的回落表达式又在同一个文件里手写了一遍。批次 1 的守门 B1-05 `test_nobody_recomputes_the_row_sku` 明确扫 services/listing_sheet.py 里的 `["sku"] or` / `.get("sku") or` 形态,白名单只放 row_sku 自身那一行。批次 2 一落地这条守门就红,而它红得「有道理」,执行者很可能顺手把断言删掉 —— 于是批次 1 花整批建立的单一实现路径当场作废。
  - 修法:B2-16 改成 `conflicts.append((r["store"], row_sku(r)))`,并把「翻回 ASIN」那一跳留在 _mark_upc_conflicts 内部经登记簿做(它本来就要查登记簿)。守门断言一个字不改。
  - 证据:services/listing_sheet.py:535 `conflicts: list[tuple[str, str]] = []` 与 :555 `conflicts.append(r["asin"])`(声明是二元组、实际 append 字符串,B2-16 正确指出了这个既有不一致);B1-05 guard_tests 第 1 条
- [批次 0b vs 横切 / 0b-25 vs C0-REG-3] 在线产品总表新增列的 registry 元组元素,0b-25 写 `"来源码"`(并配守门 `test_online_sheet_last_column_is_source_key` 断言 `columns[-1] == "来源码"`),横切 C0-REG-3 写 `"source_key"`。两条断言互斥。现有 16 个元素全是英文标识符(store/sku/itemId/…),元组元素并不是飞书表头文本(表按位置 sheet_overwrite 写),所以功能上两者都能跑 —— 正因为都能跑,谁也不会立刻发现,直到守门测试红。
  - 修法:统一取 `"source_key"`(与现有 16 个英文标识符同风格,飞书表头文本另在 docs/feishu_tables.md 记「来源码」)。0b-25 的守门断言同步改。顺带确认 0b-26 的 `test_projection_columns_match_registry` 数逗号的实现在加 LEFT JOIN 之后仍成立(`split("FROM")[0]` 只切第一个 FROM,SELECT 段里没有 FROM,可以)。
  - 证据:registry/resources.py:261-266 现有元组全英文;services/walmart_catalog.py:138-150 `_PROJECTION_SQL` 及其 :147 注释「列序与 registry.resources.ONLINE_PRODUCTS_SHEET.columns 一一对应,改必同步」
- [批次 2 / estimated_pr_split 回滚段(缺失)] 批次 2 没有回滚方案(risks 里只说「试点不过就地停」)。横切包的 PR-2 回滚段说「代码可 revert,去重闸的键是 coalesce(ls.source_key, w.sku),revert 后照样拦得住」——前半对,但漏了一条真实的回滚危害:revert 之后 `_apply_submit_result`(workflows/list_new.py:259-283)回到写 `r["asin"]`,而已经用不透明码上架成功的那批品,只要将来出现一次 O=FAILED 走 `_retry_rows` 重新排队,list_new 就会用 sku=ASIN 重发 MP_ITEM —— 同一产品在同一店多出第二条 listing,烧一个 UPC,而且不报错。
  - 修法:批次 2 补一节回滚方案,写死两条:① revert 前先把已 mint 且已提交成功的 (店, 新码) 清单落盘(SQL 取 catalog.listing_sources where workflow='list_new' and sku 形如不透明码);② revert 后必须同时把这批行在上架表上标 O 终态或从待上队列摘掉,否则 _retry_rows 会拿 ASIN 重发。并加一条验收:revert 后跑 `python cli.py list_new --dry-run`,确认这批 ASIN 全部落在去重闸(counts['dedup'])而不是候选里。
  - 证据:workflows/list_new.py:259-283 三分支回写;:668-701 `_retry_rows` 按 `r["asin"]` 重排队(:690-692 传 asin,:696 比较键 asin)
- [批次 0a / 0b / / (全部包,缺 rollback 段)] 五个批次包里只有横切包在 estimated_pr_split 的散文里给了逐批回滚方案;0a/0b/1/2/3 自身**没有 rollback 字段、没有回滚段**。而横切包按前一条建议应当被拆解并入各批次 —— 拆完之后回滚方案就没有家了。批次 3 的三态状态机自带 rolled_back 路径,但那是运行时回滚,不是发布回滚。
  - 修法:每个批次包补一个显式的「回滚」小节,进 PR 描述模板(横切 estimated_pr_split 里那张表内容是对的,直接搬):0a/0b/1 = git revert + 新列不 DROP(conventions §五:DROP COLUMN 不可回滚,未连库核对不执行)+ 飞书新列留空不删;2 = 见上一条;3 = 已 confirmed 的改码不可回滚,停止后续批次、已改的认、旧行 replaced_by 保留做反查。
  - 证据:五个批次 JSON 的顶层键里都没有 rollback;横切 estimated_pr_split 末尾「逐批回滚方案」是全套材料里唯一一处
- [批次 2 / B2-18] B2-18 要求把 workflows/match_listing.py 的整个 `for r in todo:` 循环(:149-181)包进一个 `with db.pg_conn() as conn:`,在里面逐行 mint。但这个循环体里有 `_precheck(store, r["upc"], spec_cache)` —— 那是逐行的沃尔玛 API 调用(每店固定出口代理,有速率桶与重试退避)。几百行就是几百次网络往返,数据库事务被吊在整个过程上,而每次 mint 都在 catalog.listing_sources 上留行锁直到循环结束才提交。与 list_new 的 mint 并发时会互相等锁,且长事务本身是 PG 上的运维坏味道。
  - 修法:把 mint 与 _precheck 分两趟:第一趟只跑 _precheck/_gate_reason 把 todo 过滤成 ok 行(无连接);第二趟开一个短事务,对 ok 行批量 mint 并组载荷。这样仍满足「先落库再调接口」(mint 的 commit 早于 feeds.submit_feed),但事务只覆盖纯数据库操作。测试 tests/test_match_listing.py::test_mint_transaction_does_not_span_precheck_calls(桩里断言 precheck 调用全部发生在 pg_conn 上下文之外)。
  - 证据:workflows/match_listing.py:149 `for r in todo:` 循环内 :153 `pre = _precheck(store, r["upc"], spec_cache)`;:169-171 现有 make_sku 位置;:136 已有一个 `with db.pg_conn() as conn:` 只包 gate/banned 加载
- [批次 0a / 1 /  / acceptance_commands(EXPLAIN 与 psql DSN)] 两类验收命令跑不起来。① EXPLAIN:0a-13 写 `psql ... -c "EXPLAIN <把 _SQL_VARIANT_OFFSET 贴进来,参数给空数组>"`,0a 的 acceptance_commands 末条也要求对三处跑 EXPLAIN —— 但这些 SQL 全带 psycopg 命名占位符(`%(min_batches)s`、`%(asins)s::text[]`),粘进 psql 直接语法错。② DSN 三种写法并存:0a/1 用 `psql "$(python -c 'from registry import db;print(db.pg_dsn())')"`,0b 用 `psql "${WALMART_PG_DSN:-dbname=walmart_data}"`,横切用 `psql -d walmart_data`。后两种绕过 registry,横切那种把库名硬编码进验收步骤,违反铁律 3。
  - 修法:① EXPLAIN 改成可执行形态:`python -c "from registry import db; from services import maintenance_intents as m; import psycopg; c=db.pg_conn(); print(c.execute('EXPLAIN '+m._SQL_VARIANT_OFFSET, {'min_batches':2,...}).fetchall())"`,或在 tests/ 里加一个 needs_pg 的 EXPLAIN 用例断言计划里不含 `Seq Scan on walmart_items`(照抄 tests/test_risk_trace.py 的 needs_pg 夹具写法)。② 全部验收统一 `psql "$(python -c 'from registry import db;print(db.pg_dsn())')"`,横切的 `-d walmart_data` 与 0b 的 `${WALMART_PG_DSN:-…}` 全部改掉。
  - 证据:registry/db.py:12 `def pg_dsn()`;services/maintenance_intents.py:246 `%(min_batches)s`;services/risk_trace.py:132 `%(asins)s::text[]`;tests/test_risk_trace.py 的 needs_pg 夹具(0a-26 自己引用过它)
- [批次 3 / (全包行号)] 批次 3 是五个包里唯一行号系统性漂移的:O2 说 upsert_items 在 106-122(实际 102-119),O3 说 mark_missing 在 125-136(实际 121-136),required_changes 引文里 verify_deletions 被写成 :214-245 而实际是 :238-267。任务要求「先自己打开文件核对行号」,0a/0b/1/2 做到了(抽查二十余处误差 ≤2 行),批次 3 没有。执行者按 106-122 去改会落在 upsert_items 的中段而不是开头。
  - 修法:批次 3 落地前把全部 file:line 重核一遍(尤其 services/walmart_catalog.py、services/product_events.py、workflows/problem_scan.py 三处),并把批次 3 的每条 item 的行号改成核对后的实际值。
  - 证据:services/walmart_catalog.py:102 `def upsert_items`、:121 `def mark_missing`、:138 `_PROJECTION_SQL`;services/product_events.py:238 `def verify_deletions`

**minor**(8):

- [批次 1 / depends_on 第 2 条] B1 的 depends_on 写「批次 0 的清单里也列了『_mark_upc_conflicts 改按 (store, asin)』——若 0a 已经改完,本包 B1-09 降级为核验」。核对 0a 的 26 个 item,没有任何一条碰 services/listing_sheet._mark_upc_conflicts。执行者照这句话去「先看 0a 改没改」会白等,或者更糟:以为 0a 会做而跳过 B1-09,于是批次 2 的 upc_pool.sku 一存真码,撞库标记就静默归零。
  - 修法:删掉这句;把 B1-09 明确标成批次 1 独有、不可跳过,并在 B1 的 risks 里写清跳过的后果(0101119 撞库永不标 conflict ⇒ 反复领到坏号 ⇒ 重演 SKU_LOCKED 死循环)。
  - 证据:0a items 0a-01..0a-26 全表无 listing_sheet;services/listing_sheet.py:336-361 `_mark_upc_conflicts` 现状按 `WHERE sku = ANY(%s)` 反查
- [批次 1 / B1-09 零行为变化论证] 论证里写「旧写法只匹配得到已 mark_used(status='used')的行」。实际 SQL 是 `WHERE sku = ANY(%s) AND status <> 'conflict'` —— 它同时匹配 ''(未用)、claimed、used、bad_prefix 四种状态。结论(集合实际相同)大体仍成立,因为只有 claimed/used 的行才有非空 sku,但论证本身把一个推断说成了事实,而这批包的价值恰恰在于论证可复核。
  - 修法:把论证改成:旧写法按 upc_pool.sku 反查,只有 claimed/used 的行 sku 非空,故实际命中集等于 (店,ASIN) 名下的活号;新写法按 (store, asin) join 并保留 `status <> 'conflict'`,集合相同。并把 acceptance 第 9 条体检(同 (店,ASIN) 多个活号)作为合并前置。
  - 证据:services/listing_sheet.py:351-353 `"SELECT upc, sku FROM catalog.upc_pool WHERE sku = ANY(%s) AND status <> 'conflict'"`;refdata/schema.sql:249 status 无 CHECK,取值 ''/claimed/used/conflict/bad_prefix
- [批次 0b / 0b-21] orphan_codes 告警用 `lines.insert(0, ...)`,理由是「conventions §四 摘要首行点名」。但 lines[0] 现在是带 `🧪 [DRY-RUN] ` 前缀的抬头行,insert(0) 会把 ⚠ 行顶到它前面 —— dry-run 时摘要首行不再带 dry-run 标记,而 cli 的链通知恰恰只取首行(`notify_fmt.first_line_of`,cli.py:_fold_success)。于是一条 dry-run 的告警会以真跑的面目出现在飞书里。
  - 修法:改成插在抬头行之后:`lines.insert(1, ...)`,并把 mode 前缀也拼进告警行(`f"{mode}⚠ 疑似新码漏登记 …"`),或者干脆把 ⚠ 内容并进抬头行末尾。测试 test_summary_is_byte_identical_when_no_opaque_codes 之外再加一条断言:dry-run 时首行仍以 🧪 开头。
  - 证据:workflows/sources_backfill.py:69-72 `mode = "" if execute else "🧪 [DRY-RUN] "` 与 `lines = [f"{mode}在架未登记来源…"]`;cli.py `_fold_success` 取 `nf.first_line_of(parts[1])`
- [批次 2 / B2-11] `_SQL_RETIRE_COOLDOWN` 的 `left join catalog.listing_sources ls on ls.store = e.store and ls.sku = e.sku` 没有 `and ls.source_type = 'amz'`,却用 `coalesce(ls.source_key, e.sku) as asin` 当键。跟卖行的 source_key 是 GTIN,退役冷却会按 GTIN 建键,而 list_new 的闸判用的是 `(store_name, r["asin"])` —— 永远对不上,跟卖品的退役冷却闸恒不生效(而且不报错)。同批的 `_SQL_ABANDONED_GEN` 按 `source_type = %s` 传参,是对的,两条 SQL 口径不一致。
  - 修法:`_SQL_RETIRE_COOLDOWN` 的 LEFT JOIN 补 `AND ls.source_type = 'amz'`,与 0a 全部 15 处收口的身份表达式逐字一致(`coalesce(ls.source_key, w.sku)` 且 ls 限 amz)。加断言 tests/test_list_new.py::test_cooldown_sql_scopes_the_registry_join_to_amz。
  - 证据:0a-04/0a-11/0a-17/0a-23 全部带 `ls.source_type = 'amz'`;services/listing_sources.py:25 `SOURCE_MATCH = "match"  # source_key=匹配 GTIN`
- [批次 0a / 0a-07 mint 的重抽逻辑] new_modules 里 mint 的规格写「INSERT … ON CONFLICT DO NOTHING RETURNING sku;拿不到行 = 撞了三条唯一约束之一,重抽,至多 SKU_MINT_MAX_DRAWS 次」。三条约束语义完全不同:撞 PK (store,sku) 或撞全局 sku 唯一 = 随机撞码,重抽是对的;撞活码键唯一 `(store, source_type, source_key)` = **另一个进程刚给同一个品发了码**,重抽只会再撞一次,五次之后抛 RuntimeError 说「随机源坏了」—— 诊断词是错的,而正确处置是重新走一遍复用查询拿回那个码。执行者到这里还得自己做设计决定。
  - 修法:规格里把两类冲突分开:INSERT 用 `ON CONFLICT (store, source_type, source_key) WHERE abandoned_at IS NULL AND replaced_by IS NULL DO NOTHING` 之外的冲突才重抽;拿不到行时先重跑一次步骤①的复用查询,查到就返回该码(并发双 mint 的正解),查不到才当随机撞码重抽。测试 tests/test_sku_codec.py::test_concurrent_mint_returns_the_other_process_code(而不是现在那条 test_mint_redraws_on_conflict_then_raises_loudly 一锅端)。
  - 证据:0a-07 new_modules 原文;0a-02 建的两条唯一索引 listing_sources_new_sku_uk 与 listing_sources_live_key_uk 语义不同
- [批次 2 / 横切 / R7 / depends_on 调度层前置] CLAUDE.md 安全红线「新旧系统严禁对同一破坏性任务并跑」在批次 2 的 R7 与横切的 depends_on 里都只是一句散文(「合并前必须现场核一遍旧仓 product_clear / daily_cleanup 的 cron 是否已停」),没有任何验收命令、没有 preflight、没有责任人。批次 2 是唯一发 feed 的批次,旧仓若还在跑 RETIRE/DELETE,新仓的观测会把旧仓的动作当成自己的,四个弃码点就在错误时刻触发。
  - 修法:批次 2 的 acceptance_commands 第一条改成硬前置并给可执行形态:在所有者机器上 `crontab -l | grep -Ei 'auto_listing|retire_and_relist|product_clear|daily_cleanup'`(或 launchctl list)截图贴 PR;并在 workflows/sku_migrate 之外给批次 2 也加一条摘要前置:list_new 真跑前打印一行「旧仓调度已停(人工确认于 YYYY-MM-DD)」由 -p legacy_stopped=<日期> 显式传入,缺省即拒跑(⛔ 早退,不抛异常)。
  - 证据:CLAUDE.md 安全红线第 5 条;批次 2 risks R7 与横切 depends_on 末条;cli.py:REFUSED_MARK 是仓内既有的「前提不成立就 ⛔ 早退」机制
- [全部五个包 / estimated_pr_split] 任务明确问「工时与 PR 切分是否合理」。五个包给了 PR 数、文件数、行数(0a 约 26 文件、0b 净增约 220 行、1 约 +260/−60、2 约 700-900 行、3 三个 PR),但**没有任何一个包给出工时估算**,也没有给「哪几步要等所有者/等实测/等飞书建列」的日历依赖。而这条改造的关键路径恰恰不是写代码:批次 2 卡在四个来源字母定值 + 决策 A/B/C 拍板 + 七项单品实测,批次 3 卡在 SkuUpdate 三件实测,批次 0b/1 卡在所有者在飞书建四列。
  - 修法:每个包补一个 lead_time 段,把「人的等待」与「码的工时」分开列:0a ≈ 3-4 人日(无外部依赖,可立刻开工);0b ≈ 3 人日代码 + 飞书建 3 列(所有者,可并行);1 ≈ 2 人日 + 飞书建 V 列(硬前置);2 ≈ 5-6 人日 + 四字母定值 + 三项决策 + 七项单品实测(实测是关键路径,建议与 0a/0b 并行启动);3 ≈ 4-5 人日 + SkuUpdate 三件实测 + 四级投放节奏(1→10→一店→全店,每级之间要等一轮 catalog_sync)。
  - 证据:批次 2 depends_on「所有者前置(代码之外,试点真跑前必须完成):四个来源字母定值;决策 A/B/C 拍板;单品实测 (a)-(e) 全部跑完」;批次 3 depends_on「所有者机器单品实测六件」
- [批次 2 / acceptance 试点第 1 步] `python cli.py list_new --dry-run -p check_spec=1 -p store=<试点店>` 被列为试点第 1 步的常规验收,但 check_spec 会真调 LLM(仓内注释自述「会真调 LLM,但不领 UPC 不提交」),对 ready[:20] 逐行跑一遍。dry-run 里花钱这件事没有在验收里说破,试点期间若反复跑会累积成本。
  - 修法:验收行加注:check_spec=1 会真调 DeepSeek(走 llm_cache,同批重复预检不重复计费),试点期只跑一次;不需要看 spec 预检时用不带 check_spec 的 dry-run。
  - 证据:workflows/list_new.py:1592-1596,else 分支原文「(加 -p check_spec=1 可在提交前跑 spec 一致化预检:会真调 LLM,但不领 UPC 不提交)」;:1035-1040 _spec_precheck docstring「LLM 走缓存,同一批重复预检不重复计费」

**遗漏项**:
- 批次 2 缺一个 item:给 workflows/list_new.py run() 加 limit 参数(闸门之后、_prep_rows 之前截断)。没有它,「一店一品」的试点节奏没有落地物,而这是批次 2 唯一有行为变化的批次的止损闸。
- 五个批次包全部缺 rollback 段(横切包的散文里有一份,但横切包本身应被拆解并入各批次)。
- 五个包全部缺工时/日历估算,尤其缺「关键路径是所有者实测与飞书建列,不是写代码」这一判断。
- 缺一条决定:不透明码字母表 + 长度 + 「至少一个字母」口径归哪个模块(registry / sku_asin / sku_codec 三处提案互斥)。这条不定,0a 与 0b 与横切的守门测试无法同时为绿。
- 缺一条决定:横切包是否作为独立交付物存在。按现状它与 0a/2/3 在 DDL、registry、守门测试三层大面积重复且命名不一致。
- 批次 3 缺一个 item:形态 B 下 SkuUpdate 如何穿过 mp_conform.strip_unknown(services/mp_conform.py:687)。不解决则形态 B 每一行都变成「建第二条 listing」。
- 批次 2 缺可执行的「旧仓调度已停」前置闸(CLAUDE.md 安全红线),现在只是 risks 里的一句散文。
- synthesis required_changes #10 要求确认 services/match_sheet.py 的回执找行(:107-109 按 feed 台账 + r["sku"])在改码后仍成立——B1-22 只在文档里提了一句「本次不动」,没有任何测试钉住它。
- 缺一条跨包的「身份表达式唯一文本」定义。0a 定的是 SQL 侧 coalesce(ls.source_key, w.sku) + ls 限 amz,Python 侧 pick_asin;但横切 C0-DDL-5 与批次 2 的 _SQL_RETIRE_COOLDOWN 都写了不带 amz 限定的变体。建议在 conventions §九 里把这两条表达式写成可复制的字面量,守门测试按字面量比对。

